# 実機確認・権限が必要で AI が完了できない作業の一覧

- 作成日: 2026-08-05
- 最終更新: 2026-08-06（**#340（画面の再実装）の実装結果を反映**。SC-03 が表示を求める値のうち**維持率・借株料の累計・自動縮小の発動履歴・空売り比率**に供給元が無いこと、および **SC-01 §2 の収集間隔に供給元が無い**ことを追加。develop `9b0f96f` 時点・計画 submodule `d980a01` 時点）
- 前回更新: 2026-08-06（**#375（米国口座の現金口座対応）の実装結果を反映**。B-4「第 2 回 Q24」行が誤りであったことの**訂正**と、決済済み資金・GFV 発生回数の情報源が存在しないことの追加。develop `08982c0` 時点・計画 submodule `d980a01` 時点）
- 前回更新: 2026-08-05（**#385（Stage 1 稼働営業日の供給）の実装結果を反映**。市場休場日の判定源が無いことを B-4 へ追加。develop `73498f8` 時点）
- 作成者: Claude Code（監査セッション）

## この文書の目的

AI（Claude Code）と CI だけでは**原理的に完了できない**作業を洗い出す。以下の 2 種類に分ける。

- **A. 実機・実環境が要るもの** — 実口座・実ブローカー・外部サイト・本番相当のデータ量が必要
- **B. 権限・人間の判断が要るもの** — GitHub の設定権限、資金の移動、計画側の裁定

**「実装が終わっていない」だけの作業は含めない**（それらは通常の issue で追える）。ここに挙げるのは、**誰かが実物に触るまで永久に終わらない**ものだけである。

解消した項目は削除せず「**解消済み**」として日付とともに残す。**解消の過程で本文書の記述が誤っていたと判明した場合は、誤りも消さずに訂正として残す**（同じ推測を繰り返さないため）。

> ⚠️ **重要な前提**: 現時点で**実弾の発注は 1 件も行われない**。`LiveTradingGate.LiveTradingReleased = false`（定数・2026-08-05 実測で不変）が閂として効いており、解禁は 1 ファイルの変更に集約されている。本一覧の A 群が未了である限り、この閂を開けてはならない。
>
> 2026-08-05 の PoC では**実弾口座への読み取り照会**（`TrdGetMarginRatio` 等）が利用者の手で行われたが、**発注系（`PlaceOrder` / `ModifyOrder`）は実弾環境で一度も呼ばれていない**。読み取りと発注を混同しないこと。

### 2026-08-05 の更新について

別セッションで**実機確認が実施され**、A 群の大半が動いた。反映元は次のとおり。

| 反映元 | 内容 |
| --- | --- |
| [#342 のコメント](https://github.com/endazon/ai-stock-trading/issues/342#issuecomment-5192650678) | moomoo PoC の実測結果（A-2 のほぼ全体） |
| [#381 のコメント](https://github.com/endazon/ai-stock-trading/issues/381#issuecomment-5191598064) | 日銀の系列 ID 確定と、収録スケジュールの発見（A-4） |
| [#382 のコメント](https://github.com/endazon/ai-stock-trading/issues/382#issuecomment-5192654759) | 米国株日足 OHLC の代替源（A-3） |
| PR [#394](https://github.com/endazon/ai-stock-trading/pull/394)・[#395](https://github.com/endazon/ai-stock-trading/pull/395)（いずれもマージ済） | 環流記録・IADR-0144・作業仕様書 |

### 参照する計画 ADR の状態について

本文書が根拠として引く **ADR-0016 / 0018 / 0019 / 0022 / 0023 はいずれも `Proposed` である**（`Accepted` ではない。submodule pin `d980a01` で実測）。ただし**裁定の内容そのものは確定している**。ADR-0022 の原文:

> **状態を `Proposed` に留めている理由**: 本 ADR の決定の内容は利用者裁定により確定している。**`Proposed` はその裁定を疑うものではない。** 保留しているのは記録としての承認だけである。…**`Proposed` は決定の効力を停止しない**。

保留の理由は、計画側で `/sync-impl` が **GitHub API 401（`Bad credentials`）で失敗**し、実装リポジトリへの読み取りアクセスが得られず**実装 IADR との突合が未了**であるためとされている。突合が済み次第 `Accepted` へ移る予定である。

**したがって本文書の期限・数値はそのまま有効**だが、「`Accepted` 済みの確定計画」と読むと状態表記と食い違う。なお**この 401 自体が B 群と同種の権限起因の詰まり**であり、計画側の記録承認を止め続けている点は留意されたい（本リポジトリからは解消できない）。

---

## A. 実機・実環境が要るもの

### A-1. ⏰ Hetzner（海外 IP）からの接続可否・ToS 適合の確認 — **期限が目前・唯一未着手のまま**

| 項目 | 内容 |
| --- | --- |
| 期限 | **2026-08-15**（計画 ADR-0019 決定 2 の工程 ①）。残り 10 日 |
| 追跡 | **#342**（moomoo PoC）。**#132 は #342 の duplicate としてクローズ**（2026-08-02）され、Hetzner ToS と残検証は #342 が引き継いでいる。インフラ全般は #24 |
| 現況 | **未着手のまま。** 2026-08-05 に PoC 本体（A-2）は実施されたが、[#342 のクローズ条件](https://github.com/endazon/ai-stock-trading/issues/342#issuecomment-5192686265)の筆頭として残っている |
| なぜ AI にできないか | 提供側の利用規約に対する法的・契約的判断が要る |
| 未了の帰結 | **3 か月間 OpenD を常駐させる先が確定しないと Stage 1 の期間の起算そのものを始められない。** 加えて、PoC 項目 6（長期常駐の安定性）の実測値は**常駐先に依存するため、Hetzner へ移す場合は再測が要る** |

> **⚠️ 訂正（2026-08-05）— 本項の「未了の帰結」が誤っていた。**
> 旧記述は ADR-0019 §理由を引いて「**これが決着しないと PoC もやり直しになる**」としていた。
> **PoC はローカル（Rancher Desktop の k3s・単一ノード）で実施されたため、この理由は当たらない。**
> ADR-0019 決定 2 が ①→② の順序を課した根拠は「PoC を実施する環境そのものが Hetzner 上にあるなら」という
> 条件付きであり、ローカル実施ならその条件は成立しない。**ToS が不成立でも、PoC が確認した
> 「API に当該機能があるか」という結果は失効しない。失効し得るのは項目 6 の実測値だけである。**
> （出典: [作業仕様書 20260805_342](specs/20260805_342_moomoo-poc-plan.md) §目的・背景。PR #395・マージ済）
>
> **私は ADR の条件節を落として無条件の帰結として転記していた。** 期限の切迫を過大に見積もる方向の誤りだった。

**依然として最優先。** ただし理由は「PoC がやり直しになるから」ではなく、**Stage 1 の起算が始まらないから**である。追跡そのものは #342 にあり、必要なのは着手判断である。

### A-2. moomoo OpenAPI PoC 7 項目 — **✅ 大半が 2026-08-05 に解消**

実 OpenD（常駐中）に対し、**本番コードと同一の SDK**（`moomoo-api` 10.8.6808）でクラスタ内の使い捨て Pod から probe を実行した。**発注を伴う操作と実弾口座への読み取り照会は利用者が実行した。実弾での発注は行われていない。**

> **⚠️ 訂正（2026-08-05）— 旧版の項目表が ADR-0019 決定 1 と一致していなかった。**
> 旧版は項目 2 を「逆指値を建玉と同時に発注できるか」、項目 3 を「借株可否・借株料の事前照会」と
> 書いていたが、**ADR-0019 決定 1 の項目 2 は「同口座で空売り注文を API 発注できるか」であり、
> 項目 3 には「維持率」が含まれる**。逆指値の同時発注は ADR-0016 決定 2(b) をゲートする別の確認事項である。
> 下表は**計画書（ADR-0019 決定 1）を正として転記し直した**。

| # | 確認事項（ADR-0019 決定 1） | 結果（2026-08-05） | 不成立時の帰結の適用 |
| --- | --- | --- | --- |
| 1 | US Margin Paper Trading Account に接続できるか | ✅ **成立**（accId=724808・`Simulate` × `Margin` × US） | なし |
| 2 | 同口座で**空売り注文を API 発注**できるか | ✅ **成立**。ただし `TrdSide_SellShort` は**拒否される**。建玉を持たない `Sell` が新規ショートになる | なし（実装方針は IADR-0144 決定 1・2） |
| 3 | **維持率・借株料・借株可否**を取得できるか | 🟡 **条件付き成立**。**実弾口座でのみ**取得できる（SIMULATE では照会 API 自体が失敗）。借株料は 5 銘柄すべて `1.5` で**銘柄別に動かない** | **ADR-0016 決定 3 の 20% 閾値が発火しない**（B-4 へ） |
| 4 | 実弾口座とペーパー口座の API での明示的切替 | ✅ **成立**（`TrdEnv` は列挙で分離・`TrdHeader` に必須・別 `AccID`） | なし |
| 5 | **強制買戻し（buy-in）**のイベント検知 | ⏸ **未確認**。SDK に専用の型が無く、**SIMULATE では原理的に発生しない**（ADR-0016 決定 14） | **`BuyInBanned`（#374）の供給元は依然として無い**（B-4 へ） |
| 6 | OpenD の強制アップデート頻度・長期常駐の安定性 | 🟡 **部分**。7 日 20 時間・5 回の再起動を**無人で継続**。ただし単一ノード（egress IP 安定）の観測 | 判定保留。**Hetzner へ移す場合は再測** |
| 7 | **米国株の日足 OHLC 履歴**の取得可否と品質（2026-08-04 追補） | ✅ **成立**（A-3 参照） | なし。ADR-0005 の有料枠へ移る必要は生じない |

- 期限: **項目 1〜6 は 2026-08-31**（**達成**）／**項目 7 は go-live 起算 1 か月以内**（ADR-0019 決定 2 の ② と ⑤・起算点が異なる）
- 追跡: **#342 は実装への反映が済むまで open のまま維持される**（[クローズ条件](https://github.com/endazon/ai-stock-trading/issues/342#issuecomment-5192686265)）
- 記録: [IADR-0144](adr/IADR-0144_moomoo-short-selling-poc-outcomes.md)・[作業仕様書 20260805_342](specs/20260805_342_moomoo-poc-plan.md)（PR #395・マージ済）

> **📌 項目 7 の位置づけ — ✅ 是正済み（2026-08-05）**
>
> PR #395 の記録群（IADR-0144・作業仕様書・環流記録 2 件・#342 のコメント）は当初、項目 7 を
> 「**ADR-0019 に無い補助項目**」「ADR-0019 決定 1 の**6 項目**」と記述していたが、
> **ADR-0019 決定 1 は 7 項目である**（submodule `d980a01` で実測）。
>
> - ADR の表題: 「Margin Paper Trading Account **ほか 7 項目**」
> - §決定: 「次の **7 項目**を追加する」
> - §決定 1 の見出し: 「PoC 項目（**7 項目**）と不成立時の帰結」（表は 7 行）
> - 項目 7 の追補注記: 「**起案時は 6 項目であった。**〔…〕その検証を項目 7 として追加した（2026-08-04 利用者裁定）」
>
> 「6 項目」は**決定 2 の工程 ②「PoC 項目 1〜6 … 2026-08-31」**に由来する区切りであり、
> 決定 1 の項目数ではない（項目 7 は工程 ⑤ として**別の期限**を持つ）。
>
> **PR #395 は 5 ファイルすべてを是正済み**（結果表への項目 7 の行追加・提案 1 の「項目 1・2・4・7 は成立」を含む）。
> 発見は **AI レビューが先**（[初回レビュー](https://github.com/endazon/ai-stock-trading/pull/395#issuecomment-5192639651)・2026-08-05 13:52）であり、
> 本文書からの[指摘](https://github.com/endazon/ai-stock-trading/pull/395#issuecomment-5193234814)（14:42）は独立だが後発である。
> **本項は「同じ計画書を読んだ 2 者が同じ取り違えをしなかった」記録として残す** ——
> 決定 1（項目の一覧）と決定 2（工程の期限区切り）で数が食い違う ADR は、今後も同種の誤読を招く。

### A-3. 米国株の日足 OHLC 履歴源の確定 — **✅ 代替源が判明（採用は #382）**

| 項目 | 内容 |
| --- | --- |
| 旧現況 | **Stooq は実質的に取得不能**（JavaScript proof-of-work のボット検知。ADR-0023 決定 1）。計画は**回避実装を明示的に禁止**し、**代替源を定めていなかった** |
| **新現況（2026-08-05）** | **moomoo `QotRequestHistoryKL` で取得できる。** AAPL が **2006-07-24 まで**（約 20 年）遡れる。前復権・OHLCV・1 リクエスト 1,000 件・`NextReqKey` でページング・`remainQuota: 300`。**追加費用なし**（既に接続している OpenD で取れるため ADR-0005 の有料情報源プロセスに乗せる必要が無い） |
| 実装の状態 | `StooqHistoricalBarSource` が**唯一の**履歴プロバイダのまま。既定は `none`（no-op）で安全側に倒れている。**moomoo アダプタは未実装** |
| **残る未確定事項（実機確認が要る）** | ① **取得枠の単位と回復周期**（`remainQuota: 300` が銘柄数かリクエスト数か。2 リクエスト投げても `usedQuota` は 0 のままだった）。バックテストで多数銘柄を遡ると枠を使い切り得る ② **前復権の調整方式がバックテストの費用モデルと整合するか**（ADR-0016 決定 14 は借株料・配当相当額を織り込むと定めており、二重計上・欠落を起こさないかの確認が要る） |
| 未了の帰結 | **代替源が判明しただけで、実装が入るまで FR-15 のバックテストに流せるデータは 1 件も無い。** Stage 0 の合格判定（#384 の DD ≤ 10% 厳格化を含む）は依然として一度も発火しない |
| 追跡 | **#382**（採用の判断と実装）。計画側は ADR-0023 の改定（環流済み・B-4） |

### A-4. 日銀 時系列統計API の系列 ID の確定 — **✅ 解消済み（2026-08-05）。ただし別の閉塞が判明**

| 項目 | 内容 |
| --- | --- |
| **結果** | **系列 ID は `FM08'FXERD04`（東京市場 ドル・円 スポット **17時時点**）で確定。** 単位 `￥／＄`・期種 `DAILY`・収録開始 1998/01/05 |
| 計画側の誤り 1 | **ADR-0022 決定 1 の注記が引く「中心相場」は誤り。** 決定 1 の表は「17 時時点（**オファーとビッドの仲値**）」と書いており、これに一致するのは `FXERD04` である。日銀の解説によれば**中心相場（`FXERD05`）は「最も取引の多かった出来値」**であって仲値ではない |
| 計画側の誤り 2 | **「HTTP 403」は再現しない。** 日銀は**利用登録も API キーも不要の公式 API** を提供している（`https://www.stat-search.boj.or.jp/api/v1/getDataCode`）。`STATUS,200` で取得できることが実測済み |
| 実装上の注意 | **`code` に渡すのは系列コード `FXERD04`**。画面で使うデータコード `FM08'FXERD04` を渡すとエラーになる（DB 名は `db=fm08` で別に渡す）／日次でも期間指定は**月次形式 `YYYYMM`**／該当日にデータが無い場合は値が `null`（**欠測と休場を区別できない**）／1 リクエスト 250 系列・60,000 データ、超過は `NEXTPOSITION` でページング |
| 利用条件 | **商用利用しない裁定（2026-08-05）のため事前相談は不要。** API 側の「利用時の連絡」「クレジット表示」の義務はいずれも原文が「**本機能を使用したサービスを公開した場合**」という条件付きであり、私的運用では文言上発生しない。**ただし ADR-0022 が定めた「報告書の為替欄に出典を明記」は維持を提案**（義務としてではなく値の出所を追うため） |
| 追跡 | **#381**（[実機確認コメント](https://github.com/endazon/ai-stock-trading/issues/381#issuecomment-5191598064)）／環流記録 `feedback/20260805_adr0022-boj-series-id-and-freshness.md`（PR #394・マージ済） |

> **⚠️ 訂正（2026-08-05）— 本項の「なぜ AI にできないか」が誤っていた。**
> 旧記述は「**日銀サイトが自動取得を拒否している。人が一次情報で系列 ID を確認する必要がある**」としていたが、
> **403 は再現せず、公式 API が存在する。** 私は ADR-0022 の起案時点の記述（「本環境からの取得に対し HTTP 403」）を
> **環境依存の観測として疑わずに一般化し**、「AI には原理的にできない」と分類していた。
> **実際には取得できた。** 外部サイトの取得可否を伝聞のまま「原理的な不能」に格上げしないこと。

#### 🔴 A-4 の確認で判明した新たな閉塞 — 収録は「翌々営業日 8:50 頃」

**これは推測ではなく、日銀の公表・掲載予定表（`tkohyos.xlsx`）に明記された事実である。**

| 対象 | タイミング | 頻度 |
| --- | --- | --- |
| 外国為替市況（本体の **PDF 公表**） | **17:50 頃** | 毎営業日 |
| 外国為替相場状況（…日次）＝**検索サイト／API への収録** | **翌々営業日 8:50 頃** | 毎営業日 |

**ADR-0022 決定 1 の表が「公表頻度: 毎営業日」と書いているのは本体 PDF の話であり、実装が読む経路の鮮度ではない。**

平常時（8:50 以降）の「最新データの古さ」は次のように決まる。

| 取得曜日 | 最新データ | 古さ |
| --- | --- | --- |
| 水・木・金 | 2 営業日前 | 2 日 |
| **月** | 前週木 | **4 日** |
| **火** | 前週金 | **4 日** |

**平常時でも週 2 日は必ず 3 日を超える。** 3 連休明けの火曜は 5 日、8:50 の収録前に取得すればさらに 1 営業日分古くなる。

決定 4 は「二段構成であれば平常時に 3 日を超えることはまずない。3 日を超えたら『両方の源が取れていない』という異常であり、警告として意味のある値になる」としているが、**この前提が成立しない**。正常時に発火し続けると、決定 4 が 7 日・14 日を却下した理由（異常の検知が遅れる）と同種の失敗を逆方向で招く。

**FRED フォールバックはこれを救わない。** `DEXJPUS` は公表が週次であり、月曜・火曜に日銀側が 4 日前しか持たない局面で FRED がそれより新しい保証がない。決定 3 が期待した「日本の連休中も FRED 側は動いている」という冗長化は、**日本の平日に出る収録遅れ**には効かない。

- **#381 の「やること 3」と受け入れ基準は、ADR-0022 決定 4 の改定が確定するまで確定できない。とくにしきい値 3 日をそのまま実装すると、稼働直後から週 2 日の誤警告が出る。**
- 環流記録は 3 案を挙げ、**案 A（5 日へ緩める）を最小の変更**として推している（案 B = 営業日ベースは決定 3 が明確に却下したカレンダーの持ち込み、案 C = 17:50 の PDF を一次取得元にするのは PDF 解析という新たな脆さ）。
- **決定 5（絶対上限 30 日・超過で新規建て停止）は影響を受けない**（4 日は 30 日を超えない）。
- 裁定待ちとして **B-4** に登録した。

### A-5. 本番相当の PostgreSQL に対するマイグレーション適用

| 項目 | 内容 |
| --- | --- |
| 検証済み | PostgreSQL 16.13 を起動し、**既存データ 1 行を投入した状態で `Up`／`Down` の両方が通ることを実測**（#384 の `AddStage1Progress`）。`dotnet ef migrations has-pending-model-changes` も差分なし |
| **未検証** | **本番相当のデータ量・並行接続下での適用**。単発・空に近い DB でしか確認していない |
| なぜ AI にできないか | 本番相当のデータを持つ環境が要る |
| リスクの程度 | **低い**と見ている。列追加は `DEFAULT` 付きで PostgreSQL 11 以降テーブル書き換えを伴わない。ただし「低い」は**推定であって実測ではない** |
| 対象 | `20260804090000_AddStage1Progress`（#384）／`20260805044003_AddStage1ExcludedInternalPaperDays`（#390） |

### A-6. Playwright E2E のローカル実走

| 項目 | 内容 |
| --- | --- |
| 現況 | サンドボックスで**ブラウザ取得が失敗**する（`Failed to download Chrome for Testing`）。実走は **CI の `frontend-e2e`**。**2026-08-05 の利用者裁定により AI レビュー側でも Playwright の実走が許可され**（#391 ①）、PR #396 のマージで**実証経路が 2 本になった** |
| 実害 | #390 で既存 spec の期待値ずれを**ローカルで検出できず、CI で初めて落ちた**（38 passed / 1 failed） |
| 緩和策 | 同じ検証内容を vitest（jsdom）にも二重に置いている。加えてレビュー側の実走が入れば経路が 2 本になる |
| なぜ AI にできないか | 環境側の制約。CI では動くため、**運用としては「E2E は CI に委ねる」で成立している** |

### A-7. Discord Bot の実通知・昇格承認操作

| 項目 | 内容 |
| --- | --- |
| 対象 | FR-11（監査ログ・通知）／FR-20（段階昇格の利用者承認） |
| なぜ AI にできないか | 実 Discord サーバと Bot トークン、および**利用者本人による承認操作**が要る |
| 未了の帰結 | 段階の昇格・差し戻しは**設計上すべて利用者承認を要する**ため、この経路が動かないと段階は 1 つも上がらない |

### A-8. 🆕 moomoo アカウントの**日本株 市況権限**が無い（2026-08-05 発覚）

| 項目 | 内容 |
| --- | --- |
| 実測 | OpenD の起動ログが **`JPN Stocks: No permission`** を出している（`US Stocks: LV3` に対して）。出典: [作業仕様書 20260805_342](specs/20260805_342_moomoo-poc-plan.md) §実測結果 |
| 計画との食い違い | 計画（06_technical/03_moomoo-integration）は**日本株の市況取得・現物発注が 2026-06 から可能**と記載している |
| なぜ AI にできないか | moomoo アカウント側の権限申請・開設操作が要る |
| 未了の帰結 | **日本株の市況を要する機能が動かない**（FR-02 の価格変動検知ほか）。日本株は差金決済ガード（#332）・監視銘柄（7203 等）で前提にされており、**影響範囲は市況取得にとどまらない可能性がある** |
| 未確定 | **市況権限と発注権限が別建てかどうかが分かっていない。** 「日本株が一切扱えない」と読むのは早計であり、切り分けが要る |
| 追跡 | **#397** |

---

## B. 権限・人間の判断が要るもの

### B-1. 🔴 `CODEOWNERS` の設定 — **AI のレビュー関門が形骸化している**

| 項目 | 内容 |
| --- | --- |
| 現況 | **`.github/CODEOWNERS.example` のまま**。`CODEOWNERS` は存在しない（実測） |
| なぜ重大か | `AI_SETUP.md` 共通セットアップ 4 の原文: 「**AI が実装し AI がレビューする運用では、必須レビュアが不在だと『AI の実装を AI が承認して同一人物がマージする』ループになり、人間のレビュー関門が形骸化する**」 |
| 実際に起きていること | **監査セッションで #392 / #390 / #393 を自分でマージした。** 実装も監査も AI であり、人間のレビューは介在していない |
| なぜ AI にできないか | ファイルのリネーム自体は可能だが、**レビュアに誰を指名するかは利用者の決定**であり、ブランチ保護での必須化には管理者権限が要る |

**B 群の中で最優先。** 資金を扱うシステムで、AI が書いた統制コードを AI が承認してマージしている状態である。

### B-2. ブランチ保護と必須ステータスチェックの設定

| 項目 | 内容 |
| --- | --- |
| 対象 | `ci` / `security` / `codeql` を必須チェックにする（`docs/ai-workflow.md` に手順） |
| なぜ AI にできないか | リポジトリ管理者権限が要る |
| 現況の懸念 | **必須チェックが未設定なら、赤い CI のままでもマージできてしまう**。`claude-review` が赤のまま #390 をマージできた事実は、少なくともこのチェックが必須でないことを示す |

### B-3. GitHub Secrets の登録

| 項目 | 内容 |
| --- | --- |
| 対象 | `CLAUDE_CODE_OAUTH_TOKEN`（サブスク）**または** `ANTHROPIC_API_KEY`（API）。**どちらか一方のみ**（両方設定すると競合する） |
| 現況 | `.ai-profile` は `claude-code`。GitHub 上で AI レビューが動作しているため登録済みと推定されるが、**セッションからは確認できない** |
| なぜ AI にできないか | Secrets の読み書き権限が無い |

### B-4. 計画側の裁定待ち（project-planning）

**2026-08-05 の実機確認で 4 件増えた。** いずれも「実装できない」のではなく「**実装しても意味を成さない／根拠が崩れた**」種類である。

| 待ち項目 | 内容 | 影響 |
| --- | --- | --- |
| 🆕 **ADR-0022 決定 4** | **警告しきい値 3 日**の前提（「平常時に 3 日を超えることはまずない」）が、日銀の収録スケジュール（翌々営業日 8:50）により**成立しない**（A-4）。案 A = 5 日／案 B = 営業日ベース／案 C = PDF 一次取得 | **#381 の「やること 3」がブロック。** 3 日のまま実装すると稼働直後から**週 2 日の誤警告** |
| 🆕 **ADR-0016 決定 3** | 「借株料 年率 20% 超を拒否」は**この情報源では発火しない**（空売り可能な 5 銘柄すべて `ShortFeeRate=1.5`。在庫が 20 倍開いても動かない）。実際に危険な銘柄を弾いているのは `IsShortPermit=False` | #329 / #331。**一次ゲートを可否へ移すかの裁定**。実装側は閾値判定を残置し「発火しない既知の統制」として扱う方針（IADR-0144 決定 4） |
| 🆕 **ADR-0016 決定 14 の表** | 決定 3・決定 7 を「SIMULATE は年率 6% 固定のため 20% を超えない（＝値は取れる）」と説明しているが、**実態は照会 API が SIMULATE で使えない**（値そのものが取れない） | 「Stage 1 で検証できない統制」の範囲が**当初想定より広い**ことの追認 |
| 🆕 **ADR-0023** | 代替源を定めていない。**moomoo を米国株日足 OHLC の履歴源とするか**（追加費用なし。ただし単一障害点が進む） | **#382**。実装の着手根拠 |
| **ADR-0016 決定 4**（項目 5） | 強制買戻しの検知。SIMULATE では原理的に発生せず、専用 API も無い。**事後の突合で代替するか、決定 4 を保留するか** | **#374 の `BuyInBanned` が永久に立たない** |
| ~~**第 2 回 Q24**~~ **【訂正・解消済み 2026-08-06】** | ~~米国口座の**現金口座**対応（ADR-0021）。口座種別の判定方法・現金口座で要する統制（差金決済ガードの米国株適用・GFV 回避）・ADR-0016 の前提の明記~~ **本行は誤りであった（削除せず訂正として残す）。裁定は 2026-08-04 に済んでおり、待ち項目ではなかった。** 根拠は 3 点。(1) **[ADR-0021](../planning/projects/ai-stock-trading/07_adr/ADR-0021_us-account-type-dual-support.md) 24 行が「詳細は第 2 回 Q24 で裁定済み」と明記している**（決定1〜5 が本行の「待ち」3 点にそのまま答えている）。(2) ADR-0021 が `Proposed` であることは待ちの根拠にならない。**計画リポの `.claude/rules/adr.md` が「`Proposed` は記録としての承認が保留であることを示すにすぎず、決定の効力とは別の軸である。利用者裁定によって内容が確定した決定は ADR が `Proposed` であっても計画側の現行値である」と定めている**（同ファイル「状態」節）。`Proposed` に留まる理由は `/sync-impl` の GitHub API 401 のみである。(3) **判定手段は PoC 項目 1 が実測で裏付けている**（`TrdGetAccList` / `TrdAcc.AccType` / `TrdAccType` = `Cash` / `Margin` ほか。`docs/specs/20260805_342_moomoo-poc-plan.md` 79 行「現金口座と信用口座を API で判別できる」）。**本一覧が「判定方法が未裁定」と書いていたのは、同じ文書の別セクションに実測結果があることを見落としたためである。** | **#375 は 2026-08-06 に実装した**（IADR-0153）。**PoC で実弾側に現金口座と信用口座の両方が実在することが判明**し「どちらを使うか」は依然として運用の選択だが、それは裁定待ちではなく設定の問題である。**ただし別の理由で現金口座はまだ選べない**——決済済み資金と GFV 発生回数の情報源が存在しない（下記「実装済みだが発動しない機能」を参照）。環流記録 `feedback/20260806_adr0021-rejection-reasons-and-settled-cash.md` |
| ADR-0022 決定 15 | 「強制買戻しの**発生回数**」の集計元。`BuyInBanned` の**拒否件数は発生回数にならない**（1 回の買戻しに対し禁止期間中の拒否は何度でも起こる） | 日報・月報の記載が実装できない |
| ADR-0016 決定 14 | 空売り実弾解禁の verdict の形式（有効期限・再検証の要否） | **#388** |
| FR-20 §4.2 | 半日取引日カレンダーの判定源 | **#385 で供給元を実装済み**。分母を発明せず「**取り得る通常取引時間の仮説すべてで 50% 以上**」を要求する形にした（IADR-0137 決定 1 / IADR-0150 決定 3）。**半日取引日は過少計上へ倒れる**（安全側） |
| 🆕 **FR-20 §4.2 の除外「市場休場」** | **祝日の判定源が無い。** OpenD は市場が閉じていても接続を保つため、稼働 probe は休場日も成功し続ける。週末は曜日の算術で外せるが**祝日（年 9 日前後）は外せない** | **#385 で判明**。営業日数を **60 営業日あたり 2〜3 日 過大計上し得る**（他の未供給と違い fail-safe ではない）。実装は表を発明せず限界として記録（IADR-0150 決定 5・環流記録 `feedback/20260805_fr20-stage1-market-holiday-exclusion.md`）。**半日取引日の判定源と同一の情報源で同時に解決できる** |

環流記録は `feedback/20260805_adr0016-0019_moomoo-poc-outcomes.md`・`feedback/20260805_adr0023_us-ohlc-source-moomoo.md`（PR #395・マージ済）／`feedback/20260805_adr0022-boj-series-id-and-freshness.md`（PR #394・マージ済）／`feedback/20260805_fr20-broker-provider-open-points.md` ほかにある。**planning リポへの起票は未実施**のものがある。

### B-5. 増資 $3,000 の実行

| 項目 | 内容 |
| --- | --- |
| 期限 | Stage 1 開始前（ADR-0019 決定 2 の工程 ③） |
| 担当 | **利用者**（project-planning#84） |
| なぜ AI にできないか | 実資金の移動 |

### B-6. AI レビューの許可リスト修正の**方針決定** — **✅ 裁定済み（2026-08-05）**

| 項目 | 内容 |
| --- | --- |
| 追跡 | **#391** ／ PR **#396**（マージ済・develop `209dbc8`） |
| 裁定 ① | **Playwright を許可する**（レビューでも E2E を実走する）。ブラウザ取得に 1〜2 分かかることを受け入れる |
| 裁定 ② | 「ベストを選択」→ **拒否を「許可リストで直せるか」で分類し、失敗判定を直せるもの（A）だけで行う**（IADR-0145）。しきい値 4 は据え置き、直せない形（B）は**必ず可視化するが失敗させない** |
| 起票時の前提の誤り | #391 は「拒否 1 件でジョブが落ちる」と書いていたが、`check-permission-denials.js` は既に段階ポリシー（既定 4 件超 or ターン数の半分）を持っていた。実測の失敗要因は **5 件でしきい値をわずかに超えたこと**であり、内訳も npm / dotnet ef ではなく `git -C <絶対パス>` 2 件と MCP ツール 3 件だった |
| 残る懸念 | **`.claude/settings.json` は deny 規則により AI が編集できない**ため、3 系統目の許可リスト同期は人手が要る（`ai-workflow-config` が warn で検知する） |

---

## 優先順位（推奨）

| 順 | 項目 | 理由 |
| --- | --- | --- |
| 1 | **A-1** Hetzner ToS 確認（#342） | 期限 2026-08-15 まで 10 日。**PoC は済んだが、これが決着しないと Stage 1 の起算が始まらない** |
| 2 | **B-1** `CODEOWNERS` の設定 | AI が書いた統制コードを AI が承認してマージしている状態を止める |
| 3 | **B-4** ADR-0022 決定 4 の改定 | **3 日のまま実装すると稼働直後から週 2 日の誤警告が出る**。#381 の実装がこの裁定待ちで止まっている |
| 4 | **B-2** ブランチ保護 | 赤い CI でマージできる状態を止める |
| 5 | **B-4** ADR-0016 決定 3・ADR-0023 | 空売り統制（#329 / #331）と履歴源（#382）の着手根拠。**PoC が済んだ今、実装を塞いでいるのは裁定だけ** |
| 6 | **A-8** 日本株の市況権限（#397） | 影響範囲が市況取得にとどまらない可能性がある。**まず切り分け** |
| 7 | **A-3** 履歴源の実装 | 代替源は判明した。残るのは取得枠と復権方式の確認、および実装 |

---

## 補足: 「実装済み」だが実際には発動しない機能

A 群・B 群が未了であるため、以下は**コードは存在するが一度も動作しない**。運用開始時に「実装済み」と誤認しないための一覧。

| 機能 | 実装 | 発動しない理由 | 担当 |
| --- | --- | --- | --- |
| 維持率割れの自動縮小（FR-10・UC-06） | #330 | **照会 API が SIMULATE で使えず、実弾ヘッダでの読み取りが要る**（PoC 項目 3）。定期評価ドライバも無い。**#340 で SC-03 は維持率を「取得できていません（供給元がありません）」と明示表示するようにした**（0 や「—」で正常値に見せない・IADR-0154） | #331 / #342 |
| 借株料 20% 超の拒否（ADR-0016 決定 3） | #329 | **借株料が銘柄によらず一律 `1.5`** のため閾値に届かない（PoC 実測） | **B-4 の裁定待ち** |
| 🆕 **借株料の累計の記録**（ADR-0016 決定 15・SC-03） | **未実装** | **累計を保持する型・ストア・イベントがコード全体に存在しない**（`ShortSellOrderContext.BorrowRateAnnual` は 1 注文の事前照会入力であり累計ではない）。**#340 で SC-03 は「0 ではなく不明」と明示表示するようにした**（費用が発生していないと読ませない） | **計画側で記録要件の明記が要る**（環流記録 `feedback/20260806_sc01-sc03-unsupplied-screen-items.md` 問題 4） |
| 🆕 **維持率割れ自動縮小の発動履歴の参照**（SC-03） | **未実装** | 発火元（維持率）が無く、Risk 側に**履歴ストアも照会 API も無い**。報告書側は `NoMarginReductionRecordSource`（空列）。**#340 で SC-03 は「発動なし」ではなく「記録する経路がない」と表示するようにした** | 上に同じ（供給元の実装は #331 / #342） |
| 🆕 **空売り比率の現況**（ADR-0016 決定 9・SC-03） | **条件付き未供給** | 分母（建玉総額）は**時価**であり、現在値の供給（`MarketData:EnableMarkToMarket`）は**既定 false**。取得原価で代用すると別物の比率になり上限 50% の判定を誤らせるため、**#340 は算出せず「取得できていません」と表示する** | 現在値供給の有効化（#81 / IADR-0066 の経路） |
| 🆕 **収集間隔の画面からの変更**（FR-13・SC-01 §2） | **未実装** | **設定ストアもエンドポイントも存在しない**（起動時構成 `Collection:PollIntervalSeconds` / `Monitor:PollIntervalSeconds` のみ）。動的変更には稼働中の `BackgroundService` が値を読み直す機構が要る。**#340 は入力欄を作らず「本画面から閲覧・変更できません」と明示した**（動かない入力欄は「変更できたつもり」を生む） | **計画側の裁定待ち**（環流記録 同上 問題 1）。#19 の成果物には含まれていない |
| 強制買戻し後の空売り禁止（`BuyInBanned`） | #374 | `BuyInBanUntil` の供給経路が無い。**PoC 項目 5 は未確認のまま** | B-4 の裁定待ち |
| Stage 0 合格判定（DD ≤ 10%） | #333 | **代替源（moomoo）は判明したが未実装。** 日足 OHLC が 1 件も入っていない | **#382** |
| Stage 1 の期間・件数カウント | #333 / #334 / **#385** / **#386** | **解消済み（2026-08-05）。** 稼働分数は定期 probe の観測（`BrokerAvailabilityObserved` → `stage1_session_uptime`）から、約定件数は `OrderExecuted` の観測から供給される。**ただし OpenD が実際に常駐するまで観測は 1 件も生まれない**（A-1 / A-2） | A-1 |
| Stage 1 の営業日から**市場の祝日を除外する**（§4.2） | #385 | **判定源が無く実装できていない。** 祝日に OpenD が稼働していると営業日として算入される（過大計上・上記 B-4） | **B-4 の裁定待ち** |
| クラス C 統制違反の集計 | #329 / #374 / **#387** | **解消済み（2026-08-05）。** 発注審査の観測ログから集計し、未供給と 0 件を区別する | — |
| Stage 3 の空売り実弾解禁 | #333 | 決定 14 の verdict 経路が無い（fail-closed） | #388 |
| 発注先の設定 → 実際の発注経路 | #334 | 起動時構成（`Broker:Provider`）が支配しており未結線 | 未起票 |
| 為替レートの日銀取得（ADR-0022） | 未実装 | **系列 ID は確定したが、決定 4 のしきい値が裁定待ち**（A-4） | **#381** |
| 日本株の価格変動検知（FR-02 ほか） | 実装済み | **moomoo アカウントに日本株の市況権限が無い**（A-8） | **#397** |
| 現金口座の **GFV 回避ガード**（ADR-0021 決定4-2） | **#375** | **決済済み資金（settled cash）の情報源が無い。** moomoo SDK 10.8.6808 を全走査した実測で `TrdCommon.Funds` の 42 プロパティに専用フィールドが無く、`AvlWithdrawalCash`（出金可能額）も `MaxCashBuy`（現金買付余力・**未決済の売却代金を含み得る**）も代替にならない（IADR-0153 決定4）。**供給が無い限り現金口座の買付は常に `CashAccountSettlementHold` で止まる**（fail-closed・安全側だが「現金口座で運用できない」ということでもある） | **B-4 の裁定待ち**（`TrdFlowSummary` からの導出を ADR-0019 の PoC 項目へ追加する提案を環流済み） |
| 現金口座の **GFV 発生回数の追跡・警告**（ADR-0021 決定4-3） | **#375** | **回数の情報源が無い。** `GoodFaith` / `Violation` / `Gfv` は SDK アセンブリ全体で 0 件（`IsPdt` / `PdtSeq` / `DtStatus` は PDT 系であり GFV ではない）。判定（2 件で停止・2 件で警告）は純関数として置いてあるが**常に「未供給」側で止まる**。2 回目の警告の**通知経路（Discord）も未結線** | **B-4 の裁定待ち**（情報源の決め方そのものが計画に無い。環流済み） |
| 現金口座での **差金決済ガードの米国株適用**（ADR-0021 決定4-1） | **#375** | 判定は実装済みで、**現金口座を観測できれば作動する**。ただし利用者設定の口座種別（既定＝信用口座）を切り替える UI が無く、切り替えても上の 2 件により新規建てが止まる。**信用口座では #332 の現行挙動が厳密に保たれる**（両方向をテストで固定） | 上の 2 件の解決後 |
| **口座種別の観測そのもの**（ADR-0021 決定3） | **#375** | 実装済み（既存の 5 分 probe が `BrokerAccountObserved` を発行）。**ただし OpenD が実際に常駐するまで観測は 1 件も生まれない**（A-1 / A-2）。観測が無い間、**moomoo 発注先の新規建ては `BrokerAccountTypeUnverified` で止まる**（内蔵 paper は対象外） | A-1 |
| 実弾の発注そのもの | — | `LiveTradingGate.LiveTradingReleased = false` | A 群すべて |

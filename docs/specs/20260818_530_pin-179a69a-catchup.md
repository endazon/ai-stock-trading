---
title: 計画 pin 179a69a への追随 — キット大幅更新（役割スロット制・必読の別紙化・検査強化）の還流
type: spec
status: approved
related_ids: [NFR, IADR-0200, IADR-0202, IADR-0203, IADR-0205, IADR-0206]
author: claude
created: 2026-08-18
updated: 2026-08-18
plan_refs:
  - ../../planning/tools/impl-handoff-kit/HOWTO.md
  - ../../planning/tools/impl-handoff-kit/repo-template/
  - ../../planning/docs/ai-implementation-workflow-guide.md
related_specs:
  - 20260816_524_pin-4d6a7d6-catchup.md
---

# 仕様書: 計画 pin 179a69a への追随（#530）

> 本仕様書は実装着手前に作成する。MSP#869 と対（同時起票・両リポを同一セッションで還流するため §11 の 7 日後突合は本作業内で完結する）。

## 起点

- 起点 issue: #530
- 起点 ID: **NFR**（無採番。キット追随・pin 更新＝工程のメタ作業。`.claude/rules/traceability.md`「無採番 NFR を許す 2 つの場合」の場合 2）
- 実測時点: `develop` = `2fe0575` / 計画 pin `4d6a7d6` → `179a69a`（**キット 14 コミット遅れ**）
- 正本: kit `HOWTO.md`（B-5 差し替え表・「キット版を採る前に実走で差を確かめる」planning#343・traceability.md と別紙の同時取り込み 🔴）／`kit-sync-classification.json`

## 母集合の引き方（実測）

**追随対象は記憶で挙げず、2 系統の機械出力から取った**（母集合の規則 9）。

1. `KIT_DIR=<planning 179a69a> node scripts/check-kit-sync.js` の実走出力: **違反 27 件**（未分類 8 / 分類 A ドリフト 19）。
2. `git -C planning diff --name-status 4d6a7d6..179a69a -- tools/impl-handoff-kit/repo-template/`: **36 ファイル**（新規 8 / 変更 28）。

1 は分類 B/C の土台変更と notApplicable（実名化済みワークフロー）を構造的に上げないため、2 と突合して全 36 件を下表へ割り当てた（黙った除外ゼロ）。

**除外したもの**: planning 側の `HOWTO.md`・`generators/*`（キットの手引き・計画側実行物であり配布物ではない）、`draft/feedback/*`・運用ガイド本文（計画リポの記録・正本であり複写しない）。

## 対象範囲（キット側 36 ファイルの全数割り当て）

| # | キット側ファイル | 分類 | 扱い |
| --- | --- | --- | --- |
| 1 | `.claude/agents/adr-guardian.md` | A | キット原文で上書き |
| 2 | `.claude/agents/plan-feedbacker.md` | A | （範囲内変更なし＝バイト一致を確認） |
| 3 | `.claude/agents/spec-implementer.md` | A | キット原文で上書き |
| 4 | `.claude/agents/traceability-auditor.md` | A | キット原文で上書き |
| 5 | `.claude/commands/impl-feature.md` | A | キット原文で上書き |
| 6 | `.claude/rules/traceability.md` | A | キット原文で上書き（🔴 #17 別紙と同時） |
| 7 | `.claude/settings.json` | B | 土台変更を移植（固有デルタ保持） |
| 8 | `.github/workflows/ci.example.yml` | 対象外 | 実名 `ci.yml` へ差分を移植 |
| 9 | `.github/workflows/claude-code-review.example.yml` | 対象外 | 実名 `claude-code-review.yml` へ差分を移植 |
| 10 | `.github/workflows/claude-coding.example.yml` | 対象外 | 実名 `claude-coding.yml` へ差分を移植 |
| 11 | `.github/workflows/pr-title.yml` | A | キット原文で上書き |
| 12 | `.mcp.json` | **新規 → A** | 取り込み（Context7 のみ。GitHub MCP を書かない 🔴。`/mcp` の対話承認は利用者作業） |
| 13 | `AGENTS.md` | A | キット原文で上書き（worker スロット契約の節を含む） |
| 14 | `AI_SETUP.md` | B（X） | 土台変更を移植（プロファイル宣言＝固有は保持） |
| 15 | `CLAUDE.md` | C(b) | 共通部の追随を pin 追随 issue で行う（#524 の判定どおり）。取捨は §CLAUDE.md 共通部 |
| 16 | `ai-roster.json` | **新規 → B 5** | 取り込み（キット雛形と同じ分類。配役は本リポが埋める欄） |
| 17 | `docs/traceability-appendix.md` | **新規 → A** | 取り込み（#6 と同時。companion `traceability.repo.md` は不変） |
| 18 | `docs/ai-orchestration.md` | **新規 → A** | 取り込み（キット雛形と同じ分類） |
| 19 | `docs/ai-workflow.md` | B 2+3 | 土台変更を移植 |
| 20 | `scripts/README.md` | B 3+2 | 土台変更を移植 |
| 21 | `scripts/action-versions.json` | A | キット原文で上書き |
| 22 | `scripts/ai-adapters/README.md` | **新規 → A** | 取り込み |
| 23 | `scripts/ai-adapters/run-worker-claude.sh` | **新規 → A** | 取り込み |
| 24 | `scripts/ai-adapters/run-worker-codex.sh` | **新規 → A** | 取り込み |
| 25 | `scripts/ai-adapters/run-worker.sh` | **新規 → A** | 取り込み |
| 26 | `scripts/apply-profile.sh` | A | 実走突合のうえキット原文で上書き |
| 27 | `scripts/check-action-versions.js` | A | 実走突合のうえキット原文で上書き |
| 28 | `scripts/check-ai-workflow-config.js` | A | 実走突合のうえキット原文で上書き |
| 29 | `scripts/check-commit-messages.js` | C(b) | **キット側の新検査（HTML エンティティ planning#417・PR 自番号 planning#386）を移植**。置換点 `PLAN_PROJECT` は保持。キット版 `scripts.test.js`（A）が機能を試験するため、移植しないとテストが落ちる |
| 30 | `scripts/check-cross-repo-refs.js` | A → **B 5 に再判定**（[IADR-0206](../adr/IADR-0206_kit-pin-179a69a-substitution-points-in-file.md)） | 実走突合（同一入力の exit code・自己試験とも新旧同値）のうえキット原文へ差し替え、宣言された【置換点】をファイル内で埋めた。キット版 `scripts.test.js`（A）が実データ本走を env なしの素実行で行うため、env 注入（IADR-0200 決定 5）では point-in-time 記録の除外が効かず成立しない |
| 31 | `scripts/check-doc-links.js` | A | 実走突合のうえキット原文で上書き |
| 32 | `scripts/check-kit-sync.js` | A | 実走突合のうえキット原文で上書き |
| 33 | `scripts/check-permission-denials.js` | A | 実走突合のうえキット原文で上書き |
| 34 | `scripts/check-review-verdict.js` | A | 実走突合のうえキット原文で上書き |
| 35 | `scripts/kit-sync-classification.example.json` | A | キット原文で上書き |
| 36 | `scripts/lib/ci-annotate.js` | A | キット原文で上書き（scripts.test.js 経由で試験） |
| 37 | `scripts/scripts.test.js` | A | キット原文で上書き（固有テストは `scripts.repo.test.js` のため無傷） |

（#2 は `git diff` の名寄せで範囲内に現れた場合のみ。検査器出力に無ければバイト一致＝処置不要）

**波及先（キット diff の外）**: `planning` gitlink（`4d6a7d6` → `179a69a`）／`scripts/kit-sync-classification.json`（新規 8 件の分類・`$comment` の pin 記述）／`CLAUDE.md` 共通部／本仕様書。

## CLAUDE.md 共通部の取捨（C(b)。#524 の「共通部の追随は pin 追随 issue で行う」）

| キット側変更 | 判定 |
| --- | --- |
| 任意仕様書 9 種の表 → 1 行化（planning#416） | **取り込む**（出力先・粒度の正本は `/new-spec` 定義と `docs/README.md`。複写を減らす） |
| Playwright の役割棲み分け弾（planning#409 系） | **取り込む**（本リポは対応する記述を持たない。AI の対話操作と CI の E2E ランナーの分離は本リポにも当たる） |
| 役割スロット弾（`ai-roster.json` / `docs/ai-orchestration.md` への導線。planning#413） | **取り込む**（新規取込 3 点の導線） |
| メタ作業節の予算詳細 → `docs/ai-workflow.md` §測り方 へ移設（planning#416） | **取り込む**（ai-workflow.md 側に §測り方 を移植したうえで、CLAUDE.md 側は凝縮版へ）。本リポの `scripts/check-reading-budget.js`（IADR-0204）への言及は保持 |

## 受け入れ基準

- [x] `node scripts/check-kit-sync.js --require-planning` が exit 0（未分類 0・ドリフト 0）
- [x] 上表の全件が「上書き／移植／対象外（移植先を明記）」のいずれかで処理済み
- [x] `node scripts/scripts.test.js`（`scripts.repo.test.js` 込み）が全件 pass（332 件）
- [x] 必読予算: 38,032B ≤ 51,200（74.3%。`check-reading-budget.js` も exit 0。42,897B から**別紙化で純減**）
- [x] その他ローカル検査（`check-doc-links` / `check-cross-repo-refs` / `check-plan-id-qualification` / `check-action-versions --compare-with-ref origin/develop` / `check-ai-workflow-config` / `check-adr-index-sync` / `check-feedback-dispatched` / `check-feedback-status-sync` / `check-planning-pin-freshness`）が全て exit 0

## 検証（実測）

`planning` を pin `179a69a` で populate し、`git fetch --unshallow` で履歴を完全化した状態で実測した。

```text
node scripts/check-kit-sync.js --require-planning
  OK: キット 123 件（A 93 / B 15 / C 7 / 対象外 8）                                   exit=0
REQUIRE_REPO_TESTS=1 node scripts/scripts.test.js   ✓ 332 tests passed
検査器 10 本（受け入れ基準の列挙）                                                    全て exit=0
cat CLAUDE.md .claude/rules/*.md | wc -c            38,032（74.3%）……37,512B は traceability.repo.md の置換点節を追記する前の中間測定であり、最終形を測り直した
bash -n scripts/apply-profile.sh scripts/ai-adapters/*.sh                            ok
git diff -U0 -- backend/ frontend/ にコメント行以外の変更が無いことを機械確認          ok
（dotnet SDK が本環境に無いため build/test は CI に委ねる。変更はコメントのみで挙動不変）
python3 -c "yaml.safe_load(...)"  claude-coding / claude-code-review / ci / pr-title / backlog-audit  ok
```

**検査器の実走突合**（HOWTO の原則・裁定 planning#343）: 分類 A の各検査器は「旧 pin のキット原文＝現物」であり、キット HEAD 版は同一配布物の前進である。`check-cross-repo-refs.js` は差し替え前後で同一入力（env 置換点あり）の exit code と `--self-test` を突合し同値を確認した。差し替え後に全 CLI と自己試験を実走した。

**キット版テストが要求した移植 3 件**（キット版 `scripts.test.js`＝A の実走で検出）:
1. `check-commit-messages.js`（C）へ HTML エンティティ検査（planning#415）・FR/UC/SC 実在性の拡張点（`loadExistingPlanIds` / `normalizePlanId`）・PR 自番号検査（`validateTitlePrNumber` / planning#386）・面ラベル付きクロスリポ検査（`crossRepoRefReasons` / `CROSS_REPO_REF_LABELS`）を移植した。
2. `check-cross-repo-refs.js` の置換点をファイル内で充填（上表 #30 / IADR-0206）。
3. 走査範囲の拡大（追跡下の全ファイル）で新たに検出された live ファイルの表記違反 **40 件 + 追い掛け 1 件**（ワークフロー 5・helm 1・バックエンドのコード内コメント 30・frontend e2e 4・列挙形 1）を是正した。`docs/specs/` / `feedback/` は point-in-time 凍結のため除外のまま（裁定 2026-08-15）。

## 計画書との差異・未決事項

- **`readPlanIds()` 拡張点が未実装**: `check-test-traceability.js` が `readPlanIds` を持たないため、FR / UC / SC の実在性検査は notice 付き skip となる（キット設計どおりの fail-open）。実装すれば自動で実効する。フォローアップは #530 に記録。
- キット `CLAUDE.md` 共通部の取捨は §CLAUDE.md 共通部の表のとおり全 4 点を取り込んだ（Playwright 棲み分け・役割スロット・任意仕様書表の 1 行化・予算詳細の `docs/ai-workflow.md` §測り方 への移設）。
- `.mcp.json`（Context7）は取り込んだが、**`/mcp` の対話承認は利用者作業**（HOWTO §B-3.5。ヘッドレスでは承認プロンプトを出せない）。

［2026-08-18 追記 / #530］計画側が planning#420（MSP#869 の作業で環流したキット配布物内の issue 表記混在）を即日是正した（planning#421・pin `479f4dd`）。追加還流として pin を `479f4dd` へ前進し、`scripts/check-review-verdict.js`（分類 A・docstring 1 行）をキット原文で上書きした。`claude-code-review.example.yml` 側の同一コメント行は本リポの実名ワークフローに存在せず移植先なし（grep で実測）。検証は追記時点で再実走した（`check-kit-sync --require-planning` exit 0・`scripts.test.js` ✓ 332・予算 38,032B 不変）。

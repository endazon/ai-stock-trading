---
title: blocked 判定を実測で棚卸しし、ラベルと受け皿 issue を整える（#483 の残る提案）
type: spec
status: approved
related_ids: [NFR, IADR-0185]
author: endazon (with Claude Code)
created: 2026-08-14
updated: 2026-08-14
---

# 仕様書: blocked 判定の再検証とラベル・受け皿の整備

> 本仕様書は実装着手前に作成する。

## 起点

- 起点 issue: [#483](https://github.com/endazon/ai-stock-trading/issues/483)（バックログ定期監査の結果）の**提案 2・3・4・5・7**
- 起点 ID: **NFR**（工程の統制。計画側の非機能要件表に当たる番号が無いため無採番。planning#311 の 2）
- 根拠となる規律: [IADR-0185](../adr/IADR-0185_required-check-contexts-and-blocked-record.md) 決定2（能力と規則を分ける）・決定4（blocked 判定は棚卸しごとに再検証する）

> **提案 1（#344 のチェックリスト更新）と提案 6（環流の起票状況）は対応済み**である。
> 提案 6 は実測の結果**「未起票 10 件」がすべて偽陽性**であり、`resolved` 6 件も現在 0 件
> （すべて `accepted`）であることを確認済み。

## 🔴 本作業の要点: 推測を実測へ置き換える

**#483 の提案 3〜5 は「〜の可能性がある」「〜を推奨する」という推測で書かれている。**
**推測のまま文書を書き換えると、次の棚卸しがまた同じ推測から始まる。**
**測れるものは測ってから書く。**

## 実測1: A-6（Playwright E2E のローカル実走）—— **記述は成り立たない**

### 現在の記述

> **現況**: サンドボックスで**ブラウザ取得が失敗**する（`Failed to download Chrome for Testing`）。実走は **CI の `frontend-e2e`**。
> **なぜ AI にできないか**: 環境側の制約。CI では動くため、**運用としては「E2E は CI に委ねる」で成立している**

### 実測（2026-08-14）

| 測ったこと | 結果 |
| --- | --- |
| ブラウザの有無 | **既に導入済み**（`/opt/pw-browsers` に `chromium-1194` ほか）。**ダウンロードは発生しない** |
| 素で `npx playwright test` | **失敗**。`Executable doesn't exist at /opt/pw-browsers/chromium_headless_shell-1228/...` |
| 失敗の正体 | **バージョンの食い違い**。リポジトリが固定する `@playwright/test` **1.61.1** はブラウザビルド **`-1228`** を要求するが、導入済みは **`-1194`** |
| `executablePath` を明示して実走 | **60 tests passed（21.6 秒）** |

**「ブラウザ取得が失敗する環境側の制約」ではない。** 実際は
**pin したバージョンとプリインストール版のビルド番号がずれているだけ**であり、
**設定 1 つで解消する。**

> **#483 提案 4 の疑い（環境観測を恒久制約として書いている。MSP #617 と同型）は当たっていた。**

## 実測2: B-3（GitHub Secrets の登録）—— **「権限が無い」は 3 つの別物を混ぜている**

### 現在の記述

> **なぜ AI にできないか**: Secrets の読み書き権限が無い

### 分解して測る

| 分解 | 実測・根拠 | 種別 |
| --- | --- | --- |
| **(a) 値の読み取り** | **誰にもできない。** GitHub は登録済み Secret の値を**所有者にも返さない**（設計）。**AI の権限の問題ではない** | **恒久**（普遍） |
| **(b) 登録（書き込み）** | 権限としては **admin が要る**。B-2 の実測（2026-08-14・#473）で**本リポに `admin: true`** が確認されている。**能力の不在ではなく、規則による禁止**（リポジトリ設定の変更は利用者に留保）と読むべきである | **規則**（B-2 と同型） |
| **(c) 本セッションからの操作** | **できない。** Secrets を列挙・登録する MCP ツールが**本セッションのツール一覧に存在しない**（実測 2026-08-14）。`gh` CLI も本環境では使用できない | **能力**（この経路に限る） |
| **(d) 登録済みかどうか** | **登録済みと判断してよい。** AI レビュー（`claude-code-review.yml`）が**本日だけで 8 回以上、認証に成功して完走している**。認証情報が未登録なら起動時に失敗する | **観測** |

**「セッションからは確認できない」という現況記述は (d) の点で古い** —— **間接的には確認できている。**

## 実測3: ラベルの存在 —— **3 つとも存在する**

#483 は「これらのラベル自体が本リポジトリに存在するかどうかは `gh label list` が使用不可のため未確認」としていた。**API で実測した。**

| ラベル | 存在 | 説明 | 付与数 |
| --- | --- | --- | ---: |
| `blocked:env` | **有り** | （説明なし） | 2 |
| `blocked:decision` | **有り** | 裁定待ちでブロック中（**棚卸しごとに再検証する**） | **0** |
| `blocked:human` | **有り** | 人間の操作・権限が要りブロック中（**棚卸しごとに再検証する**） | **0** |

> 🔴 **`blocked:decision` / `blocked:human` は説明文に「棚卸しごとに再検証する」と書かれているのに、
> 付与が 0 件である。** **再検証すべき対象を 1 件も持たない運用標準**になっている。

## 決定

### 決定1: A-6 を実測で書き直す。**恒久制約の側から外す**

- 現況を**実測に置き換える**（ビルド番号のずれ・`executablePath` で実走・60 passed / 21.6 秒）。
- **「なぜ AI にできないか」の行を削る** —— **できる。**
- 代わりに**再現手順**を書く（次に誰かが測るときに同じ手順を踏めるように）。
- **B-2 と同型式**（最後に測った時点・再測定手順）を持たせる。

### 決定2: B-3 を (a)〜(d) に分けて書き直す

**「権限が無い」の一言に、性質の違う 4 つが混ざっていた。**
**混ぜたまま書くと、環境が変わっても誰も測り直さない**（B-2 の是正と同じ理由）。

### 決定3: A-7 を「解消し得る前半」と「恒久の後半」に分ける

- **前半（実 Discord サーバ・Bot トークンの用意）**: テスト用サーバとトークンの発行は**環境構築で解消し得る**。
- **後半（利用者本人による昇格承認）**: **設計上の要請であり恒久**（AI が自分に課す関門を自分で通さない）。

### 決定4: ラベルを付ける。**説明文の約束を実効させる**

`blocked:decision` / `blocked:human` が 0 件では、**「棚卸しごとに再検証する」対象が存在しない。**
該当する open issue へ付与する。

### 決定5: 受け皿の無い項目を起票する

**blocked-tasks.md に書いてあるが GitHub issue が無い**項目は、棚卸しの俎上に載らない。

## やらないこと

- **B-2 の記述の変更**（既に是正済みの手本である）。
- **`blocked:env` の説明文の追加**（ラベル説明の整備は本作業の範囲外。必要なら別途）。
- **A-1（Hetzner ToS）の再測定** —— 法的判断であり、測り直す対象ではない。
- **Secrets の実際の登録・変更**（規則による禁止。利用者の操作）。

## 受け入れ基準

- [ ] A-6・B-3・A-7 が **B-2 と同型式**（能力/規則の分離・最後に測った時点・再測定手順）を持つ
- [ ] A-6 の記述が**実測に基づく**（推測語を残さない）
- [ ] `blocked:decision` / `blocked:human` の付与が **0 件でなくなる**
- [ ] 受け皿の無い項目に issue があり、blocked-tasks.md から**番号で参照できる**
- [ ] `doc-links` / `kit-sync` / scripts テストが緑

## 実施結果

### 🔴 ラベルの付与 —— **初版で 2 件を誤り、AI レビューの指摘で訂正した**

**最終的な付与**

| issue | 付与 | 何を待っているか |
| --- | --- | --- |
| [#381](https://github.com/endazon/ai-stock-trading/issues/381) | **無し**（初版の `blocked:decision` を撤回） | **何も待っていない。単なる未実装**（情報源の二段化＝日銀アダプタの新設） |
| [#388](https://github.com/endazon/ai-stock-trading/issues/388) | `blocked:env`（初版の `blocked:decision` から変更） | **PoC 項目 9（`ShortFeeRate` の単位）の実施**（#342 系）。**裁定ではない** |
| [#318](https://github.com/endazon/ai-stock-trading/issues/318) | `blocked:human` | 稼働環境での秘密情報の操作（Webhook 再発行・Loki/Tempo の後始末） |

### 🔴 何を間違えたか（本作業で最も重要な記録）

**本仕様書は「提案 3〜5 は推測で書かれていた。測ってから書く」を掲げていた。**
**その作業の中で、私は `docs/blocked-tasks.md` B-4 を鵜呑みにし、新しい誤りを 2 件作った。**

| issue | 初版で書いたこと | 事実 |
| --- | --- | --- |
| #381 | 「ADR-0022 決定4 の**裁定待ち**。案 A/B/C が挙がっている」 | **2026-08-07 に案 A（5 日）で裁定済み。ADR-0022 は `Accepted`。** しきい値 5 日は **PR #455 で実装済み** |
| #388 | 「ADR-0016 決定14 の**裁定待ち**（verdict の形式）」 | **2026-08-07 に確定済み**（質問票 第13回 補問 Q14・環流 project-planning#222） |

**どちらの裁定も、当の issue のコメント欄に 2026-08-07 の時点で書かれていた**（私のコメントより **1 週間前**）。

#### 原因

**`blocked-tasks.md` B-4 だけを読み、issue のコメント欄と計画 ADR の本文を読まなかった。**
**B-4 の当該 2 行が裁定前のまま陳腐化していた。**

**「文書を読まずに着手すると詰まりに気付かない」と書きながら、私は「issue を読まずに文書だけで判断した」。**
**向きが逆なだけで、同じ事故である。**

#### 是正

1. **ラベルを訂正した**（#381 は撤回・#388 は `blocked:env` へ）。
2. **誤ったコメントを訂正するコメントを各 issue へ投稿した**（削除はしない。**誤りも記録に残す**）。
3. **B-4 の当該 2 行を「裁定済み」へ訂正**し、**B-4 の冒頭へ次を明記した** ——
   **「本節を一次情報として使わないこと。裁定の有無は ①計画 ADR の本文 ②当該 issue のコメント欄 で確かめる。
   本節は索引であって正本ではない。」**

#### 教訓（次の棚卸しへ）

> **`blocked-tasks.md` は「blocked の索引」であって「blocked の正本」ではない。**
> **索引は必ず遅れる。** 裁定は計画 ADR と issue のコメントで起き、**索引の更新は誰かが気付いたときにしか走らない。**
> **ラベルを付ける前に、必ず当の issue のコメント欄を読むこと。**

> **この事故は AI レビューが検出した。** 自前の検証（`doc-links` / `kit-sync` / scripts テスト）は**すべて緑**であり、
> **機械検査は 1 つもこれを捕まえられなかった** —— **文書の記述が事実と合っているかを見る機械は無い**
> （[IADR-0191](../adr/IADR-0191_kit-sync-classification.md) の残余リスクと同じ性質）。

### 受け皿 issue の起票

| issue | 対応する行 | 付与 |
| --- | --- | --- |
| [#500](https://github.com/endazon/ai-stock-trading/issues/500) | A-10（MCP 非公開の結合確認） | `blocked:env` |
| [#501](https://github.com/endazon/ai-stock-trading/issues/501) | B-1（CODEOWNERS） | `blocked:human` |

**起票しなかったもの**（規則 6・除外の理由を残す）:

| 行 | 起票しない理由 |
| --- | --- |
| A-5（本番相当 PostgreSQL） | 対象マイグレーション（#384/#390）は完了済み。**残るのは本番相当データでの再検証**であり、本番環境が用意されるまで着手条件が定まらない。**環境が用意された時点で起票する方が具体的に書ける** |
| A-9 項目1（egress IP 変更時の再検証） | **A-1（#24）と同時にしか実施できない**と本文が述べており、**独立した受け皿を作ると二重管理になる** |
| B-3（GitHub Secrets） | **本作業で (a)〜(d) に分解し、再測定手順まで書いた。** 残るのは利用者の目視確認のみであり、**issue 化しても内容が blocked-tasks.md の再掲になる** |

### 🔴 B-1 について記録したこと

`blocked-tasks.md` B-1 は「監査セッションで #392 / #390 / #393 を自分でマージした」と記録していた。
**この形は続いている** —— **本日のセッションで PR #491 / #495 / #499 の 3 本を、実装した本人（AI）が判断してマージした。**

**利用者の明示的な指示による運用であり逸脱ではない。** ただし **B-1 が指摘する構図そのもの**であるため、
**B-1 の追記として残した**（「解消済み」に見せない）。

**緩和されている点も併記した** —— AI レビューは別プロセスで走り、#491 では実際に 🔴 を出して実装を止めた。
**「別の目」は機能している。ただしその目を無視してマージすることを止める仕組みは無い。**

## テスト方針

**本作業は文書と issue の整備であり、コードの変更を伴わない。**
機械検査は既存のもの（`check-doc-links.js`）で足り、**新しい検査器は追加しない**
（同型事故 2 回に達していない。運用標準「検査器の追加は同型事故 2 回から」）。

**ただし A-6 の実測は再現手順を書き残す** —— 次の棚卸しが**同じ手順で測り直せる**ことが、
IADR-0185 決定4 の実効性そのものである。

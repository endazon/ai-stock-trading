---
title: リスクガードコア（FR-10/12/19/20）テスト仕様書
type: test-spec
status: draft
related_ids: [FR-10, FR-12, FR-17, FR-19, FR-20, UC-01, UC-02, UC-06]
author: endazon (with Claude Code)
created: 2026-07-09
updated: 2026-08-04
plan_refs:
  - ../../planning/projects/ai-stock-trading/02_requirements/01_requirements.md
  - ../../planning/projects/ai-stock-trading/06_technical/05_trading-assumptions.md
---

# テスト仕様書: リスクガードコア（FR-10/12/19/20）

> **本書は再実装（[#329](https://github.com/endazon/ai-stock-trading/issues/329)）より前の写像表である。**
> 金額系の統制上限 3 値（equity 割合化）・既定値の確定単一値・空売り専用統制は
> [FR-10 リスク統制コア（再実装）](./FR-10_risk-controls-tests.md) を正とする。
> 本書の T-10-03 / T-10-04（1 注文 ¥35,000・日次 ¥100,000 の固定額境界）・T-10-17〜21
> （SIMULATE の 1,700 倍スケール）は同書が置き換えた。
>
> 計画書の受け入れ基準と作業仕様書のチェックリストを、実装済みの xUnit テストへ写像した対応表。
> 対象は `RiskManagementService.Domain.Tests`（RiskEvaluator / PositionSizer / TradingDefaults）と
> `AiStockTrading.Shared.Infrastructure.Tests`（PaperBrokerAdapter）。
>
> **テスト件数の注記**: リスク評価コア是正（Issue #25〜#31）・サイジングキャップ（#29）・トレーサビリティ整備（#32）は
> develop にマージ済み。develop 現状は 58 件（ドメイン 54 / インフラ 4）で、相場操縦・段階資金上限・日次損失合算・
> サイジングキャップ等を含む。本 PR は証券会社拒否（#30・`OrderStatus.Rejected`）を統合しており、統合後は
> 63 件（ドメイン 54 / インフラ 9）。#30 の統合が develop に載れば develop も 63 件になる。件数は Issue 単位で示す
> （PR 番号帰属はスカッシュマージのため git 履歴単独では追えないため）。テストメソッド名は日本語識別子で、各テストの
> コメントに起点 ID を残している。

## 起点となる計画書（トレーサビリティ）

- 機能要求（FR）: FR-10（リスク統制）、FR-12（ペーパートレード）、FR-19（取引ガード）、FR-20（段階ゲート）
- ユースケース（UC）: UC-01/UC-02（取引サイクルの検証段）、UC-06（設定変更・緊急停止）
- 受け入れ基準の所在: `02_requirements/01_requirements.md`、`05_trading-assumptions.md` §5、作業仕様書
  `docs/specs/20260708_risk-guard-core.md` / `docs/specs/20260709_risk-eval-core-fixes.md`

## テスト対象・範囲

- 対象: 発注前の決定的判定（`RiskEvaluator.Evaluate`）、ポジションサイジング（`PositionSizer`）、既定値
  （`TradingDefaults`）、ペーパーブローカー（`PaperBrokerAdapter`）の単体テスト。
- 対象外（別スライス）: サービスホスト・MassTransit 配線・PostgreSQL 永続化、サイジング→発注意図→検証の結合
  テスト（取引判断サービス。IADR-0003）、相場操縦検知アルゴリズム本体（後続スライス。IADR-0006）、
  ロックアウト状態管理（リスク管理ホスト #12）。

## テスト観点

- 正常系（制約を満たす注文の承認）、異常系（各違反理由の拒否）、境界値（上限ちょうど/上限+1）、
  フェイルセーフ（手仕舞い Close はエントリー制約の対象外）、全違反列挙（監査性 FR-11）。

## テストケース一覧（受け入れ基準への写像）

### FR-10 リスク統制（RiskEvaluatorTests / PositionSizerTests）

| ID | 受け入れ基準 | テストメソッド | 区分 |
| --- | --- | --- | --- |
| T-10-01 | 制約のない注文は承認され承認数量が返る | `制約のない買い注文は承認され承認数量が返る` | 自動 |
| T-10-02 | kill switch 起動後は新規発注が一切行われない | `KillSwitch起動中はすべての新規注文を拒否する` / `KillSwitch起動中はショートエントリーも拒否する` | 自動 |
| T-10-03 | 1注文金額上限を境界値で強制 | `一注文あたり金額上限を境界値で強制する`（35,000=承認 / 35,001=拒否） | 自動 |
| T-10-04 | 1日発注金額上限を累計で強制 | `一日あたり発注金額上限を累計で強制する` | 自動 |
| T-10-05 | 保有銘柄数上限で拒否 | `保有銘柄数上限に達した状態の新規買いは拒否する` | 自動 |
| T-10-06 | 日次損失上限（実現+含み損の合算）で拒否 | `日次損失上限に達したら当日の新規注文を拒否する` / `実現ゼロでも含み損の合算で日次損失上限に達したら拒否する` / `実現と含み損の合算が上限未満なら日次損失上限で拒否しない` / `含み益は実現損失を相殺して日次損失上限の到達を防ぐ` | 自動 |
| T-10-07 | 最大DD上限で拒否 | `最大ドローダウン上限に達したら注文を拒否する` | 自動 |
| T-10-08 | 手仕舞いはエントリー専用制約の対象外（フェイルセーフ） | `保有ポジションの売り注文にはエントリー専用の制約を適用しない` / `KillSwitch_日次損失上限_最大DD到達中でも損切りの売り注文は承認する` / `金額上限を超える状況でも損切りの売り注文は承認する` / `含み損で日次損失上限に達していても手仕舞いの売り注文は承認する` | 自動 |
| T-10-09 | 複数違反はすべて列挙される（FR-11 監査） | `複数の違反はすべて列挙される` | 自動 |
| T-10-10 | リスク予算×損切り幅で株数を算出・縮小係数の重畳 | `リスク予算と損切り幅から株数を算出する` / `縮小係数がサイズに乗算される` / `連敗数に応じた縮小係数を返す` / `ドローダウンが上限の半分に達したらサイズを半減する` / `連敗とドローダウンの縮小は重畳する` | 自動 |
| T-10-11 | サイジング結果が金額上限・資金でキャップされる（#29） | `リスク予算基準が金額上限内ならキャップされない` / `損切り幅が浅い場合は1注文金額上限で株数がキャップされる` / `利用可能資金が金額上限より小さい場合は資金でキャップされる` / `参照価格がゼロ以下なら見送りとして株数ゼロを返す` | 自動 |

| T-10-12 | 金額上限は基準通貨（**USD**）換算後の金額で判定する（#257 / #364 / IADR-0107 / IADR-0152） | `非基準通貨建て注文の金額上限は換算後の金額で判定する` / `非基準通貨建て注文の日次発注累計と段階資金上限も換算後の金額で判定する` / `換算レート既定1の注文は従来どおり判定される` | 自動 |
| T-10-13 | 台帳の金額集計（取得額・当日発注累計・実現/含み損益）は基準通貨で積む。建玉はローカル通貨のまま | `PortfolioProjectionTests.外貨建ての取得額と当日発注累計は基準通貨で積まれる` / `外貨建ての実現損益は基準通貨で計上される` / `外貨建ての含み損益は建玉の約定時レートで換算される` / `建玉の平均取得単価はローカル通貨のまま射影される` / `レート未記録の約定は従来どおり基準通貨建てとして積まれる_回帰` | 自動 |
| T-10-14 | 非基準通貨（#364 以降は**日本株**）で換算レートが解決できなければ新規建てを見送る（fail-safe）。基準通貨（**米国株**）は実 FX 源が無くても従来どおり判断する | `TradeDecisionServiceTests.非基準通貨建て銘柄で換算レートが無ければ見送る` / `FX未結線の既定では非基準通貨建て銘柄を見送る` / `換算レート取得が例外なら見送る` / `基準通貨の市場は実FX源が無くても従来どおり判断する` / `FredFxRateSourceTests.基準通貨は外部へ問い合わせずレート1を返す` | 自動 |
| T-10-15 | 換算後の金額でサイジングし、発注意図の価格はローカル通貨のまま載せる | `TradeDecisionServiceTests.非基準通貨建て銘柄は換算後の金額でサイジングし価格はローカル通貨のまま載せる` / `換算後に1注文金額上限を超える高価格株は見送る` / `採算評価のnotionalは基準通貨で突き合わせる` | 自動 |

| T-10-16 | 損切りの機械執行も建玉の換算レートを決済注文へ引き継ぐ。引けない場合もレート 1 へ縮退して決済は必ず発行する（ADR-0003） | `StopLossExecutionServiceTests.損切り決済は建玉の換算レートを引き継ぐ` / `該当建玉が台帳に無くても決済は必ず発行する_failsafe` / `台帳未注入でも決済は従来どおり発行する_回帰` | 自動 |

| T-10-17 | SIMULATE 限定プロファイルの金額はシミュレータ残高に基づき、比率系・取引ガードは本番既定と同一 | `SimulatorTradingDefaultsTests.金額系の上限はシミュレータ残高に基づく` / `比率系は本番既定と同一である` / `取引ガードは本番既定と同一である` | 自動 |
| T-10-18 | プロファイル有効時も実弾段階（Stage 2/3）の資金上限は不変・ペーパー段階のみ引き上がる | `SimulatorTradingDefaultsTests.実弾段階の資金上限は本番既定から変えない` / `ペーパー段階の資金上限だけを引き上げる` / `SimulatorProfileRiskSettingsStoreTests.実弾段階の資金上限は上書きしない` / `SimulatorProfileWiringTests.有効時も実弾段階の資金上限は本番既定のまま` | 自動 |
| T-10-19 | 既定（未設定・明示 false）は本番既定のまま。有効時のみ上限と基準資金が上がる | `SimulatorProfileWiringTests.既定は本番既定の上限のまま` / `明示的に無効化しても本番既定のまま` / `有効時は金額上限とペーパー段階の資金上限が上がる` / `有効時は基準資金もシミュレータ残高になる` | 自動 |
| T-10-20 | 米国株（AAPL 相当 ¥50,250/株）でプロファイル有効時は数量が算出され、本番既定では 0 株になる | `SimulatorTradingDefaultsTests.米国株の代表銘柄でも数量が算出される` | 自動 |
| T-10-21 | プロファイルは金額系 2 項目とペーパー段階の資金上限だけを差し替え、保存は素通しする（DB を汚さない） | `SimulatorProfileRiskSettingsStoreTests.有効時は金額系の上限をプロファイル値へ差し替える` / `有効時も比率系と取引ガードは内側の設定のまま` / `保存は素通しする` | 自動 |

### FR-19 取引ガード（RiskEvaluatorTests）

| ID | 受け入れ基準 | テストメソッド | 区分 |
| --- | --- | --- | --- |
| T-19-01 | 無効化された商品種別を拒否 | `無効化された商品種別の注文は拒否する` | 自動 |
| T-19-02 | 無効化された市場を拒否 | `無効化された市場の注文は拒否する` | 自動 |
| T-19-03 | 禁止銘柄を（銘柄+市場）で拒否 | `取引禁止銘柄の注文は拒否する`（6457/6902/6502） / `禁止銘柄コードでも市場が異なれば拒否しない` | 自動 |
| T-19-04 | 差金決済防止（同日再エントリー）を（銘柄+市場）で判定（#26） | `同一銘柄の同日再エントリーは拒否する` / `同日取引済みでも市場が異なれば再エントリーを拒否しない` | 自動 |
| T-19-05 | 相場操縦パターン（検出器注入時のみ判定）（#28） | `相場操縦パターン検出器が該当と判定した注文は拒否する` / `相場操縦検出器を注入しなければ現行どおり承認する` / `ガード無効時は相場操縦検出器が該当と判定してもスキップする` | 自動 |

### FR-20 段階ゲート（RiskEvaluatorTests）

| ID | 受け入れ基準 | テストメソッド | 区分 |
| --- | --- | --- | --- |
| T-20-01 | ペーパー段階で実弾モードを拒否 | `ペーパー段階で実弾モードの注文は拒否する` | 自動 |
| T-20-02 | 段階資金上限を累計投入額で強制（#27） | `段階別資金上限を超える注文は拒否する` / `保有投入額を含む累計が段階資金上限を超える新規注文は拒否する` / `保有投入額を含む累計が段階資金上限ちょうどなら承認する` / `段階資金上限は手仕舞いの投入額には適用しない` | 自動 |
| T-20-03 | ショートエントリーにも金額・段階上限が効く（#25） | `ショートエントリーにも段階資金上限と金額上限が適用される` / `ショート決済の買い戻しはエントリー制約の対象外` | 自動 |

### FR-17 既定値（TradingDefaultsTests）

| ID | 受け入れ基準 | テストメソッド | 区分 |
| --- | --- | --- | --- |
| T-17-01 | リスク統制の既定値が §5 と一致 | `リスク統制の既定値は全体前提条件と一致する` | 自動 |
| T-17-02 | 取引ガード既定値（現物のみ・日米・再エントリー禁止・相場操縦禁止） | `取引ガードの既定値は現物のみ有効かつ日米市場有効である` | 自動 |
| T-17-03 | 禁止銘柄の既定3件 | `取引禁止銘柄の既定値は利用者登録の3銘柄である` | 自動 |
| T-17-04 | 段階の既定（Stage0・ペーパー・資金上限10万） | `運用段階の既定値はStage0のペーパーモードである` | 自動 |

### FR-12 ペーパートレード（PaperBrokerAdapterTests）

| ID | 受け入れ基準 | テストメソッド | 区分 |
| --- | --- | --- | --- |
| T-12-01 | 発注で参照価格の即時全量約定 | `発注すると現在値で即時全量約定する` | 自動 |
| T-12-02 | 注文IDで状態照会・未知IDはnull | `発注済み注文を注文IDで照会できる` / `未知の注文IDの照会はnullを返す` | 自動 |
| T-12-03 | 約定済み（終端）注文は取消不可 | `約定済み注文の取消は失敗する` | 自動 |
| T-12-04 | 不正注文（数量/価格）は約定せず証券会社拒否（#30） | `数量や価格が不正な注文は約定せず証券会社拒否になる` / `証券会社拒否の注文は取消できない` | 自動 |

### FR-10/FR-05 約定の台帳到達＝統制の前提（#270 / IADR-0113）

統制の入力は約定行（`trade_fills`）であり、非同期に約定するブローカー（moomoo）では約定状態の追跡が
統制成立の前提になる（機能仕様書 FR-10「統制の入力は『約定』であるという前提」）。

| ID | 受け入れ基準 | テストメソッド（テストクラス） | 区分 |
| --- | --- | --- | --- |
| T-10-70 | 未約定（`Accepted`・約定 0）は枠を消費せず、約定が届いた後は同日再エントリーと金額枠が拘束する | `約定が台帳へ届くまで統制は拘束せず届いた後は同日再エントリーを拒否する`（`MoomooFillControlRegressionTests`） | 自動 |
| T-10-71 | 部分約定でも約定分だけ日次発注枠・段階資金枠が減り、全量約定で累積へ置き換わる（二重計上しない） | `部分約定でも統制の入力は約定分だけ進む`（同上） | 自動 |
| T-10-72 | 約定があれば全量約定を待たず台帳に載り、同一 `OrderId` は累積で更新される | `部分約定は約定時点で台帳に載り全量約定で累積値へ更新される`（`PortfolioLedgerConsumersTests`） | 自動 |
| T-10-73 | 部分約定のまま取消・失効した注文の約定分を落とさない | `部分約定のまま取消された注文も約定分が台帳に載る`（同上） | 自動 |
| T-10-74 | 再送・順序前後で約定数量が巻き戻らない（単調 upsert） | `同一注文の再送や少ない数量の後追いでは台帳が巻き戻らない`（同上）／`少ない数量の後追いでは約定が巻き戻らない`（`EfPortfolioLedgerStoreTests`） | 自動 |
| T-10-75 | 未約定注文を終端化まで追跡し、照会不能はブローカ状態を推測しない | `OrderFillPollerTests`（発注執行サービス・11 ケース） | 自動 |

### FR-10 手仕舞い（Close）は統制で止めない（#292 / IADR-0117）

| ID | 受け入れ基準 | テストメソッド（テストクラス） | 区分 |
| --- | --- | --- | --- |
| T-10-80 | owner が建玉を全量／部分決済でき、反対売買・`PositionEffect.Close`・建玉の換算レート引き継ぎで発行される | `数量省略は保有全量の決済を発行する`／`部分決済は指定数量だけを発行する`／`ショート建玉は買い決済で手仕舞う`／`決済は建玉の換算レートを引き継ぐ`（`PositionCloseServiceTests`） | 自動 |
| T-10-81 | 在庫を超える決済・多重投入による過剰決済が拒否される | `保有を超える数量は拒否する`／`処理中の決済は利用可能数量から差し引く`／`数量省略時は処理中を除いた残りだけを決済する`／`全量が処理中なら拒否する`（同上） | 自動 |
| T-10-82 | 永久に約定しない滞留承認が建玉を恒久ロックしない（30 分窓） | `窓を過ぎた滞留の承認は利用可能数量を減らさない`／`部分約定した決済は未約定ぶんだけを処理中として数える`（同上） | 自動 |
| T-10-83 | 価格を決められない決済は発注しない（価格 0 の注文を投げない） | `指値も現在値も無ければ拒否する`／`非正の指値は拒否する`（同上） | 自動 |
| T-10-84 | **kill switch 起動中・一時停止中でも手仕舞いできる**（FR-10 本文） | `kill_switch_起動中でも手仕舞いできる`／`一時停止中でも手仕舞いできる`／`決済サービスは統制ストアに依存しない`（`PositionCloseEndpointTests`） | 自動 |
| T-10-85 | 決済操作は利用者のみ（サービストークンには開かない） | `未認証は401`／`サービスロールでは403`（同上） | 自動 |
| T-10-86 | 「誰が・なぜ」決済したかが監査へ残る | `要求イベントに操作者と理由と数量が載る`／`要求イベントは承認と同一のDecisionIdで相関する`（`PositionCloseServiceTests`）／`PositionCloseRequested_は操作者と理由を残し注文と同一相関になる`（`AuditEntryFactoryTests`） | 自動 |
| T-10-87 | 処理中決済の集計が EF / InMemory の両実装で同一意味論 | `PortfolioLedgerInFlightCloseTests`（9 ケース）／`EfPortfolioLedgerInFlightCloseTests`（10 ケース） | 自動 |

### FR-10/FR-04 判断由来の決済＝AI の出口（#292 / IADR-0119）

| ID | 受け入れ基準 | テストメソッド（テストクラス） | 区分 |
| --- | --- | --- | --- |
| T-10-90 | 保有建玉の反対売買は `Close`・数量は保有全量として発行される（8 通りの組み合わせを固定） | `PositionEffectResolverTests`（8 ケース） | 自動 |
| T-10-91 | ロング保有への売り・ショート保有への買いが全量決済になり、損切り価格を持たない | `ロング保有への売り判断は全量の決済を発行する`／`ショート保有への買い判断は全量の決済を発行する`（`TradeDecisionServiceTests`） | 自動 |
| T-10-92 | **保有なし・不明の売りは発注に至らない**（裸の新規ショート建ての根絶） | `保有なしの売り判断は発注しない`／`建玉が不明な売り判断は発注しない`（同上） | 自動 |
| T-10-93 | 撤退がサイジング残枠・採算ゲートに妨げられない | `決済はサイジングの残枠に妨げられない`／`決済は採算ゲートに妨げられない`（同上） | 自動 |
| T-10-94 | 建玉照会は空配列＝0（保有なし）／失敗＝null（不明）を厳格に区別する | `空配列は保有なしのゼロ`／`非2xx_は不明`／`不正な応答は不明`／`例外は不明`（`HttpHeldPositionProviderTests`） | 自動 |
| T-10-95 | 建玉照会が失敗しても買い判断は従来どおり成立する（fail-safe の非対称） | `建玉照会が失敗しても買い判断は従来どおり成立する`（`TradeDecisionServiceTests`） | 自動 |

> 「Close が kill switch・pause・ロックアウト・段階資金上限・同日再エントリーで拒否されない」は
> 既存の `RiskEvaluatorTests`（T-10-xx 群・`Close(...)` ヘルパを用いるケース）が担保しており、重複させていない。

## テストデータ

- 既定設定は `TradingDefaults.CreateSettings()`。個別ケースは `PortfolioSnapshot` / `OrderIntent` のヘルパ
  （`Snapshot(...)` / `Buy(...)` / `Close(...)` / `ShortEntry(...)`）で境界値を注入する。

## 未カバー・実施予定

| 項目 | 理由 | 実施予定スライス |
| --- | --- | --- |
| サイジング→発注意図→`RiskEvaluator` 検証の結合 | 呼び出し側（取引判断サービス）責務（IADR-0003） | 取引判断サービス（#11） |
| 相場操縦検知アルゴリズム本体 | 注文履歴統計・閾値は運用データ待ち（IADR-0006） | 発注執行後続スライス |
| ロックアウト（翌営業日まで）状態管理 | `RiskEvaluator` はステートレス（IADR-0008） | リスク管理ホスト（#12） |
| 連敗縮小の実運用連動（勝ちトレードでの復帰） | 状態管理が必要 | リスク管理ホスト（#12） |

## 関連仕様

- 機能仕様書: [FR-10 リスク統制](../functional/FR-10_risk-controls.md)、[FR-19 取引ガード](../functional/FR-19_trading-guard.md)、[FR-20 段階ゲート](../functional/FR-20_staged-gates.md)、[FR-12 ペーパートレード](../functional/FR-12_paper-trade.md)
- データ仕様書: [リスク管理ドメインの集約](../data/risk-management-aggregates.md)
- 作業仕様書: `docs/specs/20260708_risk-guard-core.md` / `docs/specs/20260709_risk-eval-core-fixes.md` / `docs/specs/20260709_position-sizer-cap.md` / `docs/specs/20260709_paper-broker-validation.md` / [20260729_270_moomoo-fill-polling](../specs/20260729_270_moomoo-fill-polling.md)

## 未決事項

- 後続 FR（情報収集・市場監視・取引判断・報告・通知）のテスト仕様は各実装スライスで追加する。

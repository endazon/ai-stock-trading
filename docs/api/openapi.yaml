openapi: 3.0.3
info:
  title: ai-stock-trading API
  version: 0.0.0
  description: 通信仕様書（docs/api/）から自動生成した雛形。詳細はコードからの生成または手動で補完する。
paths:
  # 同期 HTTP API は未実装のため paths は空。非同期イベント/ポート契約は docs/api/events-and-ports.md を参照（IADR-0009）。
  {}

---
title: カバレッジ集計キーを cobertura の source 根で正規化する
type: work-spec
status: done
created: 2026-08-29
updated: 2026-08-29
issue: 562
---

# カバレッジ集計キーを cobertura の `<source>` 根で正規化する（#562）

## 背景と事象

`scripts/check-coverage.js` の `accumulate()` は cobertura の `class/@filename` を**生のまま集計キー**に
していた。coverlet が出す `filename` は**そのレポートの `<source>` 根からの相対パス**であり、根は
**テストプロジェクトごとに違う**。結果、**同一の実ファイルが別キーとして複数回数えられる**。

🔴 **皮肉なことに、`coverage-floor.json` の `$comment` は当初から
「集計は (ファイル, 行番号) の和集合で行う（複数テストプロジェクトの重複計上を避けるため）」と
宣言していた。** 意図は文書化されていたが、キーが揃っていないため**実装が意図に達していなかった**。

## 母集合の引き直し（規則 9・10）

**「集計キーを作っている箇所」と「キーを消費している箇所」の両方**を引いた。

| 走査 | 結果 |
| --- | --- |
| `acc` のキーを作る箇所 | `accumulate()` の 1 箇所のみ |
| `acc` のキーを消費する箇所 | `summarize()`（キーを見ない）／`applyExcludes()`（`normalizePath` して glob 照合）／`findShadowedPatterns()` |
| キーの形に依存する除外パターン | `**/Migrations/*.Designer.cs` ／ `**/Migrations/*ModelSnapshot.cs` |

**除外パターンは `**/` 始まりのため、キーが相対パスから絶対パスへ変わっても一致し続ける**
（`globToRegExp` の `**/` は 0 段以上のディレクトリに一致する）。`applyExcludes` は
受け取ったキーを再度 `normalizePath` するが、正規化は冪等なので二重適用は無害である。

**除外したもの**: `findWrongfullyExcluded()` は実ツリーを歩く関数でありカバレッジのキーを使わない。
`findSourceFiles` / `isAutoGenerated` も同様。よって本変更の射程外とした。

## 決めたこと

1. `<sources><source>…</source></sources>` を読み、`filename` が相対なら根と結合して
   **絶対パスを集計キーにする**（`resolveCoverageKey`）。
2. `filename` が既に絶対パスならそのまま使う（結合しない）。
3. **根が複数ある場合は実在する結合を優先**する。実在判定は差し替え可能にした
   —— ファイルシステムに触れずに肯定・否定の両方を固定できるようにするためである。
   どれも実在しなければ最初の根へ倒す（キーが未定義にならないこと）。
4. `<sources>` が無いレポートは従来どおり `filename` をキーにする（後方互換）。

## 🔴 検証手順そのものの落とし穴（本作業で踏んだ）

**`node scripts/scripts.repo.test.js` を直接叩くと、何も実行せずに exit 0 で返る。**
同ファイルは `module.exports = ({ ok, assert }) => {…}` の**モジュール**であり、
実行の入口は `scripts/scripts.test.js` である（同ファイル冒頭のコメントにそう書いてある）。

実際にこれで欺かれた —— **実装をわざと壊した状態で「テストが通る」という結果を得た。**
正しい入口（`node scripts/scripts.test.js`）で走らせて初めて変異が捕まった。
**「テストが緑」は、走らせたコマンドが本当にテストを走らせている場合にのみ意味を持つ。**

## 変異試験（検出力の実証）

| 変異 | 結果 |
| --- | --- |
| `resolveCoverageKey` を使わず `filename` を生でキーにする（＝是正前の挙動） | **exit 1**（和集合のテストが落ちる） |
| 根が 1 つのとき常に同じキーを返す（＝過剰併合） | **exit 1**（過剰併合の否定形が落ちる） |
| 無変異 | exit 0 |

**肯定形（畳むべきものを畳む）と否定形（畳んではいけないものを畳まない）の両方向で落ちる**ことを
確認した。片方だけでは「全部同じキーにする」壊れ方を検出できない。

## 受け入れ基準に対する状態

- [x] 集計キーが `<source>` 根を解決した絶対パスになっている
- [x] 根が異なる 2 つの疑似 cobertura を食わせるテストを置いた（`scripts.repo.test.js`）
- [ ] 正規化後の実測値で `coverage-floor.json` を引き上げる（**測定中。別コミットで行う**）
- [x] 「重なりが 1 件も無い」状態でも空振りしないこと（過剰併合の否定形テストが担保）

## 注記

- issue 本文は floor を `scripts/coverage-floor.json` と書いているが、**実際はリポジトリ直下**の
  `coverage-floor.json` である。
- issue 本文はテストの置き場を `scripts/scripts.test.js` と指定しているが、**既存の
  `check-coverage` のテストは `scripts.repo.test.js` にある**ため、そちらへ置いた
  （`scripts.test.js` はキット配布物でバイト一致を保つ側である）。
- 手書き EF マイグレーションが 0% のまま分母へ積まれる件は issue の宣言どおり**射程外**。

---

## 実測（develop `145446a`・同一のレポート集合で前後を比較）

**同じ `cov/` に対して、集計キーの解決だけを切り替えて測った**（テストは 1 度しか走らせていない
＝収集の揺れが混じらない比較である）。

| | 分子 | 分母 | 行カバレッジ |
| --- | ---: | ---: | ---: |
| 是正前（`filename` を生でキーにする） | 16504 | 20006 | **82.50%** |
| **是正後（`<source>` 根を解決）** | **16208** | **19001** | **85.30%** |
| 差 | −296 | **−1005** | **+2.80pt** |

- **分母から 1005 行が消えた** = それだけ二重計上されていた。
- 分子も 296 行減っているのは、**重複していた被覆行が和集合へ畳まれた**ためである
  （被覆率が上がるのは分母の減り方が大きいから）。
- issue は 2 レポートだけの比較から **+2.31pt** と見積もっていたが、**20 レポート全体では +2.80pt**
  だった。見積もりより大きい。
- レポート件数 **20 = `list-test-projects --count` の 20**（不変条件を確認）。

## floor の引き上げ（ratchet）

`--suggest` の候補どおり **`lineRateFloor` を 0.79 → 0.83** へ引き上げた（実測 85.30% から
`RATCHET_MARGIN` の 2.00% を引いた値）。`measuredAt` / `measuredLineRate` / `measuredNote` も更新した。

🔴 **`measuredNote` に「数値が飛んで見えるのはテストが増えたからではなく、二重計上をやめたためである」
と明記した。** 記録だけを見た人が「カバレッジが急に 3pt 上がった」と誤読するのを防ぐためである。

## 併せて直したもの

`check-coverage.js` 冒頭の測定手順の注記が「清掃後 51 件」という**固定値**を残骸検知の目安として
書いていたが、テストプロジェクト数は移送・統廃合で動く（**実測で 51 → 20**）。固定値は必ず腐るので、
**`list-test-projects.js --count` との一致**を見る書き方へ改めた。

## 受け入れ基準（最終）

- [x] 集計キーが `<source>` 根を解決した絶対パスになっている
- [x] 根が異なる 2 つの疑似 cobertura を食わせるテストを置いた
- [x] 正規化後の実測値で `coverage-floor.json` を引き上げた（0.79 → 0.83。**下げていない**）
- [x] 「重なりが 1 件も無い」状態でも空振りしないこと（過剰併合の否定形が担保・変異試験で実証）

---

## ［2026-08-29 追記］レビュー指摘（🟢）への対応 —— `..` セグメントの解決

**指摘は正しく、再現した。** `normalizePath` は `..` / `.` を解決していなかったため、
`<source>` 根と相対 `filename` の**単純結合**で次のようにキーが割れる:

```
根 /w/backend/Shared/AiStockTrading.Shared.Contracts/ + Events/X.cs
  → /w/backend/Shared/AiStockTrading.Shared.Contracts/Events/X.cs
根 /w/backend/Shared/Other/ + ../AiStockTrading.Shared.Contracts/Events/X.cs
  → /w/backend/Shared/Other/../AiStockTrading.Shared.Contracts/Events/X.cs   ← 別キー
```

🔴 **これは本 PR が消したはずの二重計上が、別の入り口から復活する経路である。**
`normalizePath` は**集計キーと除外パターン照合の両方が通る唯一の関門**なので、そこで畳んだ
（`path.posix.normalize`）。

**実測値は変わらない**（85.30%・16208/19001・レポート 20 件）。現行の coverlet 出力に `..` を
含む相対パスが無いというレビューの観察と一致する。**したがって floor 0.83 の根拠は影響を受けない。**

変異試験: `path.posix.normalize` を外すと **exit 1**（追加した 2 テストが落ちる）。
冪等性（`applyExcludes` が再度かけても壊れない）も固定した。

---
title: IADR-0369 成行手仕舞いの「確認できた拒否」は手仕舞い済みとして扱わず、記録を巡回に残したまま上限つきで撃ち直す
type: impl-adr
status: Accepted
related_ids: [FR-10, FR-11, UC-02, UC-06, ADR-0003, ADR-0040, IADR-0057, IADR-0113, IADR-0117, IADR-0210, IADR-0344, IADR-0370]
author: claude (Claude Code)
created: 2026-09-23
updated: 2026-09-25
plan_refs:
  - planning:projects/ai-stock-trading/02_requirements/01_requirements.md (FR-10「逆指値が未受理・失効した場合は建玉を持たない」)
  - planning:projects/ai-stock-trading/04_workflows/02_event-driven-trading.md (業務フロー 02「逆指値が成立しない場合の扱い」)
---

# IADR-0369: 確認できた拒否は「手仕舞い済み」ではない

- 状態: Accepted
- 日付: 2026-09-23
- 決定者: claude（起票 #857。利用者レビューは PR で受ける）

## 起点・関連

- 関連する計画書 ID: FR-10（損切りの実行機構・逆指値なしの建玉を持たない）、FR-11（監査）、UC-02、UC-06、ADR-0003
- 対象 Issue: #857（#851 の 3 巡目監査 B4 の過程で引き当てた**同型だが射程外**の穴）
- 関連する実装仕様書: [20260923_857_rejected-close-is-not-closed](../specs/20260923_857_rejected-close-is-not-closed.md)
- 関連 IADR: [IADR-0210](IADR-0210_broker-side-stop-loss-unification.md)（決定 3・4 の建玉解消）、
  [IADR-0117](IADR-0117_owner-position-close-path.md)（改定 7・改定 9。**届いたか不明**の扱い）、
  [IADR-0344](IADR-0344_s1-software-stop-loss.md)（決定 5-6。S1 の決済試行上限＝本 IADR が S0 へ写す形）、
  [IADR-0057](IADR-0057_order-dispatch-idempotency.md)（発注 3 相）

## コンテキストと課題

IADR-0117 の改定 6〜9（#848 / #851）は「**確実に未発注**」と「**送ったが届いたか不明**」を分離した。
その裏側に、同型の混同がもう 1 つ残っていた ——「**確認できた拒否**」を「**手仕舞い済み**」として扱っていた。

`ProtectiveStopGuard.ReplaceOrCloseAsync` と `OrderExecutionAppService.CloseUnprotectedPositionAsync` は、
`PlaceMarketOrderAsync` が**返り**さえすれば `closeOrder.Status` を見ずに成功として扱っていた。
発注前検証の棄却や OpenD の `retType != 0` は**終端 `Rejected` を返す**（例外ではない）ため、この経路に入る。

| 帰結 | 内容 |
| --- | --- |
| 通知が事実と逆 | Critical で「建玉を成行で手仕舞いました」と言うのに、建玉は残っている |
| 🔴 巡回から消える | 保護記録が `Completed` になり、**逆指値なしの建玉を誰も見なくなる**（IADR-0210 の不変条件が無音で破れる） |
| 台帳が空押さえ | 送られてもいない決済の承認行が足され、30 分の窓のあいだ在庫を押さえる |

SIMULATE は公式に「指値・成行のみ」だが、時間外の成行や数量の制約で拒否され得る（#809 で逆指値の拒否を実測済み）。

## 検討した選択肢

### 拒否された行の扱い

| 案 | 内容 | 判定 |
| --- | --- | --- |
| (a) 完了させる（現状） | 状態を見ずに `PositionClosed` | **不採用。** 本 issue そのもの |
| (b) 予約を `Reserved` のまま据え置く（改定 7 と同じ形） | 次の巡回は入口 (b) で据え置き | **不採用。確定した事実を「不明」として扱うことになる。** 撃ち直しも起きず、出口が恒久的に塞がる |
| **(c) 記録は `Active` のまま・試行番号を進めて次の巡回で再評価**（採用） | S1 の `Settle`（IADR-0344 決定 5-6）と同じ形 | 既に系内にある形であり、巡回の不変条件（無保護の建玉は必ず誰かが見ている）を保つ |

### 撃ち直しの上限をどこに持つか

| 案 | 内容 | 判定 |
| --- | --- | --- |
| 上限を設けない | 30 秒ごとに成行を送り続ける | **不採用。** 同じ理由で拒否され続ける注文を叩き続けるのは、実弾では発注制限にも当たる |
| 永続列（`protective_stop_orders` に `CloseRejections`） | 再起動をまたいで数える | **不採用。** 消えない側に倒れる＝**再起動しても出口が閉じたまま**になる。保護の帳簿（`RemainingProtected` 等）と違い、この数えは**安全側が「忘れる」側**である |
| **プロセス内の数え（`CloseRejectionTracker`）**（採用） | `HeldCloseNotificationTracker`（改定 9）と同じ作法 | 再起動で数えが消える＝**もう一度手仕舞いを試みる**。倒れ方が fail-loud の向きに一致し、スキーマも増えない |

## 決定

### 決定 1: 「未約定残を二度と約定させない終端」が**返った**ら `PositionClosed` を主張しない

判定は既存の純関数 `OrderStatusLifecycle.AbandonsUnfilledRemainder`（`Rejected` / `Cancelled` / `Expired`。
**`Filled` を含まない**）を使う。新しい述語は作らない（IADR-0117 改定 2 の「足すなら述語を足す」に対し、
ここは**既にある述語がそのまま当たる**）。真のとき:

- 保護記録は **`Active` のまま**残し、**試行番号だけ**を進める（`RemainingProtected` は動かさない ——
  減ったのは建玉ではない）。次の巡回は新しい `CloseDecisionId` で改めて評価する。
- 発注結果（`ExecutionRecord`）は**保存する**（拒否の一次証跡。終端なので約定追跡には載らない）。
- 予約は **`MarkCompleted` で確定する**。確認できた拒否は不明ではないため、据え置く理由が無い。
- `ProtectiveStopCoverageLost(Remediation=CloseRejected)`（Critical）を発行する。
  🔴 **`CloseIntent` は運ばない**（生きていない成行を取引台帳に押さえさせない）。
  `CloseDecisionId` は**載せる**（拒否された発注記録との相関）。この非対称は意図であり、
  `ProtectiveStopEventPayloadTests` が往復で固定する（台帳側は「レグの有無」で分岐するため承認行は足されない）。

### 決定 2: 「記録済みの手仕舞いレグ」を読む入口でも状態を見る

改定 7 の入口 (a)（`store.FindByDecisionId(closeDecisionId)` に記録がある）でも、その記録が終端（拒否）なら
完了させない。送信直後に保護記録の更新だけが失われたクラッシュ窓では、**拒否された記録**が残るため、
ここを塞がないと次の巡回が「記録があるから手仕舞い済み」と読む（決定 1 と同じ誤りが別経路で再発する）。

### 決定 3: 撃ち直しの上限は 3 回。数えはプロセス内に持ち、上限に達しても記録を閉じない

- 上限は S1（`SoftwareStopExecutor.MaxCloseAttemptsPerTrigger`）と同じ **3**。
- 数えと再通知の記憶は `CloseRejectionTracker`（singleton・**非永続**）。再起動で消える＝改めて 3 回試す。
- 上限に達した行は**成行を送らない**が、**記録は閉じない**（閉じると巡回から外れ、無音で無保護の建玉が残る）。
  未通知なら即座に、以後は **1 時間ごと**に `CloseRejected` を発行し直す（`CloseDecisionId` は null ——
  その巡回では 1 本も送っていないため、送っていない ID を載せない）。
- **逆指値の再発注には上限を掛けない**（保護の回復は常に試みてよい）。再発注に成功したら数えを 0 へ戻す。

［2026-09-24 追記 / PR #916 監査］ **「S1 と同じ上限 3」は回数だけが同じで、上限の性質は同じではない。**
上の 1 項目めと理由の「S1 が既に持っている形を S0 へ写した」は、その違いを書いていなかった。

| | S1（`SoftwareStopExecutor.MaxCloseAttemptsPerTrigger`） | 本 IADR の S0（`ProtectiveStopGuard.MaxConfirmedCloseRejections`） |
| --- | --- | --- |
| 数える単位 | **損切りラインへの到達 1 回あたり**（`attempt % 3 == 0` で使い切り） | **保護記録ごとの累計**（`CloseRejectionTracker`・プロセス内） |
| 使い切った後 | `TriggeredAt` を消して記録を**再武装**する。次に価格がラインへ到達すれば、人手なしで改めて 3 回試す（**自己回復する**） | **再武装が無い。** 市場の事象で数えが戻ることは無く、成行は送られない（通知だけが 1 時間ごとに続く） |
| 数えが戻る契機 | 次の到達（上記） | **再起動**・**逆指値の再発注の成功**・**手仕舞いの約定**（記録が完了する）だけ |

したがって本 IADR の上限は、S1 と違って**使い切ると人の手当て（証券会社の画面での手仕舞い・原因の除去）か
再起動まで出口が閉じたまま**になる。残余リスクの「上限に達した後の自動復旧は無い」はこの違いの帰結であり、
「同じ問いに 2 つの答えを持たない」は**回数の選び方**についてだけ成り立つ。決定そのもの（3 回・非永続・記録を閉じない）は変えない。
あわせて同監査で次を是正した（決定は変えない）:

- **エントリー同時の経路（`Cause=RejectedAtEntry`）の `CloseRejected` 通知**が、滞留側の約束（保護記録を閉じず巡回を続ける・
  約 1 時間ごとに繰り返す・3 回で打ち切る）を述べていた。この経路は保護記録を作らない（残余リスクの 3 項目め）ため、
  どれも事実ではない。通知の後半を `Cause` で分け、エントリー時は「巡回しない・撃ち直さない・この 1 回だけ・手で手仕舞う」と書く（T-10-684）。
- **上限に達した巡回の `CloseRejected` の発行が落ちたとき**、ガードは発行の前に通知済みを記憶するため、最大 1 時間黙っていた。
  常駐の `PublishAllAsync` が改定 9 と同じ形で未発行分の**通知の記憶だけ**を消す（拒否の数えは消さない——消すと通知の失敗が
  成行の撃ち直しへ化ける）（T-10-685）。
- 上限の値そのものを T-10-636 で固定した（従来の表明は定数を参照しており、3 → 4 の書き換えが緑のまま通った）。

［2026-09-25 追記 / #941］ 決定は変えず、次の 2 点を是正した（作業仕様書 `20260925_941_entry-indeterminate-close-no-repeat-promise`）。

- **上の表「数えが戻る契機」の「手仕舞いの約定」は「手仕舞いの受理」が正しい。** 数えを捨てる `CompleteAsClosed` は、
  成行の戻り値が `Cancelled` / `Rejected` / `Expired` でない（`Accepted` / `PartiallyFilled` / `Filled`）とき、
  および記録済みの手仕舞いレグが同じく終端でないときに呼ばれ、約定を待たない（受理の時点で保護記録も完了し、以後この記録は巡回されない）。
  `CloseRejectionTracker` の冒頭と `ProtectiveStopGuard.MaxConfirmedCloseRejections` の注記も同じく直した。
- **上の F1 と同型の偽りの約束が `CloseDispatchIndeterminate` にも残っていた。** その本文は `Cause` を見ず
  「予約が解決されるまで約 1 時間ごと（と再起動のたび）に繰り返します」と書いていたが、1 時間ごとの再通知
  （IADR-0117 改定 9）は `HeldCloseNotificationTracker` が持ち、それを使うのは保護記録を巡回する `ProtectiveStopGuard` だけである。
  エントリー同時の経路（`RejectedAtEntry`・`OrderExecutionAppService.IndeterminateClose`）は保護記録を作らず、
  同じ承認の再配送も相 1 で返って保護喪失を出し直さない（IADR-0117 改定 9 の「塞がないもの」のとおり）。
  F1 と同じく後半を `Cause` で分け、エントリー時は「巡回しない・この通知も繰り返さない（この 1 回だけ）・
  証券会社の画面で手仕舞いの注文と建玉を確かめ、手で手仕舞う」と書く（T-10-750）。根拠（保護記録が無い・
  1 時間後の巡回がイベントを出さない・再配送が出し直さない）はコードの側でも固定した（T-10-751）。

### 決定 4: 出口を入口のガードで塞がない

上限は**成行手仕舞い（出口）だけ**に掛ける。逆指値の再発注・建玉消滅時の取消・S1 の決済・利用者の手仕舞いには
掛けない。また、上限に達しても保護記録を閉じない（閉じることは「出口を塞いだうえで忘れる」ことである）。

### 決定 5: 可観測性でも「拒否」を「不明」に混ぜない

`ProtectiveStopGuardResult` に `CloseRejected` 件数を**末尾へ**足し、`Unknown`（据え置き＝不明）とも
`ClosedOut`（解消した）とも混ぜない。通知・監査の文面も 3 者を書き分ける
（「手仕舞いました」／「送ったが届いたか不明」／「拒否された・建玉が残っている」）。

［2026-09-25 追記 / #938］ PR #916 監査の F4・F5 を是正した（決定は変えない。作業仕様書 `20260925_938_guard-tracker-completion-and-close-failed-count`）。

- **F5（本決定の取りこぼし）**: 成行手仕舞いも**確実に未発注**で失敗した巡回（`Remediation=None`。記録は `Active` のまま次の巡回で撃ち直す）が
  `ClosedOut` を返し、巡回ログの「手仕舞い」に数えられていた（本 IADR より前から同じ）。本決定と同じ理由で混ぜない ——
  `Outcome.CloseFailed` を足し、`ProtectiveStopGuardResult` の**末尾**へ `CloseFailed` を足して、巡回ログは
  「手仕舞い失敗（未発注・建玉残存）」を別枠で出す。警告を出す条件にも足した（分けた後にその巡回の行が出なくならないように）（T-10-756・T-10-757）。
- **F4（決定 3 の記憶の後始末）**: `CloseRejectionTracker` の記憶は `Replaced` と `CompleteAsClosed` でしか捨てず、ガードが記録を完了させる
  他の経路（建玉消滅→残存逆指値の取消・逆指値の `Filled`・失効かつ建玉 0）では再起動まで残った。`HeldCloseNotificationTracker`
  （IADR-0117 改定 9）にも同型の残りがあった（据え置き中の完了・据え置いたレグが突合で拒否と確定・解放後の再発注）。
  **記録を完了させる出口 `MarkCompleted` で両方を捨てる**ことにし、拒否と再発注では試行が進んで二度と引かれないレグの据え置きを捨てる（T-10-752・T-10-753）。
  さらに監査が挙げていない経路として、**ガードの外**で記録を完了させる乖離の取り込み（IADR-0370・`ProtectiveStopDriftAdopter`）がある。
  ガードは巡回の冒頭（巡回対象が 0 件で戻るより前）で、記憶に載っている記録だけを引き直し、`Active` の記録が無い（行が無い・`Completed`）ものを捨てる（T-10-754）。
  🔴 **引き直しが例外で失敗したら捨てない**（不明は「無い」ではない。捨てると数えが 0 へ戻り、まだ `Active` かもしれない記録へ成行を撃ち直す側へ倒れる）（T-10-755）。
  依存は変えていない（ガードは既にストアと両方の記憶を持つ）。据え置きの記憶は `CloseDecisionId` から保護記録を逆に引けないため、
  `EntryDecisionId` も併せて覚えるようにした。
- あわせて上の表「数えが戻る契機」には、#938 以降「記録の完了」も加わる（完了した記録へ撃ち直すことは無いので、数えの意味が無くなる）。

［2026-09-25 追記 / #948］ 決定は変えず、F1（PR #916）・#941 と同型の偽りの約束が**説明文**の側に残っていたのを是正した
（作業仕様書 `20260925_948_coverage-lost-cause-aware-descriptions`）。

- 契約 `ProtectiveStopRemediation.CloseRejected` の xmldoc は `Cause` を見ず「記録は `Active` のまま残り、次の巡回が改めて評価する」と、
  機能仕様の保護喪失の表の `Remediation=None` の行は「常駐ガードは次の巡回で撃ち直す」と書いていた。
  エントリー同時の経路（`RejectedAtEntry`）は保護記録を作らず、同じ承認の再配送も相 1 で返るため、`None` でも `CloseRejected` でも
  **巡回・撃ち直し・再通知は無く、通知は 1 回きり**である（上の残余リスク 3 項目めと #941 の T-10-751 の根拠）。
  両方を原因ごとに書き分け、表に `CloseRejected` の行を足した（上の決定 1 の「記録は `Active` のまま」は滞留側 `LapsedInFlight` の決定として読む）。
- 走査の過程で、`None` の通知の**件名**が既定の腕に落ちて「保護逆指値が成立せず建玉を解消」になっていた（本文は「解消にも失敗しました」）。
  件名を「保護逆指値が成立せず、建玉の解消にも失敗」へ直した（T-10-854）。
- 走査の語は「巡回」「撃ち直」「Active のまま」「繰り返し」「次の」。PR #944 の母集合は「1 時間ごと」の文字列だけで引いていたため、
  説明文のこれらを取りこぼした。

## 理由

- **fail-loud であって fail-close ではない。** 拒否は情報の欠落ではなく確定した事実であり、
  それを握り潰して完了扱いにすると、**唯一の保護であるはずの巡回から建玉が消える**。
  倒れる向きは常に「まだ守っている・まだ鳴っている」側に取る。
- **上限を非永続にするのは、消える向きが安全側だからである。** 保護の帳簿（`RemainingProtected`・
  `PendingExternalReduction`）は消えると売り過ぎ／保護喪失に直結するため永続だが、
  この数えは消えると「もう一度出口を叩く」だけである。IADR-0117 改定 9 の通知記憶と同じ判断。
- **S1 が既に持っている形を S0 へ写した**（IADR-0344 決定 5-6）。同じ問いに 2 つの答えを持たない。

## 結果

- 拒否された手仕舞いの建玉は巡回対象に残り、通知は Critical のまま**事実を言う**ようになる。
- 取引台帳は拒否された手仕舞いレグの承認行を持たなくなる（空押さえが消える＝利用者の手仕舞いが通る）。
- 30 秒ごとの成行の連打は 3 回で止まり、通知は 1 時間ごとに続く。

## 残余リスク

- **終端かつ部分約定ありの手仕舞いレグ**（一部約定してから `Cancelled` 等）は、約定追跡（IADR-0113）が終端記録を
  ポーリングしないため `OrderExecuted` が出ず、**部分約定が取引台帳へ届かない**。本 IADR 以前からの性質であり
  （`PositionClosed` を主張していた従来も台帳の建玉は減らなかった）、ここでは直さない。
  次の巡回は**ブローカーの建玉照会**から数量を引き直すため**売り過ぎは起きず**、差は乖離として検知される（IADR-0118）。
- **上限に達した後の自動復旧は無い**（人が証券会社の画面で手仕舞うか、原因を取り除く）。
  再起動・逆指値の再発注成功のどちらかで数えは戻る。
- エントリー同時の経路（`CloseUnprotectedPositionAsync`）には**保護記録が無い**ため、拒否された建玉を
  巡回へ載せる手段が現状ない（通知が唯一の検知）。保護レグを持たないまま台帳へ載る建玉の扱いは #853 の射程である。

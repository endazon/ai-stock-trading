---
title: IADR-0344 S1 ソフトウェア逆指値 — 保護記録に機構列を足して永続化し、発注執行が損切り到達を購読して到達を先に記録し、予約つき固定 DecisionId の成行で 1 回だけ決済する
type: impl-adr
status: Accepted
related_ids: [FR-10, FR-12, FR-11, FR-09, UC-02, ADR-0040, ADR-0016, IADR-0014, IADR-0030, IADR-0035, IADR-0057, IADR-0113, IADR-0118, IADR-0129, IADR-0210, IADR-0342, IADR-0412, IADR-0365, IADR-0380]
author: claude (Claude Code)
created: 2026-09-18
updated: 2026-09-25
plan_refs:
  - planning:projects/ai-stock-trading/07_adr/ADR-0040_simulate-stop-loss-method-is-selectable.md (決定1 の S1・§結果 悪い影響「S1 は二重決済の経路を SIMULATE に戻す」)
  - planning:projects/ai-stock-trading/02_requirements/01_requirements.md (FR-10 の 3 文〔口座種別の軸〕)
  - planning:projects/ai-stock-trading/04_workflows/02_event-driven-trading.md (§損切りの実行機構)
---

# IADR-0344: S1 ソフトウェア逆指値

- 状態: Accepted
- 日付: 2026-09-18
- 決定者: claude（起票 #820。利用者レビューは PR で受ける）

## 起点・関連

- 関連する計画書 ID: **ADR-0040 決定 1 の S1**（既存の損切り検知を購読し成行で決済する）、FR-10（口座種別の軸）、FR-12、UC-02、
  ADR-0016 決定 2(b)（空売りは常に S0。改めない）
- 対象 Issue: #820（取り込み: #826 項目 2・3）
- 関連する実装仕様書: [20260918_820_s1-software-stop](../specs/20260918_820_s1-software-stop.md)
- 関連 IADR: [IADR-0342](IADR-0342_simulate-stop-loss-method-selection.md)（選択機構。S1 のフォールバックを本 IADR で外す）、
  [IADR-0210](IADR-0210_broker-side-stop-loss-unification.md)（S0・保護記録・ガード）、
  [IADR-0057](IADR-0057_order-dispatch-idempotency.md)（予約）、[IADR-0113](IADR-0113_moomoo-fill-polling.md)（約定追跡）、
  [IADR-0118](IADR-0118_broker-position-reconciliation.md)（建玉照会の null＝不明）、
  [IADR-0014](IADR-0014_market-monitor-events-and-boundary.md) / [IADR-0030](IADR-0030_position-store-sync-api.md) /
  [IADR-0035](IADR-0035_stop-loss-authoritative.md)（到達検知と損切りラインの供給元）、[IADR-0129](IADR-0129_wolverine-messaging-topology.md)（購読キュー）

## コンテキストと課題

moomoo の模擬取引は指値・成行しか受け付けず（`OrderType_Stop` は拒否・当日限り）、S0 では SIMULATE に建玉が残らない。
S2 は建玉を残すが損切りが起きない。S1 は「市場監視の到達検知（60 秒周期の `StopLossTriggered`）を発注執行が購読して成行で決済する」
ことで、ペーパーでも損切り決済までを観測可能にする。ADR-0040 は S1 が 2026-07-31 の裁定で消した**二重決済の経路を SIMULATE に戻す**
ことを悪い影響として明記しており、実装の中心課題は**二重決済を作らないこと**である。

決めるべきは (a) ソフトウェア逆指値をどこに永続化するか、(b) 市場監視は S1 の損切りラインを知っているか、(c) 到達をどう冪等に決済へ変えるか
（市場監視は価格が戻るまで毎巡回再発火する・メッセージは再配送される・ガードと並行し得る）、(d) 部分約定・未約定の扱い、
(e) ガードと手法混在、(f) 監査・通知・台帳、(g) 再起動耐性、である。

## 検討した選択肢

1. **(a) 新テーブル `software_stop_orders`** — 意味は分かれるが、ガードの巡回・EF ストア・インメモリ実装・テストの足場を二重に持つ。
   手法混在の按分では両テーブルを突き合わせることになる。**却下**。
2. **(a) 既存 `protective_stop_orders` に機構列を足す**（採用）— 1 エントリー＝高々 1 保護の主キーがそのまま効き、ガードは同じ巡回で両方を見られる。
3. **(b) 市場監視へ手法と損切りラインを別経路で供給する** — 不要と判明した。市場監視は台帳（リスク管理 `GET /risk-controls/open-positions`）の
   建玉を評価し、損切りラインは承認 Intent の `StopLossPrice`（IADR-0035）である。S1 の承認も `StopLossPrice` を必ず持つ（無ければ見送り）。**変更しない**。
4. **(c) リスク管理が到達を受けて Close の `OrderApproved` を出す**（旧 IADR-0015 の形）— 保護記録（手法）を持つのは発注執行だけであり、
   S0 / S2 の建玉にまで決済を出す危険がある。発注執行の購読で閉じる。**却下**。
5. **(e) 損切り到達の通知に建玉ごとの手法を載せる**（台帳の承認行に手法を保存 → open-positions → 市場監視 → `StopLossTriggered`）—
   3 サービスとリスク管理の migration を要し、台帳は銘柄単位で最新エントリーの値しか持たない（混在時に誤る）。**本 IADR では採らない**（決定 7）。

## 決定

1. **永続化**: `protective_stop_orders` に `Mechanism`（`StopLossExecutionMethod`・非 null・既定 0＝S0）・`TriggeredAt`（null）・`TriggeredPrice`（null）を足す
   （migration `AddSoftwareStopColumns`。既存行は S0 として読まれる）。S1 の行は `StopOrderId=""`、`StopDecisionId=ProtectiveStopIds.SoftwareStopId(entry)`、
   `TriggerPrice`＝承認の `StopLossPrice`、`Quantity`＝承認数量、`Attempt`＝**送った決済の試行数**（0 始まり）。
   `IProtectiveStopOrderStore.FindActiveSoftwareStops(symbol, market, entrySide)` を足す。
2. **解決**: `StopLossMethodPolicy` は S1 を `StopLossMethodDisposition.SoftwareStop` へ解決する（S0→S0／非 SIMULATE→拒否／空売り→S0／S2→免除 の順序は不変。
   S3・未知だけが従来のフォールバック）。
3. **発注時**（`OrderExecutionAppService`）: S1 はブローカーの逆指値能力（成行決済に使う `IProtectiveOrderBroker`）と保護記録ストアを要し、
   欠ければ見送る（`StopOrderUnsupported`）。🔴 **エントリーを送る前に行を Active で保存する**（行が無ければのみ）——送った後に保存すると
   「建玉はあるのにソフトウェア逆指値が無い」窓ができる。送った結果が生きていれば（Accepted / PartiallyFilled / Filled）`SoftwareStopArmed` を発行し、
   終端失敗・見送り（接続断）なら行を Completed にする。送信後の不明（例外）は行を Active のまま残す（ガードが扱う）。
4. **到達**（発注執行の新ハンドラ `StopLossTriggeredHandler` → `SoftwareStopExecutor.OnTriggeredAsync`）: 銘柄・市場・建玉方向が一致する Active な S1 行のうち、
   **行の作成が検知時刻以前**で、**検知時の価格が行自身の損切りラインに達している**（買い建て: 価格 ≦ ライン）ものだけを対象にする。
   🔴 **決済の前に到達を永続化する**（`TriggeredAt` / `TriggeredPrice`。既に記録済みなら上書きしない）。以降の決済が据え置きになっても、
   次の到達を待たずにガードが再試行する。**一度到達したら価格が戻っても決済する**（FR-10: 損切りラインは到達で発動する。遅れて届いた到達も同じ）。
   行自身のラインで判定するのは、台帳の損切りラインが銘柄単位で最新エントリーの値に丸められるため（同一銘柄の別エントリーを巻き込まない）。
5. **決済**（`SoftwareStopExecutor.TryCloseAsync`。ハンドラとガードが共有）:
   1. エントリーの記録（`executed_orders`）があり**未終端**ならブローカーで取り消し、再照会して**終端になるまで決済しない**（据え置き）。
      約定 0 で終端 → 行を Completed にし、自分が取り消したなら `SoftwareStopExecuted(EntryCancelled)` を出す。記録が無い（送信後不明）なら行の数量を上限に扱う。
   2. 建玉を照会し（null＝不明は据え置き）、**決済数量＝min(エントリーの約定数量, 建玉残)**。建玉残は同じ銘柄・方向の建玉から
      **手法の異なる Active 行の数量を差し引いた値**（決定 6）。0 以下なら決済せず Completed（手動決済等で既に無い）。
   3. `attempt = Attempt + 1`、`CloseDecisionId = ProtectiveStopIds.SoftwareCloseDecisionId(entry, attempt)`。
      🔴 **二重決済の防止は 3 重**: (i) 同じ DecisionId の発注記録があれば再送せずその結果で扱う、(ii) 送る前に予約表で DecisionId を確保し
      （IADR-0057 の一意制約）、確保できなければ送らない（ハンドラとガードの並行・再配送）、(iii) 行が Completed なら候補にならない。
   4. 成行を送る（参照価格＝検知時の価格）。接続断（`BrokerUnavailableException`）は予約を解放して据え置き。その他の例外は予約を残して据え置き
      （届いたか不明＝同じ DecisionId では再送しない。滞留予約はリコンサイルの領分）。
   5. 受理（Accepted / PartiallyFilled / Filled）→ 記録を保存し予約を確定、行を Completed、`SoftwareStopExecuted(ClosePlaced)`（決済 Intent 同伴）。
      約定は既存の約定追跡（IADR-0113）が `OrderExecuted` で台帳へ届ける。
   6. 拒否（Rejected / Cancelled / Expired）→ 記録を保存し `Attempt` を進める。**到達 1 回につき 3 試行で打ち切り**、到達の記録を外して
      `SoftwareStopExecuted(CloseRejected)`（Critical・手動決済を促す）。次の到達で再開する（市場監視は価格が戻るまで再発火する）。
      moomoo の模擬取引は時間外の成行を受け付けない可能性があり、無制限に送ると拒否注文を積み続けるため。
6. **ガード**（`ProtectiveStopGuard`）: **S1 行はブローカーの注文照会をしない**。到達済みなら決定 5 を再試行、未到達なら
   「エントリーが約定 0 で終端」または「建玉残（決定 5-2 の按分）が 0 以下」で Completed、それ以外は据え置く。
   **S0 行の建玉残もソフトウェア逆指値の約定数量（記録が無ければ 0）を差し引いて判定する**（#826 項目 3 の S1 側）——差し引かないと
   S1 の建玉が S0 の逆指値を生かし続け、建玉消滅後に残った逆指値が反対建玉を生む。S1 が無い構成（実弾・S0 のみ）では差し引く量が 0 で**挙動は不変**。
   S2 は記録を持たないため按分に入らない（残余リスク）。
7. **通知文**（#826 項目 2）: 損切り到達を検知する市場監視は建玉ごとの手法を知らない（選択肢 5 を採らない）。`StopLossTriggered` の通知と
   リスク管理の検知ログは「S0＝ブローカー側の逆指値が決済／S1＝システムが成行で決済（別途通知）／S2＝誰も決済しない（手動で決済）」を
   **列挙**する文へ改める。S1 の決済・拒否は `SoftwareStopExecuted` の通知が建玉を特定して伝え、S2 は免除の通知が建玉ごとに明示済みである。
8. **契約**: 新イベント 2 種（既存イベントの形は変えない）。
   - `SoftwareStopArmed`（EntryDecisionId, Symbol, Market, Side, ProductType, Quantity, StopLossPrice, Provider, OccurredAt）— 監査・通知（Warning:
     「システム停止中は決済されない」を明記）。
   - `SoftwareStopExecuted`（EntryDecisionId, Symbol, Market, Outcome, Quantity, StopLossPrice, TriggeredPrice, Attempt, CloseDecisionId?, CloseOrderId?,
     CloseIntent?, OccurredAt）・`SoftwareStopOutcome`（`ClosePlaced=0` / `EntryCancelled=1` / `CloseRejected=2`）— 監査・通知
     （ClosePlaced / EntryCancelled は Warning、CloseRejected は Critical）・リスク管理の台帳結線（ClosePlaced のとき `AppendApproval`。冪等は DecisionId）。
9. **購読と再起動耐性**: 発注執行は `StopLossTriggered` を `ai-stock-trading.order-execution-service.StopLossTriggered` で購読する（IADR-0129 の共通ヘルパ。
   Wolverine 6.24.5 の RabbitMQ キューは既定で durable）。停止中に発行された到達はキューに残り再開後に届く。行は DB にあり、再起動後の到達・ガードの巡回が
   そのまま決済する。ハンドラは Wolverine の規約発見とビルド時 codegen に載り、依存（`SoftwareStopExecutor`）は構成を問わず登録する
   （建玉照会は moomoo 構成でだけ解決され、内蔵 paper では据え置きになる。S1 は内蔵 paper で選べない＝行ができない）。
   宣言 `pipeline.json` に段 `execute-software-stop` を足す（変換段: 到達 → 決済）。

## 理由

- **到達の永続化を決済より先に置く**のは、損切りの発動を「その時点で決済できたか」に依存させないためである。据え置き（接続断・建玉不明・取消待ち）を
  ガードの巡回（既定 30 秒）で拾えば、価格が戻って到達が途絶えても決済が失われない。
- **予約表の再利用**は、IADR-0057 が発注の at-most-once を既に保証している仕組みであり、新しい排他（行ロック・楽観並行）を足さずに
  ハンドラとガードの並行・再配送を塞げる。
- **未終端のエントリーを取り消してから決済する**のは、決済後に残りが約定すると無保護の建玉が生まれるため。終端を待つ間の遅延はガードの巡回 1 回分である。
- **数量を建玉と約定数量の小さい方にする**のは、手動決済・一部決済の後に全量を売ると反対建玉（空売り）を作るためである。

## 結果・残余リスク

- 良い影響: SIMULATE＋S1 で建玉が残り、損切りライン到達で成行決済まで観測できる。S0・S2・実弾の挙動は不変（S0 のガードは S1 行が無ければ差分 0）。
- **システム停止中は決済されない**（S1 の本質。ブローカー側に保護が無い）。`SoftwareStopArmed` の通知が明記する。
- **到達の遅延**: 市場監視 60 秒周期＋キュー＋（据え置き時）ガード 30 秒。損切りラインから価格が離れて約定し得る（成行）。
- **moomoo アダプタは送信後の SDK 例外を Rejected に丸める**（既存の挙動）。実際には届いていた場合、次の試行（別 DecisionId）が重なり得る。
  次の試行の前に建玉を照会し直すため、先の注文が約定済みなら数量 0 で止まるが、**未約定のまま滞留していると二重に出る**。SIMULATE 限定で頻度は低い。
- **即時約定で返った決済は約定追跡に載らない**（非終端だけを追跡する。S0 の保護喪失の手仕舞いと同じ既存の穴）。moomoo は発注応答を Submitted で返すため現状は起きない。
- **同一銘柄の S1 と S2 の混在**: S2 は記録を持たないため按分できず、S1 側の建玉が手動決済で先に消えていると S1 の決済が S2 の建玉を売り得る。
- **損切り到達の通知は建玉ごとの手法を示さない**（列挙文。決定 7）。手法の表示は #823。
- 市場監視は平日判定だけで時間外も到達を出す。時間外の成行拒否は決定 5-6 の打ち切りで抑える。

## ［2026-09-18 追記 / #820 の監査］同じ銘柄の複数行への配分と、エントリー記録が無い行の扱い

監査（フレッシュな文脈・PR #830）が 2 件の売り過ぎを実測した。**挙動を直した。決定 1〜9 の方針は変えない。**

1. **決定 5-2 を改める（配分）**: 行ごとに「建玉残」を上限にしていたため、同じ銘柄・方向の S1 行が同じ建玉を二重に
   主張した（実測: 10 株の行 2 件＋手動売却 5 株 → 20 株の決済 vs 保有 15 株＝反対建玉）。**同じ銘柄・市場・方向の
   Active な S1 行へ、記録の作成時刻（＝エントリーの発注順）→ EntryDecisionId の順に建玉残を配分する**
   （`ProtectiveStopNetting.AllocateSoftwareStops`）。配分は決定的で、ハンドラとガードのどちらから呼んでも同じになる。
   - 🔴 **順序に到達時刻を混ぜない**。1 回の到達を処理する途中で行へ到達を書き込むと順序が変わり、どの行にも
     持ち分が回らなくなる（実測で 2 件とも据え置きになった）。
   - 🔴 **発注済みで未約定の決済数量を差し引く**。建玉照会は決済が約定するまで減らないため、差し引かないと
     次の行へ同じ建玉をもう一度配分する（実測で 20 株になった）。窓は 1 日（当日有効の注文しか出さない）。
   - **持ち分が 0 の行は完了させず据え置く**。他行の決済で建玉が減れば次の巡回で持ち分が生まれる。完了させると
     保護のない建玉が残る。建玉そのものが無い（方向の純額が 0）ときだけ従来どおり完了する。
2. **決定 5-7 を足す（孤立行）**: エントリーの発注記録が無い行は、従来は**行の数量で決済していた**ため、同じ銘柄の
   別のエントリーの建玉を売った（実測: 孤立行＋実在の 10 株で 20 株）。**記録が無いあいだは 1 株も決済しない。**
   `SoftwareStopExecutor.DefaultOrphanGrace`（15 分）を過ぎても記録が無ければ、決済を出さずに行を閉じて
   `SoftwareStopExecuted(EntryMissing=3)` を発行する（通知・監査は Critical）。閉じないと毎巡回この行で決済を試み続ける。
   猶予は、送信と記録のあいだのクラッシュ窓・予約のリコンサイルが記録を補える長さに合わせた。
3. **記録済みの決済を配分より先に確定する**: 同じ試行の決済記録がある（クラッシュ窓）ときは、建玉照会も配分も行わず
   記録の結果で行を確定する。差し引きを先に効かせると自分の決済で持ち分 0 になり、確定できなくなる（実測）。

残る監査指摘（**本追記では直していない**）: 決済が受理後に取消・失効した場合の再武装、拒否の連発に対する待ち時間、
古い写しからの上書き（楽観並行）、無期限の据え置きの Critical 化。**#833 で扱う。**

## ［2026-09-18 追記（2） / #820 の 2 巡目監査］差し引きの単位を「建玉照会がまだ映していない決済」にする

1 巡目の是正（上の追記 1 の 3 点目「発注済みで未約定の決済を差し引く」）が**新たな売り過ぎと取りこぼしを作っていた**。
2 巡目の監査が再現テストで実測した。**挙動を直した。決定 1〜9 の方針は変えない。**

1. **S0 の数量を二重に引いていた（B1・損切りが黙って止まる）**: S0 の逆指値レグは本番では必ず
   `PositionEffect=Close` / `Accepted` の発注記録として残る。配分は「手法の異なる Active 行の数量」で既に
   S0 を引いており、そこへ未約定の決済としてもう一度引いていた。**S0 と S1 が同数なら S1 の決済が 1 株も出ず、
   無期限に据え置かれる**（Critical も出ない）。→ 差し引きの母集合から **Active な S0 行の `StopDecisionId` を除く**。
   - 受け入れ基準 8 のテストが緑だったのは、**本番に必ず在る S0 のレグ記録を置いていなかった**ためである。
     テストの配置を本番と同じにした（`BrokerStop` ヘルパ）。**存在しない状態を固定したテストは防御にならない。**
2. **即時約定した決済を差し引けていなかった（B2・売り過ぎ）**: ガードは 1 巡回に 1 回しか建玉を照会しない。
   先の行の決済が終端（`Filled`）で返ると、**古い建玉にも未約定一覧にも現れない**ため、後の行が同じ建玉を
   再配分された（実測: 保有 15 株に 10＋10＝20 株の決済＝反対建玉）。moomoo アダプタの `MapState` は
   `FilledAll → Filled` を返し得るので、「Submitted で返る」は観測であって保証ではない。
   → 差し引く単位を「未約定の数量」から **「建玉照会がまだ映していない数量」** へ改める。
   **建玉を照会した時刻**（照会の**前**に採る）をガードから配分へ渡し、その時刻より後に置かれた・動いた決済は
   **丸ごと未反映**とみなす。約定が建玉へ映るまでの遅れとして 30 秒の余裕を見込む。
3. **倒れ方は過大側（差し引き過ぎ）へ寄せる**。過大なら行が据え置かれ、次の巡回が新しい建玉を見て決済する。
   過小は反対建玉（空売り）を作り、取り返しがつかない。
   - ただし **拒否（`Rejected`）は 0 として扱う**——市場へ届いていない。差し引くと拒否のたびに持ち分が消え、
     決定 5-6 の再試行ができなくなる（テストが捕まえた）。
   - **反映済みの古い決済は引かない**。引くと持ち分が永久に足りず損切りできない（T-10-359 が固定）。
4. 決済の記録は非終端だけでは足りないため、`IExecutedOrderStore.FindClosesSince`（終端・非終端を問わず
   決済を**新しい順**に返す）を足した。新しい順に切るのは、打ち切りで**直近の決済**を落とさないため
   （落とすと売り過ぎ側へ倒れる）。

## ［2026-09-18 追記（3） / #820 の 3 巡目監査］保護を外す判定と、数量を決める判定を分ける

3 巡目の監査が、**保有が残っているのに保護が黙って外れる**配置を 2 つ再現した。**挙動を直した。決定 1〜9 の方針は変えない。**

1. **決定 6 の適用範囲を狭める（B3）**: 手法間の差し引き（`RemainingPositionFor`）は**対称**であり、S0 行と S1 行が
   同数を主張すると（純額 10 に対し双方 10）**どちらから見ても残 0** になる。S0 の逆指値が約定した直後や、
   S0 分を手で決済した直後がまさにこの形で、S0 側は正しく役目を終える一方、**S1 側まで同じ理由で完了**していた。
   結果は**保護ゼロの建玉**であり、イベントも Critical も出ない（ガードの完了は何も発行しない）。
   → **保護を外す判定（S1 行の完了）には差し引きを使わず、方向の純額だけを見る。**
   差し引きは「いくつ決済してよいか」（数量の上限）にだけ使う。過小なら据え置き、過大なら反対建玉になる。
   - **S0 側の取消・完了は従来どおり**（#826 項目 3）。決済済み建玉に残る逆指値は反対建玉を生むため正しい。
     狙いは「**どちらか一方が外れても、もう一方が建玉を覆い続ける**」ことである。
   - 既存の受け入れ基準 8 のテストが S0=5 / S1=10 という**非対称な数量**しか置いておらず、同数で破れることを
     見落としていた。境界は**同数**にある。
2. **除外するレグを「その S0 行の全試行」にする（B4・追記（2） 1 の再来）**: ガードが失効した逆指値を再発注すると、
   行の `StopDecisionId` は新しい試行へ進むが、**前試行のレグ記録は `Accepted` のまま残る**（取り消した注文が
   当日一覧から消えるブローカーでは約定追跡が終端化できない）。現在の試行だけを除外していたため、古い記録で
   S0 の数量を再び二重に引き、**S1 の決済が 1 株も出ないまま無音で据え置かれた**。
   レグの DecisionId はエントリー ID と試行番号から決定的に導けるので、**行の全試行を除外集合へ入れる**。

## ［2026-09-18 追記（4） / #820 の 4 巡目監査］決定 5-2 を配分方式から**残保護数量方式**へ改める

4 巡目の監査がブロッキング 4 件を出した。**うち BLK-1 は 3 巡目の是正（追記(3)）が作った回帰**である
（S1 の完了判定から差し引きを外した結果、自分の建玉を失った S1 行が不死化し、同じ銘柄の別エントリーの
**生きた S0 逆指値を毎巡回・恒久的に黙って取り消す**）。

**個々のバグではなく設計の問題であると判断し、持ち分の決め方ごと作り直す。決定 1〜4・7〜9 は変えない。
決定 5-2（決済数量の決め方）と決定 6（ガード）を本追記で改める。**

### 根本原因

ブローカーの建玉照会は**銘柄単位の純額**であり、どの建玉がどの保護記録のものかを区別しない。
それを**毎巡回ゼロから計算し直して**持ち分を決めていたため、規則をどう変えても
「売り過ぎ（反対建玉）」か「損切りが黙って出ない」のどちらかへ倒れた。

| 巡 | 是正 | その是正が作った欠陥 |
| --- | --- | --- |
| 1 | 行ごとの「建玉残」上限をやめ、行へ配分する（追記(1)） | B1（S0 のレグ記録で二重差し引き → S0 と同数なら決済が 1 株も出ない）・B2（即時約定した決済を取りこぼして売り過ぎ） |
| 2 | 差し引きを「建玉照会が映していない決済」へ（追記(2)） | B3（差し引きが対称で、S0 と S1 が同数だと双方から見て残 0 → 建玉が残ったまま保護が外れる）・B4（再発注済み S0 の前試行レグが除外漏れ） |
| 3 | 保護を外す判定を方向の純額だけに（追記(3)） | BLK-1（幽霊行が生きた S0 の逆指値を消す） |

**毎巡回引き直す限り、規則の調整では止まらない**——4 巡の実測がそれを示している。

### 改めた決定

1. **決定 5-2'（持ち分は状態である）**: `protective_stop_orders` に `RemainingProtected`（int?・null＝未確定）と
   `StalledNotifiedAt`（DateTimeOffset?）を足す（migration `AddProtectiveStopRemainingProtected`。**既存行は `Quantity`**）。
   値を動かすのは次の 3 つだけである。
   1. **確定**: エントリーの発注記録が**終端**になった時点で、残保護数量＝**エントリーの約定数量**。
   2. **自分の決済で減算**: 受理された決済の数量だけ減らす（決定的な `SoftwareCloseDecisionId` の試行と 1:1。
      試行番号を進める保存と同じ 1 回で減らすため、再入しても二度減らない）。
   3. **外部要因の減少の一度きりの割り当て**: 主張の合計が方向の純額を超えていれば、超過分を
      **作成時刻 → EntryDecisionId** の順に**古い行から**削り、**保存する**（＝削ったことの記録）。
      次の巡回は保存された値から始まるため、**持ち分が巡回ごとに揺れることが構造的に無い**。
2. **決済数量は残保護数量そのもの**。配分（`AllocateSoftwareStops`）と「建玉照会がまだ映していない決済」の
   差し引き（`UnreflectedCloseQuantity` / `IExecutedOrderStore.FindClosesSince` / `SnapshotLagAllowance` /
   ガードから渡していた照会時刻）は**撤去した**。**持ち分の計算に発注記録を一切使わない**ため、
   完了済み S0 行の取消済みレグで持ち分が食われる経路（BLK-2）が構造的に消える。
3. **行を完了させるのは残保護数量が 0 になったときだけ**（決定 6 の改定）。純額や他手法の主張では完了させない
   ——B3（建玉が残っているのに保護が外れる）と BLK-1（幽霊行の不死化）の両方がここで止まる。
   **部分的にしか決済できていない行も完了させない**（BLK-3）。残りは次の巡回・次の到達で決済する。
4. **ガードの評価順は S0 → 到達済み S1 → 未到達 S1**。
   - S0 を先にするのは、S1 の予算が「**この巡回の後も生きている S0**」の数量を引くべきだからである
     （約定・取消で役目を終えた S0 の数量を引くと B3 が再発する）。
   - 到達済みを先にするのは、未到達の行に持ち分を先取りされて**到達した損切りが出ない**のを防ぐため（BLK-4）。
5. **S0 行は「全部か 0 か」でしか削らない。** S0 の主張は**ブローカーに実在する逆指値の数量**であり、
   帳簿だけを部分的に削っても注文は縮まない——縮まない注文が建玉より大きいまま残ると、発火して反対建玉を作る。
   全部削られた S0 行はガードが**逆指値そのものを取り消して**完了させるので、帳簿と注文が一致する（#826 項目 3）。
6. **S1 の主張が 1 株も無い群には割り当てを行わない。** 割り当ての目的は S1 の持ち分を確定させることであり、
   S0 だけの群（実弾・S0 のみ）では従来の判定がそのまま働く。**S1 が無い構成で S0 の挙動は変わらない。**
7. **決定 9 の追加（無音の失敗を残さない）**: 到達済みなのに決済できない状態が猶予
   （`SoftwareStopExecutor.DefaultSettlementGrace`＝15 分）を過ぎたら、`SoftwareStopOutcome.CloseStalled=4`（Critical）を
   **1 行につき 1 回**出す（`StalledNotifiedAt` に記録）。据え置き自体は正しい fail-safe だが、無期限に黙って続くと
   「損切りが出ていない」ことに誰も気づかない。行は Active のまま再試行を続ける。

### 方針と合わなかった点（記録）

- 4 巡目の指示は「保護記録の残保護数量の合計とブローカーの純額を比べ、超過分を古い行から削る」であった。
  **S0 行を部分的に削る**と、ブローカーに実在する逆指値が帳簿より大きいまま残って反対建玉を作るため、
  S0 は「全部か 0 か」に限った（上の 5）。また **S1 が 1 件も無い群では割り当てを行わない**ことにした（上の 6）
  ——削ると「同一銘柄に複数の S0 が併存し純額がそれを賄えない」既存の近似の挙動が変わり、
  「S1 が無い構成では従来と同一」が破れるためである（既存テスト `一件の評価が例外でも残りの件は処理し失敗として数える` で実測）。

### 残る制約

- ブローカーの純額は建玉の出自を区別しないため、**どの記録の建玉が外部で消えたかは原理的に分からない**。
  本方式は「古い行から削る」という**決定的な規則で一度だけ**決め、記録する。誤って割り当てても
  合計は純額を超えないため**反対建玉は作らない**（倒れ方は「据え置き／完了」側である）。
  - 🔴 **［2026-09-18 追記（5） / #820 の 5 巡目監査］この一文の後半は誤りであり、監査が実証した。**
    「倒れ方は据え置き／完了側である」は**成り立たない**。誤って割り当てた先が S0 の行だと、その行の主張は 0 になり、
    ガードが**ブローカーに実在する生きた逆指値を取り消して**完了させる——**無音かつ不可逆な破壊**である
    （監査は T-10-373 の作成時刻を入れ替えただけで再現した）。**前半（合計が純額を超えないため反対建玉は作らない）は正しい**
    ことも併せて実測されている。是正は下の追記（5）決定 1〜3。
- S2 は保護記録を持たないため、依然として按分に入らない（決定 6 の残余リスクのまま）。

## ［2026-09-18 追記（5） / #820 の 5 巡目監査］割り当てを「帳簿だけの行から」にし、削りの確定に 2 巡回の観測と通知を挟む

5 巡目の監査は**売り過ぎ（反対建玉）を再現できなかった**——B1〜B4・BLK-2〜4 は追記(4) の作り直しで構造的に消えている。
残ったブロッキング 2 件はいずれも**外部要因の割り当て**（`ProtectiveStopNetting.ReconcileShares`）に起因する。
**挙動を直した。決定 1〜4・7〜9 と追記(4) の「持ち分は状態である」方式は変えない。**

### 改めた決定

1. **割り当ての順序を「帳簿だけの行（S1）→ 実注文を持つ行（S0）」にする**（追記(4) 決定 1-3 の「作成時刻 → EntryDecisionId」を改める。
   各クラスの中ではこれまでどおり作成時刻 → EntryDecisionId）。
   **BLK-1 の再発**である——順序が作成時刻だけだと、古い方がたまたま S0 のときに**生きた逆指値が 0 にされて取り消される**。
   S0 の行に注文照会が `Pending` を返していること自体が「その建玉はまだ在る」証拠であり、**S0 を削る前に S1 側を使い切る**。
   **「S0 は全部か 0 か」（追記(4) 決定 5）は維持する**（帳簿だけ削ってもブローカーの注文数量は縮まない）。
2. **削りは「未確定」として持ち、2 巡回連続で観測してから確定する**（`PendingExternalReduction` /
   `ExternalReductionObservations` を `protective_stop_orders` へ追加。migration `AddProtectiveStopExternalReduction`・既存行は 0）。
   **作り直しによる退行**への是正である——`ReconcileShares` は超過を削るだけで復元経路が無く、
   照会が**1 巡回だけ過少に返る**とその巡回で 0 になった行が完了し、**次の巡回で回復しても戻らなかった**
   （監査の実測: 建玉 20 株のうち 10 株が永久に無保護・しかも無音）。
   - **数量の減算そのものは観測した巡回で直ちに行う。** 遅らせると、同じ巡回の別の行が古い建玉を再び主張して
     **売り過ぎ**へ倒れる（T-10-353 が実測で固定している）。
   - **未確定のあいだは、行を完了させず・S0 の逆指値も取り消さない**（不可逆な側だけを待たせる）。
   - **建玉が戻って余剰が出れば、未確定の削りを復元する**（新しい行から返す。S0 は「全部か 0 か」）。
     **確定済みの削りは戻さない**——「一度だけ確定的に割り当てる」性質（持ち分が巡回ごとに揺れない）を壊さないため。
   - **観測を数えるのはガードの巡回の先頭の 1 回だけ**である（群につき 1 巡回 1 回）。決済経路
     （`SoftwareStopExecutor`）からの `ReconcileShares` は数量の割り当てだけを行う。
   - `null`（照会不能）は従来どおり据え置きで、**観測とみなさない**。
3. **確定したら必ず 1 回通知する**（無音の不可逆動作をやめる）。`SoftwareStopOutcome.ProtectionReduced=5` を末尾へ追加し、
   `SoftwareStopExecuted`（通知は **Warning**・監査へ要約を残す）を確定時に 1 行 1 回発行する。
   **S0 の行を 0 にして逆指値を取り消す場合、この記録が唯一の痕跡になる。**

### 方針と合わなかった点（記録）

- **既存の受け入れ基準 8 のテスト 2 件（`S0の建玉残はソフトウェア逆指値の約定数量を差し引いて判定する`・T-10-369）は
  期待値が反転した。** どちらも「S0 の建玉だけが外部で消え、S1 の建玉が残っている」配置だが、
  **「S1 の建玉だけが消えた」配置とブローカーの純額からは区別できない同一の観測**である
  （5 巡目の監査は、まさにその形の作成時刻を入れ替えて BLK-1 を再現した）。
  決定 1 は「**不可逆な破壊（生きた注文の取消）よりも、可逆で通知つきの帳簿の削りへ倒す**」という選択である。
  - 🔴 **［2026-09-18 追記（6） / #820 の 6 巡目監査］「合計の主張は純額を超えないので反対建玉は依然として作らない」は誤りであり、
    監査が実証した。** この言い方は **Active 行の主張しか数えておらず、送信済みで建玉照会にまだ反映されていない決済を
    数えていなかった**——決済を送って残保護数量が 0 になった行はその巡回で `Completed` になり `FindActive` から消えるが、
    その決済が受理・未約定のあいだ建玉照会はまだ減っていない。正しい不変条件は
    **「Active 行の主張の合計 ＋ 送信済みで建玉照会に未反映の決済 ≦ 方向の純額」**であり、
    これを保つ実装は `ProtectiveStopNetting.Reduce`（超過は必ず削る）と復元の門
    `ProtectiveStopNetting.AlreadyHandledShares`（追記(6) で新設）である。是正は下の追記（6）。
  #826 項目 3（建玉なき逆指値を残さない）は、**S1 が超過を吸収しきれない構成**（建玉が丸ごと消えた・S1 の主張が無い）では
  従来どおり働く（T-10-383 で固定）。2 件のテストはこの意味へ書き直した。
- 5 巡目の指示は「**1 回目は「超過を観測した」ことだけを行に記録し**、次の観測でも超過が続いていたら削る」であった。
  **減算まで遅らせると売り過ぎへ倒れる**ため（上の 2 の 1 点目）、**減算は即時・確定（と完了・取消・通知）だけを 2 巡回目へ**とした。
  「無音の不可逆動作をやめる」という目的は達している。

### 残る制約

- 2 巡回の確認は**ガードの巡回間隔ぶん**だけ完了・取消を遅らせる。その間、行は Active のまま保護を主張し続ける
  （建玉が本当に消えていれば余分な主張だが、**上の不変条件を保つ限り**売り過ぎにはならない）。
- 確定済みの削りは復元しない。**確定後に建玉が戻る**（照会が 2 巡回連続で過少だった）場合は主張が戻らない。
  この場合も通知は出ているため、無音では終わらない。
- 🔴 **［2026-09-18 追記（6） / #820 の 6 巡目監査］損切り価格が意図と違う値になる近似が残る。**
  T-10-369 の新しい形（S0 の逆指値が生きていて、超過を S1 の行が吸収して完了する）では、
  残った建玉を覆うのは **S0 の逆指値**であり、**その発動価格は S1 の行が意図した損切りラインとは異なる**。
  帳簿の上では建玉は保護されているが、**どの価格で損切りされるか**は入れ替わる。
  ブローカーの純額が建玉の出自を区別しない以上、どちらの建玉が消えたかは原理的に分からないため、
  「不可逆な破壊（生きた注文の取消）を避ける」側を採った帰結である（決定 1 の代償）。

## ［2026-09-18 追記（6） / #820 の 6 巡目監査］復元の根拠を「Active 行の主張の合計」から「既に手当て済みの株数」へ広げる

6 巡目の監査がブロッキング **1 件（現れ方 2 つ）**を出した。いずれも追記(5) 決定 2 で新設した**復元経路**
（`ProtectiveStopNetting.Restore`）に起因する——**復元が売り過ぎ（反対建玉）を再導入していた**。
**挙動を直した。決定 1〜4・7〜9 と追記(4) の「持ち分は状態である」方式、追記(5) の削り順・2 巡回の確認は変えない。**

### 現れ方

1. **空売りになる**: 同一銘柄・同方向の到達済み S1 行が 2 件あり、外部要因で建玉が半分になる。
   巡回 1 で `Reduce` が古い行 A を 0 に削り（未確定）、行 B が 10 株の成行決済を**受理・未約定**で出して
   `Settle` の `remaining == 0` で `Completed` になる。巡回 2 の建玉照会は**まだ 10 株を返す**（当然である）ので
   `excess = -10` となり、`Restore` が A へ 10 株を返して A がさらに 10 株を決済する。
   **2 巡回で合計 20 株**＝保有 10 株に対して 10 株の空売り。監査の実測:
   `Expected total to be less than or equal to 10 ..., but found 20 (difference of 10)`。
2. **到達していない建玉を売る**: 幽霊行が未確定のあいだに、同じ銘柄・同方向で**新しいエントリーが約定**し、
   その発注記録がまだ終端でない（`RemainingProtected` が `null` ＝主張 0）と、同じく `excess < 0` になって
   `Restore` が幽霊行を復活させ、**新しい建玉をその行の損切りラインとは無関係に成行決済**する。

### 根本原因

`Restore` は「純額 > **Active 行の主張の合計**」を過少報告の証拠とみなしていた。
**送信済みで建玉に未反映の決済**と**確定前の新規エントリーの建玉**を数えていないため、その 2 つがそのまま
「戻ってきた建玉」に見える。追記(4) の作り直しで「決済を送った行はその巡回で完了する」ようにしたことと、
追記(5) で「復元する」ようにしたことの**組み合わせ**が作った欠陥である。

### 改めた決定

1. **復元の根拠を「保護対象として既に手当て済みの株数の合計」へ広げる**（`ProtectiveStopNetting.AlreadyHandledShares`）。
   復元の判定でだけ、純額から次を差し引き、差し引いた結果でもなお純額が上回るときだけ復元する。
   1. **送信済みで建玉照会にまだ反映されていない決済**。決済レグの DecisionId は
      `ProtectiveStopIds.SoftwareCloseDecisionId(entry, attempt)` から**決定的に導ける**ので、S1 の行
      （群の Active ＋ 同じ銘柄・方向の**完了済み**。`IProtectiveStopOrderStore.FindCompletedSoftwareStops` を新設）の
      試行 1..`Attempt` を引き当て、非終端のレグの未約定数量（`Quantity − FilledQuantity`）を合計する。
   2. **確定前の新規エントリーの建玉**（群のうち `RemainingProtected` が `null` の S1 行）。
      まだ主張になっていない建玉であり、**承認数量**（`Quantity`）＝取り得る上限で見積もる。
   - **S0 の逆指値レグは数えない。** 滞留中の逆指値は「送信済みの決済」ではなく**まだ約定していない保護注文**であり、
     その株数は S0 行の主張が既に覆っている（数えると二重に引き、正当な復元まで止まる）。
   - 🔴 **追記(4) で撤去した「毎巡回の引き直し」を復活させるものではない**（`UnreflectedCloseQuantity` /
     `IExecutedOrderStore.FindClosesSince` / `SnapshotLagAllowance` / 照会時刻は戻さない）。
     **持ち分（`RemainingProtected`）の計算には発注記録を一切使わない**——この集計は**復元してよいかの門**にだけ使う。
   - **迷ったら復元しない側へ倒す。** 復元しなければ「保護が足りない」方向（`ProtectionReduced` の通知つきの縮退）で
     済むが、復元し過ぎると**反対建玉**という不可逆な事故になる。
2. **不変条件の訂正**（追記(5) の「合計の主張は純額を超えないので反対建玉は作らない」を改める）。正しくは

   > **Active 行の主張の合計 ＋ 送信済みで建玉照会に未反映の決済 ≦ 方向の純額**

   これを保つ実装は `ProtectiveStopNetting.Reduce`（超過は必ず削る）と、上の 1 の復元の門
   （`ProtectiveStopNetting.AlreadyHandledShares`）である。`ProtectiveStopNetting` のクラス注記にも同じ形で置いた。
3. **`Reduce` は増分を積んだ巡回で観測カウンタを 0 へ戻す**（監査の非ブロッキング指摘）。
   既に未確定の削りを抱えた行へ積み増すと、その増分は **1 回しか観測していない**のに行の観測回数が閾値に達して
   まとめて確定・通知されていた（追記(5) 決定 2 の「2 巡回連続で観測してから確定する」が増分について破れる）。
   初回の削りでは元から 0 なので挙動は変わらない。
4. **`Confirm` の「超過が今も続いているか」の再検証は行わない——文言の側を実装に合わせる**（同・非ブロッキング指摘）。
   減算は観測した巡回で即時に行うため（追記(5) 決定 2）、確定の時点では「超過」という状態そのものが残っていない。
   再検証にあたる働きは**復元経路（`Restore`）が担う**（建玉が戻れば未確定の削りを返す）。
   実装を文言に合わせて `Confirm` へ再検証を足すと、同じ判断を 2 箇所で持つことになり、
   復元の門と食い違ったときにどちらが正かが決まらない。

### 方針と合わなかった点（記録）

- 6 巡目の指示は「受理済み・未約定の決済レグ」を差し引くことであった。**滞留中の S0 の逆指値レグ**も同じ形
  （`PositionEffect.Close`・`Accepted`・未約定）で発注記録に残るが、**これは数えない**。
  S0 の逆指値は「送信済みの決済」ではなく**まだ約定していない保護注文**であり、その株数は S0 行の主張が
  既に覆っているため、数えると二重に引いて**正当な復元まで止まる**（S0 と S1 が併存する構成で
  追記(5) 決定 2 の復元が効かなくなる）。そのため対象を**ソフトウェア逆指値の決済レグに限り**、
  そのために完了済み S1 行を引く store メソッドを 1 つ足した。

### 残る制約

- **確定前の新規エントリーの建玉は承認数量で多めに見積もる。** 一部しか約定していない場合は実際より多く差し引き、
  **正当な復元がその巡回で止まる**ことがある（次の巡回でエントリーが終端になれば主張が確定し、判定は正確になる）。
- **完了済み S1 行の走査には上限（`ProtectiveStopNetting.RestoreScanLimit`＝50）がある。** 上限を超えて完了済みの
  行が積み上がった銘柄では、古い行の未反映レグを数え落とし得る。更新が新しい順に引くため、直前の巡回で
  完了した行が先に入る（上の現れ方 1 の配置は上限に依らない）。
- 送信した決済が**終端（`Filled`）になった直後**に建玉照会がまだ追いついていない窓は、依然として差し引けない
  （レグは非終端でなくなるため数えない）。この窓は追記(2) が照会時刻で扱おうとして追記(4) で撤去した領域であり、
  **時刻に依る引き直しは復活させない**。倒れ方は「復元し過ぎ」側になり得るため、ここは残余リスクとして記録する
  ——現れ方 1（受理・未約定）とは違い、約定していれば次の照会で純額が下がるため窓は短い。

## ［2026-09-19 追記（7） / #820 の 7 巡目監査］復元という操作そのものを撤去し、「確定するまで帳簿を書かない」へ改める

7 巡目の監査がブロッキング **2 件**を出した。いずれも追記(5) で新設し追記(6) で門を足した**復元経路**
（`ProtectiveStopNetting.Restore`）に起因する。**復元という操作を無くした。
決定 1〜4・7〜9 と追記(4) の「持ち分は状態である」方式、追記(5) の削り順（帳簿だけの行から）は変えない。**

### 現れ方

1. **BLK-7-1（追記(6) の是正が作った回帰）**: `AlreadyHandledShares` が、確定前の S1 行の**承認数量をそのまま**
   （1 株も約定していなくても上限で）復元の予算から差し引いていた。**同じ銘柄に確定前の新規エントリーが
   1 件あるだけで**「1 巡回だけ過少照会 → 復元」が効かなくなり、建玉が実在するのに行が `Completed` になる。
   監査の用量反応（実測）:

   ```
   新規エントリー承認数量=0   → A.RemainingProtected=10 A.State=Active     ← 対照（正しい復元）
   新規エントリー承認数量=10  → A.RemainingProtected=0  A.State=Completed  ← 建玉 10 株が無保護
   ```

2. **BLK-7-2（追記(6) の是正が不完全）**: **S1 の記録を持たない建玉**（S2 の建玉・S0 の発注窓・人手で建てた建玉）が
   同じ銘柄・方向に現れると、`Restore` がそれを「戻ってきた自分の建玉」と読んで幽霊行を復活させ、
   到達済みならその建玉を**無関係な損切りラインで成行決済する**。監査の実測:

   ```
   失敗 P2_S1以外の建玉で幽霊行が復活して売り過ぎる
    Expected f.Broker.MarketCloses to be empty …, but found { Quantity = 10, Side = Sell, Price = 940 }
   ```

   **到達可能性は高い。** S2 は保護記録を作らず、本 PR の反映手順は**稼働中の S2 から S1 へ切り替える**ことが
   前提であり、切替時点で S2 の建玉が同一銘柄に残り得る。

### 根本原因

**建玉照会の純額からは「自分の建玉が戻った」と「他人の建玉が現れた」を区別できない。**
戻す操作がある限り、この区別を毎回誤る。**7 巡のうち 3 巡（5・6・7）が「削る／戻す」の往復で潰れた。**

- 追記(5): 即時に削り、**復元**を足した → 復元が売り過ぎを再導入（6 巡目 BLK）。
- 追記(6): 復元に**門**（`AlreadyHandledShares`）を足した → 門が厳しすぎて復元が効かない（BLK-7-1）／
  門が拾えない相手（S1 の記録を持たない建玉）で復活する（BLK-7-2）。

門を足すほど「復元し過ぎ」と「復元しなさ過ぎ」の間で振れる。**振れの原因は復元そのものである。**

### 改めた決定

1. **外部要因による減少は、2 巡回連続で観測してから初めて `RemainingProtected` へ書き込む**（確定）。
   1 回目の観測では**永続する帳簿を一切変更しない**。観測の連続回数（`ExternalReductionObservations`）と、
   観測した超過量（`PendingExternalReduction`）だけを記録する。
   🔴 **`PendingExternalReduction` の意味を「既に削った株数」から「観測したが、まだ帳簿へ書いていない超過量」へ改める**
   （列は追加しない。migration `AddProtectiveStopExternalReduction` の注記へ日付つき追記で残した）。
2. **同じ巡回の中では、削った後の値で数量を決める**——`ProtectiveStopOrder.EffectiveProtectedQuantity`
   （＝ `ProtectedQuantity − PendingExternalReduction`）。決済数量の上限（`SoftwareStopExecutor`）と
   S0 の建玉残の判定（`RemainingPositionFor`）はこの値で行う。
   🔴 **これは一時的な計算であって永続化しない。** 追記(5) が「減算まで遅らせると同じ巡回の別の行が古い建玉を
   主張して売り過ぎる」として即時減算を選んだ問題は、これで塞がる（T-10-353 が実測で固定している）。
3. **超過が消えたら観測の連続回数を 0 に戻すだけでよい。何も書き戻さない＝復元が要らない。**
   観測値は**単調**であり、**確定か行の完了でしか消えない**（`ProtectiveStopNetting.Confirm` と
   `SoftwareStopExecutor.Complete` / `Settle`）。これで BLK-7-1（復元が効かない）と
   BLK-7-2（幽霊行の復活）が**両方とも構造的に消える**——戻す判断を一度も行わないためである。
4. **確定したら従来どおり `SoftwareStopExecuted`（`ProtectionReduced`）を 1 行 1 回発行する**（無音にしない）。
   確定によって 0 になった行だけをガードが完了させる。
5. `null`（照会不能）は観測とみなさない（従来どおり据え置き）。
6. **`Restore` と `RestoreScanLimit` は撤去した。** `AlreadyHandledShares` は
   **`SharesAccountedElsewhere` へ改名し、役割を「復元してよいかの門」から
   「観測を数え続けてよいかの門」へ変えて残す**（次節）。`IProtectiveStopOrderStore.FindCompletedSoftwareStops` も残す。

### `AlreadyHandledShares` / `FindCompletedSoftwareStops` を消さずに残した理由

7 巡目の是正方針は「復元が無くなれば不要になるはず」だった。**実際には不要にならなかった。**
超過が見かけ上消えた巡回で「本当に建玉が戻ったのか」を分けるのに、次の 2 つがまだ要る。

- **送信済みで建玉照会に未反映の決済**: 決済を送って残保護数量が 0 になった行はその巡回で `Completed` になり
  `FindActive` から消えるが、その決済が受理・未約定のあいだ建玉照会はまだ減っていない。
  これを数えないと幽霊行の観測が**いつまでも確定せず、帳簿の主張を持ち続ける**（T-10-384 が実測）。
  完了済み S1 行を引くために `FindCompletedSoftwareStops` が要る。
- **確定前の新規エントリーの建玉**: 新しいエントリーの約定分が純額に入ると超過が見かけ上消える。
  これを数えないと幽霊行の観測が確定しない（T-10-385 が実測）。
  🔴 **ただし承認数量ではなく約定数量（`ExecutionRecord.FilledQuantity`）で数える**——これが BLK-7-1 の是正である。

**役割が変わったことで倒れ方も変わった。** 門が誤っても結果は「その行はしばらく動けない（帳簿の主張は残る）」側であり、
**復元と違って反対建玉という不可逆な事故へは倒れない。** 追記(4) で撤去した「毎巡回の引き直し」は復活させていない
——`RemainingProtected` の計算に発注記録は使わず、この集計は**門にだけ**使う。

### 訂正（誤りだった記述）

- 🔴 追記(6) 決定 1-2 の「**確定前の新規エントリーの建玉**（群のうち `RemainingProtected` が `null` の S1 行）。
  まだ主張になっていない建玉であり、**承認数量**（`Quantity`）＝取り得る上限で見積もる」は**誤りであり、
  監査が BLK-7-1 として実証した。** 1 株も約定していない新規エントリーが復元を止めてしまう。
  正しくは**その発注記録の約定数量**で数える。
- 🔴 追記(5) 決定 2 の「**数量の減算そのものは観測した巡回で直ちに行う**」「建玉が戻って余剰が出れば、
  **未確定の削りを復元する**（新しい行から返す。S0 は「全部か 0 か」）」「**確定済みの削りは戻さない**」は、
  **帳簿を確定まで書かないことにしたため成り立たない。撤回する。** 同じ巡回で売り過ぎないための働きは、
  上の決定 2（`EffectiveProtectedQuantity`）が引き継いだ。
- 🔴 追記(6) 決定 4 の「再検証にあたる働きは**復元経路（`Restore`）が担う**」は、`Restore` の撤去により無効である。
  再検証にあたる働きは**毎巡回の観測の突き合わせそのもの**（観測値と今回の割り当てが一致するか）が担う。
- 🔴 追記(6) の残る制約「**完了済み S1 行の走査には上限（`ProtectiveStopNetting.RestoreScanLimit`＝50）がある**」の
  名称は `SentCloseScanLimit` である（値 50・意味は同じ）。
- 追記(5) の「**復元経路がある（可逆）**」を根拠にした説明（生きた注文の取消より帳簿の削りを選ぶ理由）は、
  **可逆性ではなく「2 巡回の確認と通知を経る」ことを根拠に読み替える**。判断（S1 を先に削る）は変えない。

### 残る制約

- 🔴 **観測値は単調で、確定か完了でしか消えない。** 超過が確定しないまま消え続ける配置
  （例: S0 の逆指値が約定して S0 行の主張が消え、S1 行の観測だけが残る＝T-10-368 の配置）では、
  S1 行は `Active`・帳簿も無傷のまま、**その巡回で動かせる株数が 0** の状態が続く。
  **建玉を誤って売ることはない**が、その行の損切りは実質出ない。到達している行なら据え置きが続き、
  猶予 15 分で `CloseStalled`（Critical）が 1 回出るため**無音では終わらない**。
  この状態を解くには復元を戻すほか無く、復元は 3 巡にわたって不可逆な事故を作ったため**採らない**。
- **到達済みの行が「その巡回で動かせる株数」を売り切った場合は、その巡回で完了する**（`Settle`）。
  実際に成行決済を出した以上は行動が確定であるとみなす扱いで、**追記(4) からの挙動は変えていない**。
  照会が過少だった場合は帳簿の残りが無保護になるが、これは決済経路に元からある残余リスクである。
- 送信した決済が**終端（`Filled`）になった直後**に建玉照会がまだ追いついていない窓は、依然として数えられない
  （追記(6) と同じ。時刻に依る引き直しは復活させない）。
- 追記(6) の「損切り価格が意図と違う値になる近似」（T-10-369 の形）はそのまま残る。
## ［2026-09-19 追記（8） / #820 の 8 巡目監査］観測を「対称に」失効させ、S1 の武装に前提条件（帰属不明の建玉が無いこと）を置く

8 巡目の監査がブロッキング **2 件**を出した。いずれも追記(7) の「観測値は単調で、確定か完了でしか消えない」に起因する。
**決定 1〜4・7〜9、追記(4) の「持ち分は状態である」方式、追記(5) の削り順、追記(7) の「復元を持たない」は変えない。**

### 現れ方

1. **BLK-8-1（1 巡回だけの過少照会で、その行の損切りが二度と出なくなる・無音）**:
   `PendingExternalReduction` は確定か完了でしか消えない（単調）ため、超過が消えても残り続け、
   `EffectiveProtectedQuantity = ProtectedQuantity − PendingExternalReduction` が**恒久的に 0** になる。
   行は `Active`・帳簿（`RemainingProtected`）も無傷なのでどの検査も通るが、**到達しても 1 株も決済しない**。
   監査の実測:

   ```
   P1: 建玉 20 株・行 2 件、照会が 1 巡回だけ 10 株、以後 10 巡回ずっと 20 株
     → first: Active Remaining=10 Pending=10 Obs=0 Effective=0（永久に 0）
   P2: 同状態で到達 → 決済は 10 株だけ（建玉 20 株のうち 10 株に損切りが出ない）
   P13: T-10-368 の配置（S0 の逆指値が**正常に約定**した＝S0 の標準的な終わり方）
     → s1: Active Remaining=10 Pending=10 Effective=0 / closes=[]（10 株が無保護）
   ```

   🔴 **P13 が示すとおり、これは異常系ではなく S0 の標準的な終わり方で起きる。**
   追記(7) の「残る制約」はこの状態を「到達済みなら 15 分で `CloseStalled` が出るため無音ではない」と書いていたが、
   **未到達の行には効かない**（監査の実測: 20 巡回 100 分でイベント 0 件）。射程を実態より狭く書いていた（下の「訂正」）。

2. **BLK-8-2（他人の建玉が先に在ると、幽霊行がそれを自分の損切りラインで売る）**:
   追記(7) の復元撤去が塞いだのは「幽霊行が**先に**超過を観測した後で他人の建玉が現れる」経路（T-10-411）だけだった。
   **他人の建玉（S2・人手・S0 の発注窓）が先に在ると超過が一度も観測されず**、主張は満額のまま残る。
   監査の実測（**稼働中の S2 から S1 へ切り替えた直後そのものの時系列**）:

   ```
   1. 切替前からの S2 建玉 10 株（保護記録なし）
   2. 切替後の S1 エントリー 10 株が約定 → 純額 20（超過は出ない）
   3. S1 の建玉だけ人手で決済 → 純額 10（S2 のぶん）。超過は依然 0
   4. 到達 → closes=[10@940/Sell]  ← S2 の建玉を S1 のラインで売った
   ```

   追記(7) 自身が「到達可能性は高い。本 PR の反映手順は稼働中の S2 から S1 へ切り替えることが前提」と
   書いていながら、**その配置が未修正**だった。

### 根本原因

**追記(7) は「観測を増やす」方向だけを持ち、「観測を減らす」方向を持たなかった。**
確定（2 巡回連続の観測）は対称な相手（2 巡回連続の不在）を欠いており、一度積んだ観測が消えない。
BLK-8-2 は逆に、**武装の時点で純額の帰属が付いていない**ため観測がそもそも起こらない。
**どちらも「銘柄単位の純額からは自分の建玉と他人の建玉を区別できない」という同じ限界の裏表**である。

### 改めた決定

1. **観測を対称に失効させる（BLK-8-1）**。超過が `ExternalReductionConfirmations`（＝2）回**連続で消えた**ら、
   `PendingExternalReduction` を 0 に戻す（`ProtectiveStopNetting.Expire`。新しい列
   `ExternalReductionAbsences`＝migration `AddProtectiveStopSuspension`）。
   🔴 **これは「復元」ではない。** 帳簿（`RemainingProtected`）を書き戻さないので、6・7 巡目の復元問題
   （送信済み未反映の決済を「戻った建玉」と読む／他人の建玉で幽霊行が復活する）は生じない。
   観測が増える方向（真の追加減少）では従来どおり不在の連続回数を 0 へ戻す。
2. 🔴 **ただし到達済みの行では失効させない。** その行は失効した瞬間に成行決済を出すため、
   「戻ってきた自分の建玉」と「他人の建玉」を取り違えたときの倒れ方が**不可逆（反対建玉）**になる
   ——これは 7 巡目 BLK-7-2（T-10-411）そのものである。**方針との差異であり、実測で決めた**（下の「方針と合わなかった点」）。
   到達済みの行は据え置いたまま `CloseStalled`（到達からの猶予）と下の決定 3 で人手へ回す。
3. **実効数量 0 が猶予（15 分・`ProtectiveStopNetting.ProtectionSuspendedGrace`）を超えたら
   `SoftwareStopOutcome.ProtectionSuspended=6`（`SoftwareStopExecuted`・Critical）を 1 行 1 回出す（BLK-8-1 の無音対策）。**
   **到達の有無に依らない**——未到達の行には `CloseStalled`（到達からの猶予）が効かない。
   計時は新しい列 `ProtectionSuspendedSince` / `ProtectionSuspendedNotifiedAt`（同 migration）で持ち、
   実効数量が戻ったら両方を `null` へ戻す（再発したら改めて数え直す）。
   猶予は孤立行（`DefaultOrphanGrace`）・据え置き（`DefaultSettlementGrace`）と**同型の 15 分**にする。
4. **S1 の武装に前提条件を置く（BLK-8-2）。** `OrderExecutionAppService` は S1 で新規建てを武装する時点で、
   同一銘柄・同方向に**帰属不明の建玉**（＝方向の純額 − Active な保護記録の主張合計 > 0）があるなら、
   **エントリーを送らず建玉を持たずに見送る**（`OrderDispatchForgoneReason.UnattributedPosition=4`
   〔🔴 **2026-09-19 / 13 巡目のマージで序数は 4 → 6 へ繰り下げた**——#864 が 4・5 を先に取ったため。
   **先にマージされた側が確保する**（改番手順と同じ規律）。値は下の追記(12) が正本〕）。
   「保護レグを張れない Open では建玉を持たない」（IADR-0210 決定 1）と同じ倒し方である。
   - 🔴 **確かめられない場合も「ある」側へ倒す**（建玉照会の能力が無い発注先・照会が `null`）。
     「不明」を「無い」と取り違えると、他人の建玉を S1 のラインで売る不可逆な事故になる。
   - 判定は**初回の武装のときだけ**行う（再配送で既に行がある場合は触らない。決定 3 の冪等を壊さない）。
   - **既存の S0 / S2 / S3 の経路は 1 バイトも変えない**（分岐は `disposition == SoftwareStop` の内側だけ）。
   - 結果として「S2 の建玉が残ったまま S1 へ切り替える」構成では、**帰属不明の建玉が無くなるまで
     S1 の新規建てが出ない**。安全側であり、運用手順（先に手仕舞ってから切り替える）とも一致する。

### 方針と合わなかった点（記録）

🔴 **監査が示した「超過が N 巡回連続で消えたら観測を 0 に戻す」を、そのまま（到達済みも含めて）実装すると
7 巡目 BLK-7-2 が再発する。** 監査は「帳簿を書き戻さないので 6・7 巡目の復元問題は生じない」としていたが、
**決済数量の上限は帳簿ではなく `EffectiveProtectedQuantity` である**ため、観測を捨てれば到達済みの行はその場で売る。
実測（本 PR で一度そう実装して測った）:

```
$ dotnet test --filter "FullyQualifiedName~S1の記録を持たない建玉が現れても幽霊行は主張を取り戻さない"
  失敗 …S1の記録を持たない建玉が現れても幽霊行は主張を取り戻さない
  エラー メッセージ:
   Expected f.Broker.MarketCloses to be empty, but found {{ Item1 = OrderIntent { … Quantity = 10,
   Side = TradeSide.Sell {value: 1}, Price = 940 … } }}.
失敗!   -失敗:     1、合格:     0、スキップ:     0、合計:     1
```

そこで**失効を未到達の行に限った**（決定 2）。これは方針（「超過が 2 巡回連続で消えたら 0 に戻す」）を
**狭めた**ものであり、別方式へ倒したのではない。根拠は「失効で得るもの（未到達の行の保護が戻る）と
失うもの（到達済みの行が他人の建玉を売る）の非対称」である——未到達の行の失効は
**次の到達の時点で改めて純額と突き合わされる**のに対し、到達済みの行の失効は**その場で不可逆に注文を出す**。

### 訂正（誤りだった記述）

- 🔴 追記(7) の残る制約「超過が確定しないまま消え続ける配置では…**到達している行なら据え置きが続き、
  猶予 15 分で `CloseStalled`（Critical）が 1 回出るため無音では終わらない**」は、**射程が実態より狭かった**。
  監査が実測したとおり、**未到達の行では 20 巡回 100 分にわたりイベントが 1 件も出ない**（`NotifyIfStalled` は
  `TryCloseAsync` の Deferred 経路＝到達済みの行にしか作用しない）。さらに、その配置は
  「S0 が約定して S1 の観測だけが残る等」という例外的なものではなく、**T-10-368＝S0 の標準的な終わり方**である。
  決定 3（`ProtectionSuspended`）で埋めた。
- 🔴 追記(7) 決定 3 の「**超過が消えたら観測の連続回数を 0 に戻すだけでよい**」「観測値は単調であり、
  **確定か行の完了でしか消えない**」は、**そのままでは保護を恒久的に失わせる**。決定 1・2 で改めた
  （未到達の行では 2 巡回連続の不在で観測そのものを捨てる。到達済みの行では従来どおり単調のまま）。

### 残る制約

- **到達済みの行の観測は依然として単調である**（決定 2）。この状態に陥った行は、確定するか人手が介入するまで
  1 株も決済しない。**無音ではない**（`CloseStalled` と `ProtectionSuspended` がいずれも Critical で出る）が、
  **自動では回復しない**。これは「他人の建玉を売らない」ことと引き換えに選んだ倒れ方である。
- **武装の前提条件は「武装の時点」しか見ない**（決定 4）。武装より**後**に他人の建玉が同一銘柄・同方向へ現れる経路は、
  従来どおり観測（超過）として現れ、決定 2 により幽霊行が売ることはないが、その行の保護は止まる。
- **確定前（エントリーの約定がまだ終端でない）の S1 行は 1 株も主張しない**（追記(4)）ため、
  同一銘柄・同方向へ **S1 の新規建てを続けて出すと、2 本目が「帰属不明の建玉がある」と判定されて見送られ得る**
  （1 本目の約定がガードの巡回で確定するまでの窓）。**安全側の見送り**であり建玉は作らないが、
  同一銘柄の連続エントリーは取りこぼす。
- 追記(7) の他の残る制約（`SentCloseScanLimit`＝50 の走査上限、`Filled` 直後の建玉照会の窓、
  T-10-369 の損切り価格の近似）はそのまま残る。

## ［2026-09-19 追記（9） / #820 の 10 巡目監査］BLK-10-1 を**原理的な限界**として受容し、武装の判定を実効数量へ替え、帰属不明の建玉を毎巡回可観測にする

10 巡目の監査は**売り過ぎ（反対建玉）を乱数 6,000 列の掃きで 1 件も再現できなかった**。
過去 16 件のうち 15 件は再発しない。残ったブロッキング **BLK-10-1 は、規則を変えても直らない原理的な限界**である。
**決定 1〜4・7〜9、追記(4) の「持ち分は状態である」方式、追記(5) の削り順、追記(7) の「復元を持たない」、
追記(8) の「到達済みでは失効させない」は変えない。**

### BLK-10-1 — 何が区別できないのか

未到達の行が超過を観測し、失効（追記(8) 決定 1）で主張を取り戻したあと、次の到達で 10 株を売る。
この 10 株が**自分の建玉なのか他人の建玉なのかは、純額の系列からは決められない**。
次の 2 つは純額の系列が `[0, 10, 10]` で**完全に同一**である。

| 実際に起きたこと | あるべき挙動 | 根拠 |
| --- | --- | --- |
| **一過性の過少照会**（建玉照会が 1 巡回だけ 0 を返した） | 主張を**戻すべき** | 戻さないと、その行の損切りが二度と出ない（8 巡目 BLK-8-1） |
| **真の減少の後に、保護記録を持たない他人の建玉が現れた** | 主張を**戻してはいけない** | 戻すと、他人の建玉を自分の損切りラインで売る（7 巡目 BLK-7-2） |

9 巡目の「失効は未到達の行だけ」でも解けない。未到達の幽霊行が失効で主張を取り戻し、
**次の到達の時点では純額 10・主張 10 で超過 0 になるため、そのまま他人の 10 株を売る**（監査の P1）。

🔴 **どんな規則でも両方は満たせない。** 利用者の裁定は**案①「残余として明示的に受容する」**である
（案②＝ブローカーの注文一覧から「自分が出していない約定」を特定する、は **#863** として起票済み。本 IADR の射程外）。

### 改めた決定

1. **武装の判定を `EffectiveProtectedQuantity` に替える**（追記(8) 決定 4 の「Active な保護記録の主張合計」を改める）。
   帳簿の主張（`ProtectedQuantity`）で引いていたため、**未確定の観測を抱えた幽霊行が他人の建玉を「帰属済み」に見せ、
   新しい S1 が武装されていた**（監査の P6(1)。実測 SOLD=20）。実効数量で引けば幽霊行は 0 株しか主張せず、
   帰属不明が正しく見えて**安全側（見送り）へ倒れる**。
2. **武装の判定の前に `ProtectiveStopNetting.ConfirmEntryFills` を通す**。
   終端になったエントリーの約定数量がまだ帳簿へ書かれていない窓（追記(8) の残る制約 ③＝
   「同一銘柄への連続エントリーは 2 本目が見送られ得る」）では、直前のエントリーが実際には主張しているのに
   主張 0 と読まれていた。確定を先に通せば**ガードの巡回を待たずに主張が立つ**ので、この窓が消える。
   確定は建玉照会を要さない純粋な突き合わせであり、ガードが巡回の先頭で行っているのと同じ操作である。
3. **ガードが毎巡回「帰属不明の建玉」を検知して 1 回だけ知らせる**
   （`SoftwareStopOutcome.UnattributedPosition=7`・`SoftwareStopExecuted`・**Warning**）。
   判定は **方向の純額 − Active 行の主張の合計 − `SharesAccountedElsewhere`（送信済みで未反映の決済＋確定前エントリーの約定分）> 0**。
   - 🔴 **材料は武装の時点にしかなかったが、ガードは毎巡回これを持っている。** 1 本出すだけで、
     追記(8) の**残る制約 ②**（武装より後に他人の建玉が現れる経路）と、
     **受理後に 0 約定で取り消された決済の残り**（監査の P5。行は既に `Completed` で `FindActive` に現れず、
     どのイベントも出ないまま建玉が無保護で残る）が可観測になる。
   - **走査は建玉の側から行う**——P5 の配置には Active な行が 1 件も残らないため、Active 行の群を回すだけでは見えない。
     S1 の足跡がある群（Active ∪ `FindCompletedSoftwareStops`）に限る。
     **S1 が 1 件も無い群（実弾・S0 のみ）では 1 バイトも動かない。**
   - **同じ状態で毎巡回鳴らさない**: 群の S1 行のうち**作成が最も新しい 1 行**へ
     `UnattributedNotifiedQuantity` / `UnattributedNotifiedAt` を記録し（migration `AddProtectiveStopUnattributedNotice`）、
     **株数が変わったとき**か **`UnattributedRenotifyInterval`（60 分）**が経ったときだけ出す。
     帰属不明が消えたら両方を `null` へ戻す（再発したら改めて知らせる）。
   - 🔴 **通知は「検知」であって是正ではない。** 建玉を売らず・記録も作らず・主張も動かさない。
     BLK-10-1 を直すものではなく、**受容した残余が起きていることを人に見せる**ためだけに出す。
4. **`RecordObservation` は観測値を増やすときに `ExternalReductionAbsences` も 0 へ戻す**（監査の非ブロッキング 6）。
   決済経路が観測を積み増しても不在の連続回数が残っていたため、**真の追加減少の直後の 1 巡回で失効し得た**
   （確定と対称であるべき失効が、対称でなくなる）。`Confirm` 側は既に 0 へ戻していた。
5. **`NotifySuspended` は S1 の行にだけ効かせる**（監査の非ブロッキング 7）。
   S0 の行も `IsProtectionSuspended` になり得るが、S0 の保護は**ブローカーに実在する逆指値**であり、
   帳簿の主張が一時的に打ち消されても**建玉は現に守られている**——「ソフトウェア逆指値が 1 株も決済できない」という
   `ProtectionSuspended` の文面はその行に当たらない。S0 側の帰結は確定時の `ProtectionReduced`
   （＋ガードによる逆指値の取消）が既に 1 回残す。

### 受容した残余（BLK-10-1）

- **発生条件**: 未到達の S1 行が外部要因の減少を観測し、**その観測から次の観測までのあいだ（ガードの巡回 1 回ぶん・
  既定 30 秒。失効の確定までは約 2 巡回＝約 60 秒）**に、**保護記録を持たない建玉が同じ銘柄・同方向へ現れる**こと。
- **起こること**: 失効で主張が戻り、次の到達でその建玉を S1 の損切りラインで成行決済する（最大でその行の主張ぶん）。
- **起こらないこと**: 🔴 **売り過ぎ（反対建玉）にはならない。** 決済数量は常に
  `EffectiveProtectedQuantity`（≦ 方向の純額）で頭打ちであり、10 巡目の監査は乱数 6,000 列の掃きで 1 件も再現していない。
- **射程**: **SIMULATE 限定**である（実弾では `StopLossMethodPolicy` が S1 を拒否し、空売りは常に S0）。
  被害は「模擬口座の他人の建玉が意図しない価格で手仕舞われる」ことに留まる。
- **恒久対策**: **#863**（ブローカーの注文一覧から「自分が出していない約定」を特定する）。
  それが入れば「減ったのは誰の注文か」が分かり、推測が要らなくなる。
- **可観測性**: 決定 3 の `UnattributedPosition` が、この残余が起き得る状態（帰属不明の建玉の存在）を
  **到達より前に**知らせる。テストで挙動を固定した（T-10-483。**不具合の固定ではない**ことをテスト名と注記で明示した）。

### 🔴 S1 を選ぶときの運用上の前提条件

10 巡目の監査は「**次の 3 つが満たされるかぎり取引は成立する**」ことを実測した。
S1 へ切り替える前に確認する。

1. **切替の時点で、S2 由来の建玉（および人手で建てた建玉）を手仕舞っておく。**
   残っていると決定 1 の前提条件で S1 の新規建てが出ない（安全側）か、上の残余の入口になる。
2. **非終端の一部約定が無い。** エントリーが部分約定のまま終端していない行は主張が確定せず、
   その窓では帰属が付かない。
3. **受理後に取り消された決済の残りが無い**（監査の P5）。決済が受理された時点で行は完了するため、
   その決済が 0 約定で取り消されると建玉だけが無保護で残る。決定 3 の通知がこの状態を知らせる。

### 訂正（誤りだった記述）

- 🔴 追記(8)「方針と合わなかった点」の「未到達の行の失効は**次の到達の時点で改めて純額と突き合わされる**」は**誤り**である。
  次の到達でも `ReconcileShares` は通るが、**失効後は純額 10・主張 10 で超過 0** になるため割り当てが起きず、
  **そのまま 10 株を売る**（監査の P1 が実測）。突き合わせが「歯止めになる」という含意が成り立たない。
  失効を未到達に限った判断そのものは変えない（到達済みの失効は**その場で**売るのに対し、
  未到達の失効は**次の到達まで猶予がある**——歯止めではなく**時間差**が違いである）。
- 🔴 追記(8)「残る制約」の「武装より**後**に他人の建玉が同一銘柄・同方向へ現れる経路は、…
  決定 2 により**幽霊行が売ることはない**」は、**未到達の行では偽**である。
  未到達の行は追記(8) 決定 1 の失効で主張を取り戻し、次の到達で売る。**到達済みの行では真**（失効しないため）。
  上の「受容した残余」がこの経路そのものである。
- 追記(8) の残る制約「同一銘柄への連続エントリーは 2 本目が見送られ得る」は、**決定 2（確定を先に通す）で
  エントリーが終端になっている場合は解消した**。まだ終端でない（約定の記録が届いていない）場合は残る。

### 残る制約（追記(8) から更新）

- 🔴 **受容した残余（BLK-10-1）は上の「受容した残余」節が正本である。**
- 🔴 **帰属不明の検知は、Active な行が 1 件も無い巡回では走らない。** ガードは
  `stops.FindActive(batchSize)` が空なら建玉を照会せずに戻る（「巡回対象が無ければ建玉照会もしない」は
  無駄な OpenD 往復を避けるために**明示的に固定された不変条件**であり、既存テストが守っている）。
  そのため「受理後に取り消された決済の残り」が**その口座で唯一の S1 の痕跡**である場合、
  次にどこかの銘柄で S1 の行が Active になるまで検知が走らない。
  - **完全に無音ではない**——その銘柄へ次の S1 を武装しようとすると、武装の前提条件が
    `UnattributedPosition` で見送り（`OrderDispatchForgone`）を出す。
  - 🔴 **［2026-09-19 追記（10） / #820 の 11 巡目監査］この次の一文は誤りであった。**
    「直すには『対象ゼロでも建玉を照会する』へ変えるほかなく、**上の不変条件を壊す**ため本 IADR では採らない。
    #863 が入れば注文一覧から直接特定できるようになる」——**「ほかなく」が成り立たない。**
    同じサービスに `Hosted/BrokerPositionSnapshotService`（既定 600 秒・`GetPositionsAsync` を**無条件に**叩いて
    `BrokerPositionsObserved` を発行する）が既にあり、**そこへ相乗りすれば OpenD 往復を 1 回も増やさずに検知できる**。
    ガードの不変条件（巡回対象ゼロなら照会しない）も壊さない。**採らない理由は「できないから」ではなく
    「本 PR が 11 巡目であり、射程を広げると巡回がさらに増えるから」である**（追随は #880）。
- **到達済みの行の観測は依然として単調である**（追記(8) のまま）。
- 追記(8) の残る制約「同一銘柄への連続エントリーの 2 本目が見送られ得る」は、決定 2 により
  **エントリーが終端していれば解消**した。まだ終端でない（約定の記録が届いていない）場合は残る。
- 追記(7) の他の残る制約（`SentCloseScanLimit`＝50 の走査上限、`Filled` 直後の建玉照会の窓、
  T-10-369 の損切り価格の近似）はそのまま残る。**帰属不明の検知も同じ走査上限を共有する**
  （完了済み S1 行が 50 件を超えて積み上がった銘柄では、古い行の未反映レグを数え落として
  「帰属不明あり」へ倒れ得る——**倒れ方は「余計に知らせる」側**であり、売買は起こさない）。

## ［2026-09-19 追記（10） / #820 の 11 巡目監査］武装の判定の 2 文を入れ替え、検知の射程を利用者の文書へ正しく開示する

11 巡目の監査は**無作為 3,000 試行の掃きで超過売り 0 件・最大超過 0 株**を実測した。
ブロッキング 2 件のうち **1 件は追記(9) 決定 2 が作った競合**である。
**決定 1〜4・7〜9、追記(4)〜(9) の方式・受容した残余（BLK-10-1）は変えない。**

### 改めた決定

1. 🔴 **武装の判定で `ConfirmEntryFills` を `GetPositionsAsync` の「前」へ移す**（BLK-11-1。追記(9) 決定 2 の**順序**を改める）。
   - **何が壊れていたか**: 建玉照会は OpenD への RPC である。その待ちのあいだに `OrderFillPollingService` が
     先行エントリーの記録を終端化すると、**`claimed`（主張）だけが新しく `net`（純額）は古い**組み合わせで
     突き合わせることになり、**帰属不明が過少に読まれて武装する**。
     これは追記(9) 決定 1（実効数量で安全側へ倒す）が塞いだはずの **P6(1) と同じ帰結の門**である。監査の実測:

     ```
     PROBE4  Forgone = (なし＝武装した)
             Entries = 1                    ← 他人の 10 株が在るのに新規建てを送った
             prior.RemainingProtected = 10
     PROBE4b（順序が入れ替わらない場合）→ 正しく武装を見送る
     ```

   - **なぜ「前」が正しいか**: 確定を先に置けば、取り違えは必ず「`claimed` が古く `net` が新しい」＝
     帰属不明を**過大**に読む側になり、**安全側（見送り）へ倒れる**。
     `ProtectiveStopGuard.RunOnceAsync` も確定 → 照会の順である。
     - 🔴 **［2026-09-19 追記（11） / #820 の 12 巡目監査］直前の 2 文は誤りであり、撤回する。**
       「取り違えは**必ず**安全側へ倒れる」は**偽の全称命題**であった——`claimed` は窓のあいだ
       **減る方向にも動く**（決済・外部要因の確定・`PendingExternalReduction` の計上）。
       減る側では「`claimed` が古く**大きく**／`net` が新しく**小さい**」となり、帰属不明が**過少**に読まれて武装する
       （12 巡目 PROBE-A / PROBE-A3。**11 巡目 PROBE4 と同じ帰結**）。
       また「`ProtectiveStopGuard` も同じ順序」という類比も**片手落ち**である——
       **ガードは検知の直前に保護記録を照会後へ引き直しており、照会前の主張を持ち越さない**。
       是正は追記(11) 決定 1（**照会の前と後の両方で読み、小さい方を採る**）。
   - 🔴 **追記(9) 決定 2 のコード注記「ガードが巡回の先頭で行っているのと同じ操作である」は、
     順序が逆だったため実態と食い違っていた。** 本追記で順序を合わせ、注記も書き直した。
   - **固定**: T-10-506。**発注側だけが危険な向きである**ことも注記に残した。
2. **利用者が読む文書（`docs/`）へ、検知の射程を実装どおりに書く**（BLK-11-2）。
   下の「訂正」を参照。**IADR の「残る制約」では正しく開示されていたが、`docs/` と PR 本文が広く書いていた。**

### 訂正（誤りだった記述）

- 🔴 `docs/functional/FR-10_risk-controls.md`「どの記録も主張していない建玉があるとき」の行が
  「**常駐ガードが毎巡回検知し**…これが無いと②**受理された後に 0 約定で取り消された決済の残り**
  （決済を送った記録はその時点で完了するため、**巡回の対象に 1 件も残らない**）が、どの通知も出ないまま無保護で残る」
  と書いていた。**事実はその逆で、まさにその②の配置では検知が走らない。** 監査の実測:

  ```
  PROBE1  2 時間（240 巡回）で出た UnattributedPosition = 0 件
          建玉照会の回数 = 0
  ```

  T-10-487 のテスト本文自身が「ガードが巡回するための Active な行（別銘柄…）」と書いており、
  **検知は「他に無関係な Active 行が在ること」に依存している**。**1 銘柄しか持たない PoC 口座では、
  ②はこの PR の後も完全に無音である。** 行から②を落とし、「検知が走らない配置」を独立した箇条書きで開示した。
- 🔴 PR 本文の「運用上の前提条件 ③ …（下の『帰属不明の検知』がこの状態を知らせる）」も同じ誤りである。
  **実際に知らせるのは次の武装の見送り**（`OrderDispatchForgoneReason.UnattributedPosition`。監査の PROBE7 で確認）
  であって、ガードの検知ではない。PR 本文を直した。
- 🔴 上の「残る制約」の「直すには『対象ゼロでも建玉を照会する』へ変えるほかなく…」（NB-2）。
  **`Hosted/BrokerPositionSnapshotService` へ相乗りすれば往復を増やさずに塞げる**ため「ほかなく」は成り立たない。
  該当箇所をインラインで訂正した。**追随は #880。**

> 🔴 **10 巡目の是正 5 は「文書の事実誤認の訂正」だったのに、その是正で追加した文に同じ種類の誤りが入った。**
> `traceability.repo.md` の**規則 10**（是正のたびに「この変更で新たに誤りになる自分の記述」を引き直す。
> 是正前の語で引いても捕まらない）の実例である。11 巡目では、
> **新たに書いた文の側の語**（`常駐ガードが毎巡回検知` / `この状態を知らせる` / `へ変えるほかなく`）で全走査した。

### 残る制約（追記(9) から更新）

- 🔴 **受容した残余（BLK-10-1）は追記(9) の「受容した残余」節が正本である**（変更なし）。
- 🔴 **帰属不明の検知は、Active な行が 1 件も無い巡回では走らない**（追記(9) から継続・上で訂正済み。追随は #880）。
  **1 銘柄しか持たない口座では、「受理後に 0 約定で取り消された決済の残り」は無音のままである。**
  気づける唯一の経路は**その銘柄への次の武装の見送り**である。
- 🔴 **［NB-1］通知済みの記録は、その銘柄が建玉照会から消えた巡回ではリセットされない。**
  `DetectUnattributedPositions` の外側ループは `snapshot.Where(p => p.Quantity != 0)` を回すため、
  純額 0 の巡回ではリセット分岐に到達しない。結果、**一度解消したあと再発した同数の帰属不明が
  最大 60 分（`UnattributedRenotifyInterval`）黙る**。監査の実測:

  ```
  PROBE2a 純額 0 の巡回後の通知済み記録 = 10 / 再出現の巡回で出た件数 = 0
  PROBE2c 再出現から 59 分の沈黙中 = 0 件 ／ 60 分経過後 = 1 件
  ```

  **本 PR では直さない**（11 巡目であり射程を広げない）。**追随は #880**（相乗りと併せて直すのが自然である）。
- 🔴 **［NB-3］主張の一部だけが打ち消された行は、どちらの通知にも当たらない。**
  `UnattributedPosition` は帳簿の主張（`ProtectedQuantity`）で引くので 0 になり、
  `ProtectionSuspended` は実効数量が 0 でないため非該当である。
  **2 巡回（約 60 秒）で失効して BLK-10-1 の形へ収束する**ため受容する
  （失効までのあいだ、その行は主張の一部しか動かせない）。
- **到達済みの行の観測は依然として単調である**（追記(8) のまま）。
- 追記(9) のその他の残る制約（`SentCloseScanLimit`＝50 の走査上限とその倒れ方、
  エントリーが未終端のあいだの 2 本目の見送り）はそのまま残る。

## ［2026-09-19 追記（11） / #820 の 12 巡目監査］武装の判定は照会の「前と後」の両方の主張と突き合わせ、小さい方を採る

12 巡目の監査は**中核が無傷である**ことを実測した——無作為順列 1,200 試行で `violations=0` /
`worstNetAfterSystemClose=0`（武装 1,355 回・実決済を伴うガード巡回 304 回まで届いた**非空虚な掃き**）。
T-10-506 も変異注入で非空虚と確認された。**決定 1〜4・7〜9、追記(4)〜(9) の方式・受容した残余（BLK-10-1）は変えない。**

ブロッキング 2 件のうち **1 件（BLK-12-1）は 11 巡目の是正が新設した鏡像の競合**である。

### 根本原因: 是正の向きだけを見て、逆向きを見ていなかった

11 巡目の是正（確定 → 照会に固定する）は、**「窓のあいだ `claimed` は増える方向にしか動かない」を暗黙に仮定**していた。
**実際には両方向へ動く。**

| 向き | 何が動かすか | 起きる取り違え |
| --- | --- | --- |
| **増える** | `OrderFillPollingService` が先行エントリーの記録を終端化 → `ConfirmEntryFills` が確定する | `claimed` が新しく大きい／`net` が古い（11 巡目 PROBE4） |
| **減る** | `SoftwareStopExecutor` の決済（`RemainingProtected`→0 かつ `Completed`）／ガードによる外部要因の確定／`PendingExternalReduction` の計上 | `claimed` が古く大きい／`net` が新しく小さい（12 巡目 PROBE-A / PROBE-A3） |

**どちらも帰結は同じ**——帰属不明が**過少**に読まれ、他人の建玉が在るのに武装する。
**実現性は減る側の方がむしろ高い**: PROBE4 は別サービスのローカル書き込みが挟まる窓だが、
**PROBE-A3 は同一プロセスの常駐ガードの決済が挟まる窓**であり、両者は「同じ建玉の消滅」という
**相関した 1 つの事象**から生じる。

### 改めた決定

1. 🔴 **武装の判定は、建玉照会の「前」と「後」の両方で主張を読み、`claimed = min(前, 後)` を採る**
   （追記(10) 決定 1 の「確定を前に固定する」を包含して改める）。
   - **確定（`ConfirmEntryFills`）は引き続き照会の前に 1 回だけ行う**（永続化されるので後の読みにも効く）。
     後の読みは**保護記録を引き直すだけ**である——**OpenD への往復は増えない**（ローカル読みが 1 回増えるだけ）。
   - **`min` を採るのは、どちらへ動いても帰属不明を「過大」に読む側へ倒すためである。**
     過大なら**見送り**（可逆・建玉を作らない）、過少なら**他人の建玉を自分の損切りラインで売る**（不可逆）。
   - **両方向が同時に閉じることを 2 本のプローブで示した**（T-10-506＝増える側・T-10-507＝減る側）。
     片側だけの形では必ずどちらかが赤くなることも実測した（下の「方針と合わなかった点」）。
2. **コード注記の類比を正す**（NB-12-5）。`ProtectiveStopGuard.RunOnceAsync` は**検知の直前に保護記録を
   照会後へ引き直している**（`stops.FindActive(batchSize)`）ため、**照会前の主張を持ち越さない**。
   「ガードと同じ順序だから安全」という論は**発注側には移せない**。両者の注記へその差を書いた。

### 方針と合わなかった点（記録）

無い。**ただし、是正の形を決める前に 3 通りを実測した**（片側だけでは必ず逆側が空く）:

| 形 | T-10-506（増える側） | T-10-507（減る側） |
| --- | --- | --- |
| 照会の**後**だけで数える（10 巡目の形） | **赤** | 緑 |
| 照会の**前**だけで数える（11 巡目の形） | 緑 | **赤** |
| **`min(前, 後)`（本追記）** | **緑** | **緑** |

### 訂正（誤りだった記述）

- 🔴 追記(10) 決定 1 の「確定を先に置けば、取り違えは**必ず**…**安全側（見送り）へ倒れる**」は
  **偽の全称命題**であり撤回した（該当箇所へインラインで訂正を入れた）。
  🔴 **`.ai-context/adr/` は凍結記録であり、偽の全称命題を凍結すると次の巡回がそれを根拠に使う。**
  本追記では「`min` なら**どちらへ動いても**過大側へ倒れる」と、**動く向きを列挙したうえで**述べている。
- 🔴 追記(10) 決定 1 の「`ProtectiveStopGuard.RunOnceAsync` も同じ順序である」は**片手落ち**であった（上の決定 2）。
- 🔴 `docs/data/audit-events.md` に **BLK-11-2 と同型の誤記が残っていた**（BLK-12-2・規則 10 の追随漏れ）。
  「受理された後に 0 約定で取り消された決済の残り」は、**まさにその配置でこの記録が出ない**。
  11 巡目に `docs/functional/FR-10_risk-controls.md` を直したとき、**同じ文言を持つ別ファイルを引き直していなかった**。
  作業仕様書の受け入れ基準 49 にも「毎巡回」と②が残っていたので直した。
- 🔴 作業仕様書の走査表が `IADR-0344:689` / 仕様書 `:621` を「材料を毎巡回持っている＝誤りではない」と分類していたが、
  **この 2 箇所は文字どおり『検知する』と書いている**（NB-12-2）。**凍結記録なので本文は残す**判断は変えないが、
  分類の理由を「誤りだが凍結記録であり、訂正は追記側で行う」へ直した。

### 残る制約（追記(10) から更新）

- 🔴 **受容した残余（BLK-10-1）は追記(9) の「受容した残余」節が正本である**（変更なし）。
- 🔴 **帰属不明の検知は Active な行が 1 件も無い巡回では走らない**（追記(10) から継続。**追随は #880**）。
- 🔴 **［NB-1］通知済みの記録は、その銘柄が建玉照会から消えた巡回ではリセットされない**（追記(10) のまま。#880）。
- 🔴 **［NB-3］主張の一部だけが打ち消された行はどちらの通知にも当たらない**（追記(10) のまま。2 巡回で収束するため受容）。
- 🔴 **［NB-12-4］11 巡目の是正以降、`ConfirmEntryFills` は建玉照会に失敗した（`snapshot` が `null`）巡回でも走る。**
  従来（10 巡目まで）は照会の後に置かれていたため、早期 return で走らなかった。
  **確定は冪等な書き込み**（終端の発注記録の約定数量を `RemainingProtected` へ 1 回書くだけ）であり実害は見ていないが、
  **挙動の変化であることを記録する**。照会不能の巡回でも帳簿の確定だけは進む。
- **`min` は「窓のあいだ主張が動いた」ことを検知するものではない**——動いたぶんだけ**保守的に倒す**だけである。
  窓のあいだに主張が減り、かつ同量の建玉も消えた（＝本当は帰属不明が無い）場合、**安全側に見送る**ことがある。
  倒れ方は「建玉を作らない」側であり、次の巡回で解消する。
- **到達済みの行の観測は依然として単調である**（追記(8) のまま）。


## ［2026-09-19 追記（12） / #820 の 13 巡目監査とマージ］`UnattributedPosition` の序数を 4 から 6 へ繰り下げた

13 巡目の監査は**ブロッキング 0 件**で通り、非ブロッキング 5 件（NB-13-1〜5）を直してマージへ進んだ。
その最後の `develop` 取り込み（`890f6b24`＝#874）で、**同じ enum の末尾を取り合う衝突**が起きた。

- **#864（先にマージ済み。PR は #873・IADR-0355）**が `OrderDispatchForgoneReason` へ `BrokerPositionAbsent=4` と
  `BrokerPositionsIndeterminate=5` を足していた。
- 本 PR は同じ enum へ `UnattributedPosition=4` を足していた（追記(8) 決定 4）。

🔴 **序数はメトリクスのタグ・監査 payload の整数として往来するため、既存の値を動かせない。**
**先にマージされた側が番号を確保する**（`traceability.md` の改番手順と同じ規律）ので、
**本 PR の値を 4 → 6 へ繰り下げた**（総数は 7）。追随させたのは次のすべてである。

- 契約: `OrderDispatchForgone.cs`（enum の位置と doc の「序数 6」）
- 契約テスト: `StopLossMethodContractTests` の序数表（`UnattributedPosition == 6`・`HaveCount(7)`）
- 発注執行のテスト 7 箇所（`Should().Be(6, "OrderDispatchForgoneReason.UnattributedPosition")`）
- 本 IADR の追記(8) 決定 4 と作業仕様書の追記(5)（**凍結記録なので本文は残し、インラインで繰り下げを注記**）

**挙動は 1 バイトも変わらない**（見送りの条件・通知の文面・監査の要約はいずれも enum 名で書かれている）。
変わったのは**整数として外へ出る値**だけであり、**本 PR は未マージなので過去の記録との齟齬は生じない**。

### 同じマージで併せて解決したもの（いずれも両側の和集合）

- `OrderDispatchResult`: #874 が `Drift` / `DriftDispatchedQuantity` を位置引数で足していたため、
  **`SoftwareStopArmed` を末尾へ回した**（位置引数の並びを壊さない）。
- `OrderExecutionAppService` の引数名 `positions` → **`brokerPositions`**（#874 の名前を採る）。
  `Forgone` は #874 の `drift` 付き署名を採る。
- `NotificationFormatter` / そのテスト・`docs/functional` と `docs/tests` の trace ブロック: 両側の項目を残した。


## ［2026-09-23 追記（13） / #909・IADR-0380］S1 が保護するのは通常取引時間だけであることを残余リスクとして明記する

稼働 PoC の運用観測（[#909](https://github.com/endazon/ai-stock-trading/issues/909)）で、
**市場監視の開場判定が「UTC の曜日が土日でない」だけだった**ことが分かった（`WeekdayMarketSchedule`）。
米東 16:00 の引けから 5 時間後も市況 API への照会が 60 秒ごとに続いており、**閉場中の凍った終値で S1 の到達判定が
成立し得る**状態だった（終値がラインを割った日は閉場中ずっと成立し続ける。そこで出した成行は翌寄りまで約定しない）。

**是正は [IADR-0380](IADR-0380_market-session-schedule-and-closed-protection-gap.md) が持つ**（開場判定を
市場ローカル時刻の取引時間＋規則計算の休場日へ差し替え、閉場している市場は評価そのものを行わない）。
**本 IADR の決定は 1 つも覆らない。** 本追記が足すのは、**射程の開示**である。

### 🔴 残余リスク: S1 は「開場中だけの保護」である

- **S1 の保護は市場監視の巡回が到達を検知して初めて働く。** 巡回は通常取引時間（米国株は米東 9:30–16:00。
  休場日・半日取引日の午後を除く）だけ回るため、**閉場中の建玉は次の寄り付きまで無保護である**。
- **夜間・寄り前の急落からは守られない。** 気配がラインを割っても、システムは翌寄りまで何もしない。
- **これは縮退ではなく S1 の性質である。** 閉場中に成行を出しても約定しないため、
  **閉場中に発動させないことが正しい**（IADR-0380 決定2）。ブローカー側の逆指値（S0）も約定は寄り付き以降であり、
  「閉場中に守られる」機構はそもそも存在しない —— 違うのは、S0 が**寄り付きに自動で発注済み**であるのに対し、
  S1 は**寄り付き後の最初の巡回（最大 1 周期＝既定 60 秒）を待つ**点である。
- **黙って通り過ぎさせない。** 閉場のたびに市場ごと 1 回、保有・ライン・最終観測値・次の開場時刻をログへ出す
  （最終観測値が既にラインを越えていたら Critical）。IADR-0380 決定3。
- **利用者へ出す。** S1 配置通知（`SoftwareStopArmed`）の文面と**日報 §3（ポジション一覧）の注記**に
  「保護が働くのは通常取引時間だけ」を書いた。**コードのコメントに留めない**（この追記の主眼）。

### 訂正（誤りだった記述）

- 🔴 決定 8 が言う「損切りライン到達（`StopLossTriggered`）で発注執行が成行決済する」は、
  **到達の検知が開場中に限られる**という前提を書いていなかった。本文は凍結記録として残し、
  **射程は本追記が正本**とする（挙動は IADR-0380 の是正で本追記のとおりになった）。

## ［2026-09-25 追記（14） / #936・IADR-0393］台帳のラインは「最新エントリーに丸められる」ではなくなった

決定4 の理由文「行自身のラインで判定するのは、**台帳の損切りラインが銘柄単位で最新エントリーの値に丸められる**ため」と、
選択肢 5 の「台帳は銘柄単位で最新エントリーの値しか持たない」は、[IADR-0393](IADR-0393_most-protective-stop-line-per-entry-lot.md) で
**前提が変わった**（本文は凍結記録として残す）。台帳のラインは**保有中のエントリーのうち最も保護的な値**になった。

- **決定4（行自身のラインで判定する）は変えない。むしろ依拠する。** 市場監視は最も保護的なラインで到達を 1 件出すので、
  同じ銘柄の他の行（ラインが低い）は `Reached` で外れなければならない。外さないと、まだ自分のラインに達していない建玉を売る。
- 稼働中の配置（713 株 331.67／715 株 330.88）で、331.40 の到達は 713 株の行だけを決済し、続く 330.50 の到達で 715 株の行を決済する。
  合計は 1,428 株を超えない（T-10-767 / T-10-768）。
- 決定 5〜9 と追記(1)〜(13) の決済経路（残保護数量・`ReconcileShares`・予約・建玉照会）は 1 行も触っていない。

## ［2026-09-25 追記（15） / #833 項目2］決定 5-6 の打ち切りを撤去し、行ごとの待ち時間（永続）に替える

[#833](https://github.com/endazon/ai-stock-trading/issues/833) の項目 2。作業仕様書
[20260925_833_software-stop-close-backoff](../specs/20260925_833_software-stop-close-backoff.md)。

### 現れ方

開場中に決済の拒否が続くと、ガード（30 秒）と市場監視の到達（60 秒）が同じ行を撃ち続けた。決定 5-6 の歯止めは
「到達 1 回あたり 3 試行で**到達の記録を消す**」打ち切りで、次の到達（60 秒後）で即座に再武装するため、
**毎分 3 件前後の成行と 3 回ごとの Critical** が持続した。受理 → 0 約定で失効 → 再武装（IADR-0389）のループには
回数の歯止めすら無かった（IADR-0389 §結果の残余）。

🔴 **打ち切りそのものが出口を塞いでいた。** 到達の記録を消すと、**価格がラインの内側へ戻ったとき二度と撃たない**
（次の到達が来ないため）。決定 4「一度到達したら価格が戻っても決済する」と矛盾していた。

### 改めた決定

1. **決定 5-6 の打ち切り（`attempt % 3 == 0` で `TriggeredAt` / `TriggeredPrice` を消す）を撤去した。**
   拒否が何回続いても到達の記録は残り、撃ち直しは**止まらない**。
2. **行ごとの待ち時間を永続化する**: `protective_stop_orders` に `CloseFailures`（int・既定 0）・
   `NextCloseAttemptAt`（null）・`LastTriggerSeenAt`（null）を足した（migration `AddSoftwareStopCloseBackoff`。
   **列の追加だけ**で、既存行は 0 / null / null＝待ち時間なしで読まれる）。
3. **待ち時間は連続失敗 n 回目の後 min(30 秒 × 2^(n−1), 15 分)**（`SoftwareStopExecutor.CloseBackoff`）。
4. **判定は `TryCloseAsync` の、成行を送る直前**に置く——ハンドラ（到達）とガード（巡回）が共有する入口なので
   **両方が守る**。待ち時間が止めるのは新しい成行だけで、記録済みの結果での確定・残保護数量 0 での完了・建玉照会・
   持ち分の確定は待ち時間中も行う。待ち時間中は据え置き（`Deferred`）であり、決定 9（据え置き継続の Critical）はそのまま効く。
5. **「失敗」**: ①決済が受理されなかった ②受理された決済が **1 株も約定しないまま**終端した（`SoftwareStopReArmer` の再武装）。
   1 株でも約定した再武装は前進として数えを 0 へ戻す。
6. **Critical（`CloseRejected`）は連続失敗 3 回目で出し、以後 4 回ごとに 1 回**（上限 15 分に達した後はおよそ 1 時間ごと）。
   **抑止はしない**——失敗のたびの `LogError` は従来どおり残す。定数名 `MaxCloseAttemptsPerTrigger`（= 3）は
   IADR-0369 などが引いているため変えず、意味を「Critical を出す連続失敗の回数」へ改めた。
7. **新しい到達の窓では数えをやり直す**: 前回の到達から `TriggerEpisodeGap`（5 分）以上空いた到達（閉場を挟んだ・価格が一度戻った）を
   受けたら、数えと待ち時間を 0 へ戻してすぐ撃つ。60 秒間隔で続く到達では戻さない（戻すと待ち時間が毎分消え、拒否連発が再発する）。
   再配送・遅れて届いた古い到達は `LastTriggerSeenAt` を巻き戻さず、窓も開かない。

### 方針と合わなかった点（記録）

- issue の提案は「Critical は 1 窓 1 回」であった。待ち時間の 1 窓（上限 15 分）ごとに Critical を出すと、
  閉場中にガードが撃ち直し続ける配置（下の残る制約）で**一晩に数十本**の Critical になり、本当に見るべき通知が埋もれる。
  **「3 回目で 1 回、以後 4 窓に 1 回」**とし、各失敗の `LogError` は残した（抑止ではなく間引き）。
- 「新しい窓でやり直す」規則は issue に無い。入れないと、閉場中の撃ち直しで待ち時間が上限（15 分）へ育ち、
  **寄り付き後の最初の撃ち直しが最大 15 分遅れる**（従来は寄り後最初の到達＝最大 60 秒）。出口の遅延を増やさないために足した。

### 残る制約

- **閉場中もガードは到達済みの行を撃ち直す**（発注執行は市場の開場判定を持たない。判定は市場監視にしか無い・IADR-0380）。
  閉場中の成行が拒否される構成では、待ち時間の上限（15 分）ごとに 1 本の成行と、4 本に 1 回の Critical が続く。
  寄り付き後の最初の到達で窓がやり直されるため、出口の遅延は増えない。
- **決定 9（据え置き継続の Critical）は「1 行 1 回」のまま**（#833 項目 4 は着地済みとみなした）。本追記で到達の記録を
  消さなくなったため、行の生涯で `TriggeredAt` は最初の到達のまま動かず、`StalledNotifiedAt` は再武装（IADR-0389）でしか落ちない。
  長期の据え置きでは再通知しない——拒否が続く配置は上の 6 の Critical が 4 回ごとに鳴るので無音にはならないが、
  **拒否ではない据え置き**（建玉照会不能・取消待ち・送信結果不明）が何日も続く配置は最初の 1 回しか鳴らない。
- 🔴 **偽になった注記**: `ProtectiveStopGuard.cs` の `MaxConfirmedCloseRejections` の doc と
  `CloseRejectionTracker.cs` 冒頭は「S1 の上限は到達 1 回あたりで、使い切ると TriggeredAt を消して次の到達で自ら再武装する」と書いていた。
  **本追記で偽になった**ため、並行 PR（#944 / #945）のマージを待って同じ PR で直した（挙動には関与しない注記である）。
  IADR-0369 の本文（S1 と S0 の比較表）も同じ記述を持つが、他 IADR の凍結本文であり、**射程は本追記が正本**とする。
- **同じ行を並行に書く経路の last-writer-wins**（#833 項目 3）は本追記の射程外。待ち時間の列も古い写しで上書きされ得る。

## ［2026-09-25 追記（16） / #880・IADR-0412］帰属不明の検知を建玉観測の常駐へ相乗りさせ、NB-1・NB-2 を解消した

追記(10)・追記(11) の残る制約のうち、**帰属不明の検知に関する 2 行**（「Active な行が 1 件も無い巡回では走らない」＝NB-2、
「通知済みの記録は、その銘柄が建玉照会から消えた巡回ではリセットされない」＝NB-1）は、[IADR-0412](IADR-0412_unattributed-position-detection-on-position-snapshot.md) で解消した。
**本文（追記(9)〜(11)）は書き換えない。射程は本追記と IADR-0412 が正本である。**

- NB-2: 建玉観測の常駐（`Hosted/BrokerPositionSnapshotService`・既定 600 秒）が、観測で取ったスナップショットで同じ検知を走らせる（照会は増やさない）。
  **ガードの「巡回対象ゼロなら照会しない」不変条件は変えていない。** 「気づける唯一の経路は次の武装の見送り」は、
  `Reconciliation:Positions:Enabled=false` の構成と内蔵 paper 構成に限って成り立つ記述になった。
- NB-1: 検知が「通知済みの印を持つ行の群」（`IProtectiveStopOrderStore.FindUnattributedNotified`）も訪れ、純額 0 ならリセットする。
- 照会不能（null）では検知しない（印も消さない）。
- 追記(10)・追記(11) の他の残る制約（NB-3・到達済みの行の観測の単調性・`SentCloseScanLimit` の走査上限・受容した残余 BLK-10-1）は変わらない。

## ［2026-09-25 追記（17） / #937］S1 はホストとクラスタが生きている間しか守れないことを残余リスクとして明記する

稼働 PoC の運用観測（[#937](https://github.com/endazon/ai-stock-trading/issues/937)）で、**2026-09-23 22:37:16 JST（米国の開場 7 分後）にホスト PC が再起動**し、
Rancher Desktop が自動起動しなかった。Kubernetes API（127.0.0.1:6443）は接続拒否となり、22:41:31 にオーナーが手で起動するまで
**約 4 分間、AAPL 707 株は無保護**だった。復帰の 2 分後（22:43:33）に S1 が発火し全量約定した——**無保護の窓でラインを割っていれば、
成行は窓が閉じるまで出なかった**。

**本 IADR の決定は 1 つも覆らない。** 本追記が足すのは、追記(13)（開場中だけの保護）と同じく**射程の開示**である。

### 🔴 残余リスク: S1 は「ホストとクラスタが生きている間だけの保護」である

- **到達の検知（市場監視）も決済（発注執行）もクラスタの中で動く。** ブローカー側に逆指値は無い（S1 を選ぶ理由そのもの。SIMULATE は
  ブローカー側の逆指値を受理しない。#809）。したがって**ホスト PC の再起動・スリープ・電源断、Rancher Desktop（k3s）の停止、
  市場監視・発注執行・メッセージ基盤の停止の間は、開場中であっても到達を検知せず、決済も出ない**。
- **S0 との違い**: S0 はブローカー側に逆指値が置かれているため、ホストが止まっていてもブローカーが発火させる。S1 にはこの保護が無い。
  追記(13) の「開場中だけ」と合わせると、**S1 が守るのは「開場中」かつ「ホスト・クラスタ・3 サービスが生きている」間だけ**である。
- **クラスタの中の監視はクラスタと一緒に止まる。** Prometheus のアラート・Discord 通知サービス・生存要約（[IADR-0365](IADR-0365_s1-stop-evaluation-liveness-summary.md)）は
  いずれもクラスタの中にあり、2026-09-23 の停止はどこからも知らされなかった。

### 緩和（本追記と同じ PR）

- **クラスタの外から見張る**: `scripts/host-liveness-monitor.ps1`（PowerShell 7・ホストで 5 分ごと）が、**米国株の通常取引時間だけ**
  Kubernetes API（`/readyz`）・市場監視と発注執行の Pod の Ready・2 つの生存要約（市場監視の「損切り評価は稼働中」／発注執行の「S1）の保護は継続中」）の
  鮮度を**読み**、クラスタに依らない経路（Windows の通知・任意の Discord Webhook・ログファイル）で知らせる。開場判定は共有カーネルの `MarketHours` と同じ規則
  （[IADR-0380](IADR-0380_market-session-schedule-and-closed-protection-gap.md)）を写した。
  - **S1 行が Active（発注執行の要約が新しい）なのに市場監視の要約が 10 分を超えて無い**ことを「S1 の監視が死んでいる」として鳴らす（寄り付き・再起動の直後は猶予）。
  - 🔴 **要約が両方無いことを「保有なし」と読まない。** 保有の有無を外から確かめる手段は要約しか無い（HTTP は認可つき）。
    この取引時間中に保有を見ていたときだけ 1 回警告し、それ以外は「不明」として記録する。
  - 平常時は鳴らさず、遷移で鳴らし、警報は 30 分ごとに鳴らし直し、回復を 1 回知らせる。
  - 判定・通知の要否は純関数で、`scripts/host-liveness-monitor.test.ps1`（T-10-1040〜T-10-1049。kubectl を関数で差し替えた観測の層を含む）が CI で固定する。
    設計の詳細は作業仕様書 [20260925_937_host-liveness-monitor](../specs/20260925_937_host-liveness-monitor.md)。
- **オーナーの操作**（手順は `docs/operations/host-liveness-monitor-runbook.md`。**AI は登録・設定変更をしない**）:
  タスクスケジューラへの登録、Rancher Desktop のログオン時の自動起動、Windows Update のアクティブ時間（場中に自動の再起動をしない）、スリープの無効化。

### 残る制約

- **ホストそのものが止まっている・誰もログオンしていない間は、ホスト上の監視も止まっている。** 再起動の後、ログオンするまでは
  Rancher Desktop も監視も起動しない。ホストの外から見張る仕組み（外部の死活監視）は用意していない。
- 監視は**知らせるだけ**で、復旧（Rancher Desktop の起動・Pod の再起動）も建玉の手当ても行わない。検知から手当てまでの窓は、人が通知に気づくまで開いたままである。
- 評価の停止の検知は要約の途絶が 10 分を超えてから（実行間隔 5 分を足して最大およそ 15 分）。発注執行の要約は moomoo 構成でだけ出る。
- **無保護の窓そのものを閉じる手段は S0（ブローカー側の逆指値）しか無い**（SIMULATE では不受理。#809）。本追記はこの構造上の弱点を開示し、窓を短くするだけである。

<!-- IADR-0060 決定 3: バージョン付き全体前提条件（FR-17）の共有クライアント。消費側サービス（費用統制 #139・損益集計・
     AI 判断・リスク統制）が同じキャッシュ・無効化・fail-safe を書き写さずに済むよう 1 箇所へ集約する。設定サービスの
     Application 層（サーバ内部）には依存せず、Domain（前提条件の型）と Shared.Contracts（AssumptionsChanged）のみを参照する。 -->
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Library</OutputType>
    <RootNamespace>AiStockTrading.Configuration.Client</RootNamespace>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\ConfigurationService.Domain\ConfigurationService.Domain.csproj" />
    <ProjectReference Include="..\..\..\..\Shared\AiStockTrading.Shared.Contracts\AiStockTrading.Shared.Contracts.csproj" />
    <!-- IADR-0051: s2s トークン付与（AddAiStockTradingServiceToken）。MassTransit（IConsumer）も本参照から得る。 -->
    <ProjectReference Include="..\..\..\..\TestSupport\AiStockTrading.TestSupport.PlatformShim\AiStockTrading.TestSupport.PlatformShim.csproj" />
  </ItemGroup>
  <ItemGroup>
    <InternalsVisibleTo Include="ConfigurationService.Client.Tests" />
  </ItemGroup>
</Project>

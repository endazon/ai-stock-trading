# CI（検証ゲート）— impl-handoff-kit の ci.example.yml を本リポジトリのスタック（C#/.NET）へ適用したもの。
#
# 技術非依存のジョブ（commit-messages / scripts-tests / doc-links / pipeline-config / ai-workflow-config）は
# キットの雛形と同型に保つ。スタック依存のジョブ（lint / build-and-test）と本リポジトリ固有の
# ジョブ（shell-scripts / runtime-scaffold / consumer-endpoint-names / frontend / frontend-e2e）は
# 本リポジトリ側で維持する。
#
# ── スタック別 --allowedTools（claude-coding / claude-code-review の claude_args を書き換える） ──
# このファイルの build/test ステップを変更したら、claude-coding.example.yml と
# claude-code-review.example.yml の claude_args（スタック別ツール）を使用スタックに合わせて
# 書き換えること。**両方**を同じ内容に保つ（レビュー側から実行系を落とすと、AI レビューは
# 検証できず静的読解へ退行し、毎回「Bash が承認待ちでブロックされた」と報告するだけになる）。
# .claude/settings.json の permissions.allow にも同じコマンドを追記すること（3 系統を揃える）。
# 記法誤りと複製漏れ（レビュー側に一部/全部の実行ツールが無い）は ai-workflow-config ジョブが
# 機械検出する。比較対象はスタック別の実行ツール（dotnet / node・npm / python・pytest / go /
# mvn・gradle）であり、setup-* を書いていないもの（node のようにランナー既定で入るランタイム）も
# 含む。設計上の差（Edit / Write / 書き込み系 git がレビュー側に無いこと）は対象外である。
#
# 注: claude_args の --allowedTools は「二重引用符でくくった 1 引数・カンマ区切り」で書く。
# 1 ツール 1 行に戻すと空白で割れて全指定が無効になる（各ワークフローの記法コメントを参照）。
#
# スタック別の対応コマンド例:
#   C#/.NET : Bash(dotnet build:*), Bash(dotnet test:*), Bash(dotnet format:*), Bash(dotnet restore:*)
#   Node.js : Bash(npm run:*), Bash(npm ci:*), Bash(npx:*)
#   Python  : Bash(pytest:*), Bash(pip install:*), Bash(ruff:*)
#   Go      : Bash(go build:*), Bash(go test:*), Bash(go vet:*)
#   Java    : Bash(mvn:*) または Bash(gradle:*)
# ─────────────────────────────────────────────────────────────────────────────────
#
# 推奨運用（README「全自動化のための推奨ツール・運用」も参照）:
#   - main へのマージ前に lint → build → test → coverage を必須ステータスチェックにする
#   - ブランチ保護で「マージ前に必須チェックの成功」と「レビュー承認」を要求する
#   これにより AI が作成した PR も、人手の検証なしに最低限の品質を機械で担保できる。

name: CI

on:
  push:
    branches: [develop, main]
  pull_request:
    types: [opened, synchronize, reopened]

# NFR / IADR-0208 決定 1: 連続 push で古い実行を捨てる。実測（2026-08-21・run 32443950165）では
# PR ブランチへ短い間隔で push が続き、キャンセルされない旧 run がランナーを占有し続けていた。
#
# 🔴 develop / main への push はキャンセルしない。PR から外した検証（全量 CodeQL・vulnerable-scan）の
# 担保がそちらに載るため、途中で捨てると担保そのものが失われる（IADR-0208 決定 1）。
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  # 🔴 **変更領域の判定は、専用ジョブを置かず各ジョブが自前で行う**（NFR / IADR-0208 決定 12）。
  #
  # 以前は `changes` ジョブを 1 本置いて `needs:` で配っていたが、**実測で直列の前置きになり
  # シャードの開始が +34 秒まで遅れていた**（判定そのものは 6 秒で、残りはランナーの起動待ち）。
  # 各ジョブが自前で判定すれば**前置きが消え、開始が約 +5 秒になる**。
  #
  # 🔴 **副次的に、必須チェックが黙って「合格」になる経路が構造的に消える。**
  # `needs:` を持つと、上流ジョブが失敗したとき依存ジョブは `if:` を評価する前に skip され、
  # **`if` でスキップされた必須チェックは「合格」**として扱われる（決定 7）。
  # `needs:` を持たなければ、この経路自体が成立しない。
  # ただし集約ジョブ（build-and-test）は脚の成否を知る必要があるため `needs:` を残し、
  # **`always()` ＋ Gate** で守る（決定 11 の追補）。
  #
  # 判定と自己試験は scripts/detect-changed-areas.js が持つ（`--git` で差分取得まで行う）。

  # 変更領域の判定（NFR / IADR-0208 決定 11）。
  #
  # 実測（2026-08-22・直近 52 マージ）では **18 本（34%）が backend に影響しない変更だけ**で、
  # それでも build-and-test（208 秒）と lint を全額払っていた。
  #
  # 🔴 **既定は「走らせる」。** 許可リストに全件が収まるときだけ skip する。
  # 誤りの倒れ方が非対称だからである —— 余計に走らせる誤りは遅いだけで気付けるが、
  # **走らせない誤りは退行を無検証でマージし、緑のまま誰も気付かない。**
  # 判定と自己試験は scripts/detect-changed-areas.js が持つ。
  # コミットメッセージ規約（種別(起点ID): 要約）の機械チェック（再発防止）。
  # PR で追加されるコミット（base..HEAD）のみ検査し、bot・マージ・[skip ci] は除外する。
  # fetch-depth: 0 は base..HEAD の範囲解決に必須。
  commit-messages:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version: "20"
      - name: Check commit messages
        env:
          GITHUB_BASE_REF: ${{ github.base_ref }}
        run: node scripts/check-commit-messages.js

  # スタック非依存: scripts/ 配下の単体テスト（外部依存ゼロ・Node 標準 assert のみ）。
  #
  # 個別の検証器（validate-pipeline-config / check-ai-workflow-config）は下のジョブで
  # --self-test を走らせるが、それらは検証器**単体**の試験である。scripts.test.js は
  # 横断的なテストであり、両者は重複しない。
  # 【重要】このジョブが無いと、scripts.test.js は「誰かが手で叩いたときだけ走るテスト」になる。
  # 実際に、CHANGELOG 生成が全面的に壊れる回帰が、PR の CI をすべて green のまま通り抜けた
  # ことがある（changelog.yml は push でしか起動しないため、壊れるのはマージ後）。
  # fetch-depth: 0 は allowlist の到達可能性検査と gen-changelog の実行に必要
  # （浅いクローンでは当該テストが best-effort で skip されるため、検査が実効しない）。
  scripts-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version: "20"
      - name: Run scripts unit tests
        # 本リポジトリは固有テスト（scripts/scripts.repo.test.js）を持つため有効化する。
        # companion が消失（誤削除・リネーム・キット同期での上書き）したときに失敗する。
        # 未設定だとテスト件数が静かに減るだけで CI は green のままになる。
        env:
          REQUIRE_REPO_TESTS: "1"
        run: node scripts/scripts.test.js

  # NFR / IADR-0208 決定 6: 依存ゼロの検査（Node ＋ bash）を 1 ジョブへ束ねる（統合前は 15 の独立ジョブ）。
  #
  # 束ねた理由は「ジョブが多いと遅いから」ではない。ci.yml は needs: を 1 つも持たず全並列なので
  # 待ち時間はほとんど作っていなかった（実測: 各ジョブ 10〜15 秒）。問題は 2 つある。
  #   - 1 ジョブあたり約 13 秒（checkout 2s ＋ setup-node 5s ＋ 起動 3s）が実処理 1 秒未満の検査に掛かる
  #   - 同時実行スロットを 15 個占有し、本命の build-and-test の開始を遅らせる
  #
  # 副次効果として、統合前は 4 ジョブがそれぞれ 1147 個の .cs を個別に走査していたのが 1 回で済む。
  #
  # 🔴 検査は 1 本も減らしていない。ジョブが減っただけで、走る検査器の本数と対象は統合前と同一である。
  #
  # 🔴 各ステップに `if: ${{ !cancelled() }}` を付けてある。これが無いと最初の失敗で以降が走らず、
  # 1 回の CI で 1 件しか分からなくなって往復が増える（束ねたことで可読性が落ちる形）。
  # 付けてあるので、落ちた検査が複数あれば一度に全部出る。ジョブの合否は従来どおり
  # 「どれか 1 つでも失敗すれば赤」である。
  #
  # 🔴 各検査の直前のコメントは統合前のジョブのものをそのまま移した。ADR 番号・事故の経緯・
  # 「これを外すと検査が空回りする」という設計要点を持つため、1 行も捨てない。
  #
  # commit-messages（必須チェック）と scripts-tests（子プロセスを多数回す重量級）は単独のまま残す。
  static-checks:
    runs-on: ubuntu-latest
    # 【置換点】キット既定は空（採らないプロジェクトでは自己試験だけが走る）。本リポは取引パイプラインを宣言する。
    env:
      PIPELINE_CONFIG: "deploy/helm/ai-stock-trading/files/pipeline.json"
    steps:
      - uses: actions/checkout@v7
        with:
          # check-action-versions の --compare-with-ref が統合ブランチ時点のワークフローを
          # 読むため、履歴が要る（shallow だと当該検査が warn で skip される）。
          # adr-index-sync も diff 範囲の解決に履歴を使う。
          # 履歴は現在 50 コミット程度なので全取得しても安い。
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version: "20"

      # #263, IADR-0109: ローカルデプロイスクリプトの ast-secrets 同期（投入済みの鍵を無言で壊さない）を
      # kubectl スタブで検証する。実クラスタ・実 kubectl・追加パッケージは不要（Bash のみ）。
      - name: Test k8s-local-deploy.sh (ast-secrets sync)
        if: ${{ !cancelled() }}
        run: bash scripts/k8s-local-deploy.test.sh
      # #274, IADR-0060 決定3: OpenD entrypoint の RSA 鍵検査（k8s Secret の symlink 越しに実体の
      # パーミッションを見る）を固定する。symlink と chmod が要るため ubuntu-latest で実行する。
      - name: Test opend entrypoint.sh (RSA key permission check)
        if: ${{ !cancelled() }}
        run: bash deploy/opend/entrypoint.test.sh

      # スタック非依存: docs/ ・ .ai-context/ 配下 Markdown の相対リンク切れを検査（再発防止）。
      # scripts/check-doc-links.js は Node 標準モジュールのみで動作し、依存インストール不要。
      # NFR / #493 / #496: 相対リンクの実在検査。
      #
      # 資料再編（ADR-0029 決定 2）で本リポジトリは planning への依存を持たない。かつて
      # ここにあった「PR 段階で planning submodule を populate して当該配下も検査する」分岐
      # （submodule 取得・取得用トークンの受け渡し・--require-planning）は、planning
      # 自体が撤去されたため削除した。
      - name: Self-test doc link checker
        if: ${{ !cancelled() }}
        run: node scripts/check-doc-links.js --self-test
      - name: Check doc links
        if: ${{ !cancelled() }}
        run: node scripts/check-doc-links.js

      # 受け入れ基準 → テスト写像のトレーサビリティ検査（issue #343 / IADR-0127）。
      # 必須範囲 FR（網羅裁定 #211: FR-10/12/15/19/20）にテストと必須仕様書が存在することを機械的に強制する。
      #
      # 資料再編（ADR-0029 決定 2）で本リポジトリは planning への依存を持たない。テストが参照する
      # FR/UC/SC が計画書へ実在するかの突合（旧 --require-planning。取得用トークンの受け渡しも含む）は、
      # 突合先の planning submodule 自体が撤去されたため恒久的に skip へ倒れる（`planIds()` が
      # null を返す。notice で可視化される）。**本ジョブ自体は存続する** —— 必須 FR のテスト・仕様書の
      # 存在チェック（本検査の主眼）は元から planning 非依存であり、引き続き実効する。
      - name: Check test traceability
        if: ${{ !cancelled() }}
        run: node scripts/check-test-traceability.js

      # trace ブロック検査（project-planning ADR-0029 決定4「trace ブロック規約」の検査の実現手段。
      # 同決定の後続 PR で新設）。docs/**/*.md（.ai-context/ は対象外・凍結記録のため）に置く
      # `<!-- trace: -->` / `<!-- trace-table: -->` の文法・許可キー・値域（FR/UC/SC は
      # `.claude/rules/traceability.repo.md` 宣言レンジ、計画 ADR は同宣言のレンジ、IADR は
      # `.ai-context/adr/` のファイル実在）を検査し、可視本文に計画 ID・IADR・修飾付き issue 参照が
      # 残っていないかも見る。他プロジェクト／他リポジトリの修飾子（`MSP:` 等）は個別名をハードコードせず
      # 「英字+英数字の短縮名 + `:`」という形だけで external とみなす（利用者裁定・ADR-0029 決定9。
      # 実装リポジトリが今後増える前提）。
      - name: Self-test trace block checker
        if: ${{ !cancelled() }}
        run: node scripts/check-trace-blocks.js --self-test
      - name: Check trace blocks
        if: ${{ !cancelled() }}
        run: node scripts/check-trace-blocks.js

      # ナレッジグラフ生成（docs/ の trace / trace-table ブロックと .ai-context/{adr,specs}/ の
      # frontmatter〔related_ids / related_specs / plan_refs〕から組み立てる）。生成物はコミットしない
      # （都度標準出力へ書く。--json / --mermaid は成果物確認用、CI では --check のみを走らせる）。
      # --check は in-repo 実在検査（計画 ID・計画 ADR はレンジ、IADR はファイル実在）。修飾付き参照
      # （`planning:` / `MSP:` 等。個別名はハードコードしない）は external として数のみ数え、
      # specs / related_specs の基準名が解決できないものは計画リポジトリ側の文書等を指し得るため
      # 失敗とはしない（標準出力に注記する）。
      - name: Self-test knowledge graph generator
        if: ${{ !cancelled() }}
        run: node scripts/gen-knowledge-graph.js --self-test
      - name: Check knowledge graph (in-repo existence)
        if: ${{ !cancelled() }}
        run: node scripts/gen-knowledge-graph.js --check

      # 不採用ライブラリ（platform ADR-0030 の棚卸し）の再混入を止める（issue #345 / #351）。
      # 剥がした事実をレビューの記憶に頼ると必ず戻るため、機械的に落とす。移行未完了のものは
      # スクリプトの PENDING に担当 issue 付きで残し、移行完了時に BANNED へ移す。
      - name: Check banned libraries
        if: ${{ !cancelled() }}
        run: node scripts/check-banned-libraries.js

      # FR-19, #425, ADR-0025, IADR-0165: 決済済み資金（settled cash）の代替に使ってはならないブローカー値
      # （MaxTrdQtys.MaxCashBuy / Funds.AvlWithdrawalCash / Funds.MaxWithdrawal）のコード参照を止める。
      # とりわけ**現金買付余力は現金口座では未決済の売却代金を含む**のが通例であり、
      # **分母に据えると GFV 回避ガードが GFV を許可する**。コメントでの注意書きだけでは足りず、
      # 「決済済み資金の供給元」を探した実装者の手が最も近い名前へ伸びる経路を機械的に塞ぐ。
      - name: Check banned settled-cash sources
        if: ${{ !cancelled() }}
        run: node scripts/check-banned-settled-cash-sources.js

      # NFR / #357 / IADR-0168: Wolverine のテストハーネスの**素の入口** `host.TrackActivity()` を止める。
      # 素の入口は**既定 5 秒の壁時計**で打ち切るため、ソリューション全体の並列実行では
      # スケジューリング遅延だけで超え、flaky failure になる（実測 6 秒・#357）。
      # 131 か所を予算つきの入口へ替えても、**次に書かれるテストは標準 API を素直に呼ぶ**。
      # flake は CI ゲートを構造的に無効化する（確率的な赤が「再実行すればよい」を習慣化し、
      # 本物の退行も同じ反応で流される）ため、コメントではなく機械で止める。
      - name: Check tracked session timeout
        if: ${{ !cancelled() }}
        run: node scripts/check-tracked-session-timeout.js

      # NFR / #497: 実装ADR の本文を変更したら .ai-context/adr/README.md の索引行も変更されていること。
      #
      # 同型の事故が連続する 2 PR で 2 回起きた（#491 は自己試験の件数、#495 は解消済みの残余リスク）。
      # いずれも AI レビューが検出し、**CI は緑のまま通した**。運用標準「検査器の追加は同型事故 2 回から」。
      #
      # fetch-depth: 0 が要る —— 差分の範囲（origin/<base>..HEAD）を解決するため。
      # 浅いクローンでは範囲を決められず skip する（その旨を warn で出す）。
      - name: Self-test ADR index sync checker
        if: ${{ !cancelled() }}
        run: node scripts/check-adr-index-sync.js --self-test
      - name: Check ADR index sync
        if: ${{ !cancelled() }}
        run: node scripts/check-adr-index-sync.js

      # NFR / #524 / IADR-0204 の残余リスクの解消: 必読規約の総量予算をエージェントごとに検査する。
      #
      # 予算値 51,200 の正本は計画リポ運用ガイド §8（planning#364）であり、検査器が持つのは複製である
      # （値の隣に出典を書いてある）。母集合は「そのエージェントが自動で読み込む集合」であり、
      # Claude Code（CLAUDE.md ＋ .claude/rules/*.md）と AGENTS.md 系は**合算しない**（合算は誰も
      # 背負わない量を作る。実測で 2 回誤った）。submodule は要らない（母集合に planning/ は入らない）。
      #
      # 超過で fail・90% で warn。**#519 で配線を後回しにした残余リスク**（「検査器はあるが CI に居ない」）
      # を本ジョブで塞ぐ —— 手で叩いたときだけ走る検査器は、予算を守らせない。
      - name: Self-test reading budget checker
        if: ${{ !cancelled() }}
        run: node scripts/check-reading-budget.js --self-test
      - name: Check reading budget (per agent, not summed)
        if: ${{ !cancelled() }}
        run: node scripts/check-reading-budget.js

      # NFR / #477 / IADR-0189: 他プロジェクトの計画 ID が `<PROJ>/<ID>` の形で書かれていることを
      # 検査する（短縮名と ID が空白・TAB 等で離れる形を止める）。検査器は kit 由来である。
      #
      # **ここに違反の実例を書かないこと。** 規約は「意図的な誤例はインラインコードへ入れる」と定めるが、
      # **その除外は Markdown でしか効かない**（YAML のバッククォートはコード記法ではない）。
      # 実際、本コメントの初版が誤例を平文で書いて自分の検査に捕まった。
      #
      # **本ジョブは exit 1 で落とす**（他の一部検査器のような warn ではない）。表記の是正は
      # 人手の判断を伴わず、書式が一意に決まるためである。
      #
      # 🔴 ［2026-08-28 変更・IADR-0262］`PROJECT_PREFIXES` の既定はもう空ではない
      # （`MSP, AST` をファイルへ直書き。IADR-0189 決定2・決定6 を部分的に supersede した）。
      # **ここで env を明示するのは冗長化のため**であり、落としても skip して緑にはならない
      # （`scripts.repo.test.js` に既定へ戻る退行の回帰テストがある）。
      # 判定規則そのものが壊れていれば実データに対する結果は意味を持たない。自己試験を先に走らせる。
      - name: Self-test plan ID qualification checker
        if: ${{ !cancelled() }}
        run: node scripts/check-plan-id-qualification.js --self-test
      - name: Check plan ID qualification
        if: ${{ !cancelled() }}
        env:
          # AST は自プロジェクトだが、本リポは `AST/` を修飾として実際に使うため含める
          # （修飾を使うと決めた以上、表記が一貫していなければ機械的突合の役に立たない）。
          PLAN_ID_PREFIXES: "MSP,AST"
        run: node scripts/check-plan-id-qualification.js

      # 実行環境スキャフォールド（docker-compose / appsettings / .env.example）の静的検査（issue #107 / IADR-0048）。
      # fail-safe 既定（base appsettings が挙動中立・機密は空既定）と構成の存在・整合を Docker 起動なしで検証する。
      # 実コンテナ/実 API 疎通は #82 に分離するため、本ジョブは Node 標準のみ（依存・Docker 不要）で完結する。
      - name: Validate runtime scaffold
        if: ${{ !cancelled() }}
        run: node scripts/validate-runtime-scaffold.js

      # 【任意コンポーネント・本リポジトリは採用】宣言的パイプライン構成のスキーマ検証。
      # 検証器自体の自己試験に加え、取引パイプラインの実宣言を検証する（ADR-0001, IADR-0077, #22）。
      # 宣言が接続性/循環/構造違反へ退行した時点で CI が止める。
      # 検証器自体の試験。構成ファイルの有無に関わらず毎回走らせ、検証ロジックの退行を防ぐ。
      - name: Validate pipeline config (self-test)
        if: ${{ !cancelled() }}
        run: node scripts/validate-pipeline-config.js --self-test
      - name: Validate pipeline config
        if: ${{ !cancelled() && (env.PIPELINE_CONFIG != '') }}
        run: node scripts/validate-pipeline-config.js "$PIPELINE_CONFIG"

      # Claude 系ワークフローのツール許可設定を検査する（安全弁）。
      # claude_args の記法誤りは CI を失敗させずに「AI が検証できない」状態を作るため、
      # 人間にもジョブ結果にも現れない。ここで機械的に止める。
      - name: Check AI workflow config (self-test)
        if: ${{ !cancelled() }}
        run: node scripts/check-ai-workflow-config.js --self-test
      # 警告（検査そのものが成立していない状態）は GitHub のアノテーションとして出るため、
      # ジョブログを開かなくても PR の Checks 画面で気付ける。
      #
      # 本リポジトリはファイル名・ワークフロー構成が固まっている（既定名の 2 ファイルが
      # ともに claude_args を解析できる）ため、警告を失敗として扱う（scripts-tests の
      # REQUIRE_REPO_TESTS と同じ opt-in 方式）。キット既定が fail-open なのは、
      # claude-code-action の入力名が変わったときに全リポジトリの CI が検証器の更新まで
      # 一斉に落ちるのを避けるためである。追随できない事態になったら本 env を外す。
      - name: Check AI workflow config
        if: ${{ !cancelled() }}
        env:
          STRICT_AI_WORKFLOW_CONFIG: "1"
        run: node scripts/check-ai-workflow-config.js
      # Actions のバージョン退行を止める（キット issue planning#148 / planning#152）。
      #
      # **巻き戻りが実際に起きるのはここ（キットを同期した実装リポ）である。** 配布元
      # （project-planning）にも同じ検査があるが、それが守るのは「テンプレートが取り残されて
      # いないか」であり、実装リポがキットのファイルをコピーした瞬間の退行は守らない。
      # 実測: キット pin 0847687 の時点でキットは upload-artifact@v4・本リポは既に @v7 で、
      # 素直にコピーしていれば 3 メジャー分の退行を持ち込んでいた。
      #
      # 2 つの基準で検査する。
      #   - scripts/action-versions.json の下限（キット既定 ＋ action-versions.repo.json の追記）
      #   - --compare-with-ref: **統合ブランチ時点の同じファイル**より下がっていないか
      # 後者が要るのは、本リポが Dependabot で下限より先へ進んだあとにキットをコピーすると、
      # 本リポにとっては退行なのに表の上では合格してしまうためである。
      #
      # 【置換点】本リポの統合ブランチは develop（キット既定は origin/main）。
      # ref を読めない場合（shallow clone 等）は warn を出して素通りする（fail-open）。
      - name: Check action versions
        if: ${{ !cancelled() }}
        run: |
          node scripts/check-action-versions.js \
            --dir .github/workflows \
            --compare-with-ref origin/develop

      # FR-03/FR-10, ADR-0013, IADR-0106, IADR-0129, Issue #258 / #354 再発防止:
      # サービスを跨いで RabbitMQ のキュー名が衝突していないかを機械検査する。
      # #258 では 2 サービスが同一キューを共有し、pub/sub のつもりが competing consumer（取り合い）になって
      # 取引判断が承認・拒否・エラーのいずれにも現れず無言で消えた。
      # Wolverine（#354 で全面移行）はキュー名の導出にハンドラのクラス名を一切使わないため、当時の対策
      #（クラス名をサービス跨ぎで一意にする）は効かない。キュー名は <ServiceName>.<メッセージ型名> であり、
      # 一意性は ServiceName の一意性へ帰着する。よって検査するのは
      #   N1 ServiceName の一意性 / N2 トポロジの直接指定の禁止 / N3 共通ヘルパ経由でのみ Wolverine を配線する
      # ＋ メタ検査（走査したサービス数・Wolverine 配線サービス数の下限＝検査の空振り防止）である。
      # 旧 MassTransit 規則は移行完了に伴い #354 第 3 段階で撤去した（再混入は check-banned-libraries.js が止める）。
      # 外部依存ゼロ（Node のみ）。--self-test で検査器自体も毎回試験する。
      # 🔴 **この 2 本は「常に走るジョブ」に置かねばならない**（IADR-0208 決定 12）。
      # 以前は detect-changed-areas が `changes` ジョブ（廃止した）、
      # list-test-projects が `backend-test`（backend 非変更 PR では中身が skip される）に
      # 居た。そのままだと**判定器と分配器の自己試験が走らなくなる** ——
      # どちらも黙って壊れると「skip する」「テストが減る」側へ倒れるため、
      # **自己試験が走らなくなること自体が事故**である。
      - name: Self-test changed-areas detector
        if: ${{ !cancelled() }}
        run: node scripts/detect-changed-areas.js --self-test
      - name: Self-test test-project sharder
        if: ${{ !cancelled() }}
        run: node scripts/list-test-projects.js --self-test
      # docs/ai-workflow.md が挙げる check 名・ジョブ名を .github/workflows/ の実物と突き合わせる
      # （NFR / IADR-0208 決定 14。同型の乖離が 2 回起きたため新設 ——
      # 1 回目は基盤リポの scripts/README.md、2 回目は本リポの ai-workflow.md が
      # `doc-links` / `pipeline-config` / `ai-workflow-config`〔いずれも static-checks へ統合済み〕
      # を挙げたまま残っていた）。
      # 🔴 実在しない check 名をブランチ保護へ指定すると、report されない check を待ち続けて
      # develop が恒久的にマージ不能になる。文書自身がその失敗形を警告しながら破っていた。
      - name: Self-test workflow job reference checker
        if: ${{ !cancelled() }}
        run: node scripts/check-workflow-job-refs.js --self-test
      - name: Check workflow job references in docs
        if: ${{ !cancelled() }}
        run: node scripts/check-workflow-job-refs.js

      - name: Self-test consumer endpoint name checker
        if: ${{ !cancelled() }}
        run: node scripts/check-consumer-endpoint-names.js --self-test
      - name: Check queue name uniqueness across services
        if: ${{ !cancelled() }}
        run: node scripts/check-consumer-endpoint-names.js

      # NFR-07, NFR-13, issue #287, IADR-0255: 業務メトリクスとダッシュボードの乖離検査。
      # 🔴 乖離は**エラーを出さない** —— 系列名がずれたパネルは空のグラフを描き、空のグラフは
      # 「異常が起きていない」と読める。**監視しているつもりで何も見ていない**状態が続くため、
      # (a) ダッシュボードが引く ast_* 系列がコード側のレジストリに実在すること、
      # (b) レジストリの各計器を少なくとも 1 枚のダッシュボードが引いていること、
      # (c) dev の otel-collector が metrics を debug（標準出力のみ）にしか出さないこと
      #     ＝既定では計装が有効でも外部へ送らない（opt-in の作法）
      # を機械で止める。
      - name: Self-test observability asset checker
        if: ${{ !cancelled() }}
        run: node scripts/check-observability-assets.js --self-test
      - name: Check observability assets (dashboards vs instruments)
        if: ${{ !cancelled() }}
        run: node scripts/check-observability-assets.js

      # MSP/ADR-0069 決定 5・#663: frontend/src 配下に「.gitkeep のみのディレクトリ」が
      # 無いことを検査する。MSP/ADR-0069 は MSP/ADR-0065 決定 4（バックエンド 8 要素標準の
      # `.gitkeep` 枠置きの撤回）と同じ理由（枠が「適合の見え方」を作る）がフロントエンドにも
      # 及ぶと定め、本リポジトリの IADR-0290 決定 2（枠置きの採用）を覆した。17 件の枠を撤去した
      # 状態を固定し、新しい枠の再導入を機械で止める。
      - name: Self-test frontend empty-frame checker
        if: ${{ !cancelled() }}
        run: node scripts/check-frontend-empty-frames.js --self-test
      - name: Check frontend has no .gitkeep-only directories
        if: ${{ !cancelled() }}
        run: node scripts/check-frontend-empty-frames.js


  lint:
    runs-on: ubuntu-latest
    # 🔴 NFR / IADR-0208 決定 8: changelog の自動 PR（automation/*）では回さない。
    # 実測で automation/changelog-update-develop の PR が CI を毎回起動し、
    # 6 件以上が action_required のまま積み上がっていた。生成物だけの PR に
    # restore / build / test は要らない。
    #
    # ⚠️ 必須チェックのジョブへ if: を付けてよい根拠: **`if` でスキップされたジョブは
    # 必須チェック上「合格」として扱われる**（docs/ai-workflow.md に明記）。
    # `paths:` でワークフローごと起動しない場合と違い、恒久 pending にはならない。
    # claude-code-review.yml が既に同型の除外条件を持っている（:69）。
    #
    # NFR / IADR-0208 決定 11: backend に影響しない変更だけの PR では回さない
    # （dotnet format は backend しか見ない）。判定は changes ジョブが持つ。
    #
    # 🔴 **`needs:` を持たない。** 上流ジョブの失敗で skip され、
    # **必須チェックが黙って「合格」になる**経路を構造的に断つため（決定 12）。
    if: ${{ !startsWith(github.head_ref, 'automation/') }}
    steps:
      # 変更領域の判定（IADR-0208 決定 12）。`--git` が HEAD^1..HEAD の差分を自分で取る。
      # 🔴 差分が取れなければ判定器は **backend=true（走らせる）へ倒す**。落とさない。
      - uses: actions/checkout@v7
        with:
          fetch-depth: 2 # HEAD^1 の解決に必要な最小。pull_request では HEAD^1 が base である
      - name: Gate
        id: gate
        run: node scripts/detect-changed-areas.js --git
      # --- スタック別に書き換え（例: C#/.NET） ---
      # 🔴 キャッシュは**ブランチ単位でスコープされる**（実測 2026-08-21）。
      # ブランチは**自分の base ブランチのキャッシュを読める**ので、develop が温まっていれば
      # **新規 PR ブランチは初回からヒットする**。逆に **develop へマージ直後の初回 run は miss** する。
      # **miss を見たら壊れたと決めつけず、どのブランチのスコープかを先に確かめること。**
      #
      # NFR / IADR-0208: NuGet のグローバルパッケージをキャッシュする。着手前の実測では
      # setup-dotnet が本リポジトリで 6 箇所使われて 1 つも cache: を設定しておらず、
      # 101 個の csproj を毎 run コールドで restore していた（Restore 20 秒 ＋ Build 47 秒）。
      # packages.lock.json を持たないため setup-dotnet の cache: true は使えない
      # （ロックファイルのハッシュをキーにする実装のため）。
      - uses: actions/cache@v6
        if: ${{ steps.gate.outputs.backend == 'true' }}
        with:
          path: ~/.nuget/packages
          key: nuget-${{ runner.os }}-${{ hashFiles('Directory.Packages.props', '**/*.csproj') }}
          restore-keys: nuget-${{ runner.os }}-
      - uses: actions/setup-dotnet@v6
        if: ${{ steps.gate.outputs.backend == 'true' }}
        with:
          dotnet-version: "10.0.x"
      - name: Format check
        if: ${{ steps.gate.outputs.backend == 'true' }}
        run: dotnet format backend/backend.slnx --verify-no-changes

  # 🔴 テストのシャーディング（NFR / IADR-0208 決定 10）。
  #
  # 着手前の実測: build-and-test 208 秒 ＝ Restore 13 / Build 43 / **Test 133** / セットアップ 19。
  # テストアセンブリ 51 本・所要の合計 480 秒・実効並列度 3.61 本・**最長 28 秒**。
  # 決定 8 で確かめたとおり **1 ランナー内は既に 4 並列で頭打ち**であり、
  # これ以上縮めるには**ランナーを増やす**しかない。
  #
  # ⚠️ **このジョブは必須チェックではない。** 必須チェック名 `build-and-test` は
  # 下の集約ジョブが保持する（改名は恒久的なマージ不能を招く）。
  backend-test:
    # 🔴 **`needs:` を持たない**（決定 12）。直列の前置きを消し、シャードを即座に起動させる。
    # backend 非変更なら下のステップが全部 skip され、ジョブは数十秒で success になる。
    if: ${{ !startsWith(github.head_ref, 'automation/') }}
    runs-on: ubuntu-latest
    strategy:
      # 🔴 fail-fast を切る。1 シャードが落ちても他を走り切らせ、
      # **失敗の全体像を 1 回の run で見る**（赤が入れ替わって何周もするのを避ける）。
      fail-fast: false
      matrix:
        # 🔴 **シャード数を本文へ焼き込むのはここ 1 箇所だけにする**（下の `--of` と対で読む）。
        # 7 → 4（NFR / IADR-0257）。分配が LPT になり重みを見るようになったため、
        # 脚を増やしても makespan は理想分配より下がらない一方、
        # **`dotnet build backend.slnx` は脚ごとに丸ごと重複する**（`--no-build` はテスト時のみ）。
        # 脚を減らした分だけ runner 時間が直接減る。
        shard: [1, 2, 3, 4]
    steps:
      # 変更領域の判定（決定 12）。`--git` が HEAD^1..HEAD の差分を自分で取る。
      # 🔴 差分が取れなければ判定器は **backend=true（走らせる）へ倒す**。落とさない。
      - uses: actions/checkout@v7
        with:
          fetch-depth: 2 # HEAD^1 の解決に必要な最小。pull_request では HEAD^1 が base である
      - name: Gate
        id: gate
        run: node scripts/detect-changed-areas.js --git
      # 🔴 キャッシュは**ブランチ単位でスコープされる**（実測 2026-08-21）。
      # base が温まっていれば新規 PR ブランチは初回からヒットする。
      # develop へマージ直後の初回 run が miss するのは壊れているのではない。
      - uses: actions/cache@v6
        if: ${{ steps.gate.outputs.backend == 'true' }}
        with:
          path: ~/.nuget/packages
          key: nuget-${{ runner.os }}-${{ hashFiles('Directory.Packages.props', '**/*.csproj') }}
          restore-keys: nuget-${{ runner.os }}-
      - uses: actions/setup-dotnet@v6
        if: ${{ steps.gate.outputs.backend == 'true' }}
        with:
          dotnet-version: "10.0.x"
      # ⚠️ ここも setup-node を置かない（実測 4〜9 秒）。`list-test-projects.js` は依存を持たない
      # 素の Node スクリプトで、ランナー既定の Node で動く。**このジョブが必須チェックの
      # クリティカルパスそのもの**なので、数秒がそのまま待ち時間になる。
      # static-checks でも走らせているが（そちらが「常に走る」担保）、ここでも走らせる。
      # **分配が壊れるとテストが黙って減る**ため、使う直前でも確かめる。1 秒未満である。
      - name: Self-test (分配ロジック)
        if: ${{ steps.gate.outputs.backend == 'true' }}
        run: node scripts/list-test-projects.js --self-test
      - name: Restore
        if: ${{ steps.gate.outputs.backend == 'true' }}
        run: dotnet restore backend/backend.slnx
      - name: Build
        if: ${{ steps.gate.outputs.backend == 'true' }}
        run: dotnet build backend/backend.slnx --no-restore --configuration Release
      # NFR, #616: 11 Worker の publish を乾式実行する（shard 1 のみ・--no-build＝ファイルコピーだけ）。
      # VSA 移送でサービス本体（Web SDK）同士の参照ができ、参照先の appsettings*.json が publish 出力へ
      # 流れ込んで NETSDK1152 で落ちる退行を、build/test だけの CI が見逃していた。対象は
      # scripts/k8s-local-images.sh の MAPPING と同じ 11 本（Dockerfile と同じ publish を行う）。
      - name: Publish (dry run)
        if: ${{ steps.gate.outputs.backend == 'true' && matrix.shard == 1 }}
        run: |
          set -euo pipefail
          for csproj in backend/Services/*/*.csproj; do
            dotnet publish "$csproj" --no-build --no-restore --configuration Release               -o "${RUNNER_TEMP}/publish/$(basename "$csproj" .csproj)"
          done
      - name: Test (shard ${{ matrix.shard }} / 4)
        id: test
        if: ${{ steps.gate.outputs.backend == 'true' }}
        env:
          # NFR, #596 フォローアップ / IADR-0277 §結果フォローアップ・[2026-09-03 追記]:
          # `--diag` 単体の寄与を A/B 計測した結果、既定は無効にした（切り替え口は残す）。
          # `"1"` にした PR を出せば再現できる。
          VSTEST_DIAG: ""
        run: |
          set -euo pipefail
          # 🔴 分配は **LPT 貪欲**（重み＝走査で数えた `[Fact` / `[Theory` の件数）。
          # 重みは JSON へ持たず**毎 run 走査で導く**（IADR-0257 決定 2）——
          # コミットした重みは必ず実体とずれ、**ずれても誰も気付かない**。
          node scripts/list-test-projects.js --shard ${{ matrix.shard }} --of 4 --slnx shard.slnx > shard.txt
          echo "--- このシャードの担当 $(wc -l < shard.txt) 件 ---"
          cat shard.txt
          # NFR, #596: 実行の前後で空きメモリと並列度を記録する。
          # 🔴 「メモリ枯渇でテストホストが落ちた」という仮説は実測で棄却済みだが（IADR-0277）、
          # **次に同じ疑いが出たとき推定ではなく実測で棄却できるようにする**。出力は数行で費用が無い。
          echo "--- 実行前: nproc=$(nproc) ---"
          free -m
          # 成否によらず実行後も記録する（落ちたときこそ知りたい値である）。
          trap 'echo "--- 実行後 ---"; free -m || true' EXIT
          # 🔴 **シャード用 solution を 1 回だけ叩く。** 1 プロジェクトずつ回すと
          # **1 回あたり約 4.9 秒の起動コスト**（MSBuild 評価・テストホスト立ち上げ）が
          # 本数だけ積み上がる —— 実測で較正した値であり、51 本なら**約 250 秒**の無駄になる。
          # solution にまとめれば起動はシャードごとに 1 回で済み、
          # **プロジェクト間の 4 並列は MSBuild が従来どおり面倒を見る**（分割前と同じ経路）。
          #
          # NFR, #596 / IADR-0277 —— ここに足した 3 つの計装:
          #   --logger trx : **失敗したテストを機械で名指しできる唯一の出力**。並列実行では
          #     各 VSTest のコンソールロガーが同じ標準出力へ同期せずに書くため、サマリ塊が
          #     他プロジェクトの逐次出力に割り込まれて分断される。実測では `Test Run Failed.` が
          #     `Total tests:` と `Passed:` の間へ入り込み、**ログ末尾に最も近いサマリは別
          #     プロジェクトの `Test Run Successful.`** だった。人は必ず読み違える。
          #   --blame-crash / --blame-hang : **本物のホスト異常終了が起きたときだけ**
          #     Sequence.xml と dump を残す（何も起きなければ書かないので定常の費用はほぼ無い）。
          #     #596 が求めた保険はここに掛かる。
          #   --diag : VSTest の診断ログ。失敗時のみ artifact として上げる。
          #     🔴 [2026-09-03 追記] A/B 計測の結果、既定は無効（`VSTEST_DIAG` が空）にした
          #     （NFR, #596 フォローアップ / IADR-0277 §結果フォローアップ）。狙う仮説
          #     （テストホストの異常終了）は IADR-0277 の調査で実測により棄却済みであり、
          #     計装は「棄却済みの仮説のために毎 run 払う」形になっていた。取り外さず
          #     切り替え口として残す（`VSTEST_DIAG=1` で再現できる）。
          # 🔴 `-m:1`（プロジェクト間並列の停止）は入れない —— 恒久策として遅く、かつ
          # 原因が並列度ではないことが実測で判明している（IADR-0277）。
          diag_args=()
          if [ -n "${VSTEST_DIAG}" ]; then
            diag_args=(--diag "${RUNNER_TEMP}/vstest-diag/log.txt")
          fi
          dotnet test shard.slnx --no-build --configuration Release \
            --filter "Category!=Integration" \
            --collect:"XPlat Code Coverage" \
            --results-directory "$PWD/cov" \
            --logger trx \
            --blame-crash --blame-hang --blame-hang-timeout 10m \
            "${diag_args[@]}" \
            --verbosity normal
      # 🔴 **失敗したテストを名指しする**（NFR, #596 / IADR-0277）。
      # TRX は構造化データなので並列の割り込みを受けない。ここを読めば
      # 「テスト失敗 0 件なのに exit 1」という誤診は起こらない。
      # 要約器は 🔴 **TRX を 1 件も読めなければ落ちる**（0 件検査で緑にしない）し、
      # 🔴 **TRX はあるが失敗 0 件のときは「ホスト異常終了を疑え」と次に見る成果物を挙げる**。
      # `steps.test.outcome` で条件を絞る —— Test より前（Build 等）で落ちた場合に
      # TRX が無いのは当然であり、そこで fail-loud を鳴らすと信号が濁る。
      - name: Summarize test failures
        if: ${{ always() && steps.test.outcome == 'failure' }}
        run: node scripts/summarize-test-failures.js cov
      # 診断成果物（VSTest 診断ログ・blame の Sequence.xml / dump・TRX）は**失敗時のみ**残す。
      # 常時上げると保管と転送を毎 run 払うことになる（IADR-0208 のレイテンシ・費用の方針）。
      - name: Upload test diagnostics
        if: ${{ failure() && steps.gate.outputs.backend == 'true' }}
        uses: actions/upload-artifact@v7
        with:
          name: test-diagnostics-shard-${{ matrix.shard }}
          path: |
            ${{ runner.temp }}/vstest-diag/**
            cov/**/*.trx
            cov/**/*Sequence.xml
            cov/**/*.dmp
          retention-days: 3
          if-no-files-found: warn
      # 🔴 **1 テストプロジェクト = 1 カバレッジレポート**の不変条件で、
      # 「走らなかった」と「二重に走った」を**両方**捕まえる。
      # 第 1 弾では同じテストプロジェクトが 2 回走ってレポートが 30 件になり、
      # カバレッジが 20% へ半減した（実測）。件数を数えるのはそのときの教訓である。
      - name: Verify report count
        if: ${{ steps.gate.outputs.backend == 'true' }}
        run: |
          set -euo pipefail
          want=$(wc -l < shard.txt)
          # 🔴 **深さ 2 に限定する**（NFR, #596 / IADR-0277）。XPlat Code Coverage の収集器は
          # `cov/<guid>/coverage.cobertura.xml` へ**テストプロジェクト 1 本につき 1 件**書く。
          # 一方 **TRX ロガーは添付をすべて deployment root へ複製する** ——
          # `cov/<user>_<machine>_<timestamp>/In/<machine>/coverage.cobertura.xml` が増える。
          # 深さを見ずに数えると**同じレポートを二重に数える**（実測: 期待 6 件に対し 11 件）。
          # この不変条件が無ければ、複製されたレポートがそのまま artifact へ載っていた。
          got=$(find cov -mindepth 2 -maxdepth 2 -name coverage.cobertura.xml | wc -l)
          echo "期待 $want 件 / 実際 $got 件"
          if [ "$want" -ne "$got" ]; then
            echo "::error::シャード ${{ matrix.shard }}: カバレッジレポートが $want 件でなく $got 件である。テストが走っていないか二重に走っている。"
            exit 1
          fi
      - uses: actions/upload-artifact@v7
        if: ${{ steps.gate.outputs.backend == 'true' }}
        with:
          name: coverage-shard-${{ matrix.shard }}
          # 🔴 `**` ではなく `*`（NFR, #596 / IADR-0277）。TRX ロガーが deployment root へ
          # 複製した `.../In/<machine>/coverage.cobertura.xml` を**載せないため**である。
          # 🔴 こちらの誤りは**上の不変条件と違って赤くならない** —— 集計は (ファイル, 行番号) の
          # 和集合なので率は変わらず、**重複を黙って運ぶだけ**になる。だから glob 側にも書く。
          path: cov/*/coverage.cobertura.xml
          retention-days: 3
          if-no-files-found: error

  # 集約ジョブ。🔴 **必須チェック名 `build-and-test` はここが保持する。**
  # 脚（backend-test）の成否をまとめ、カバレッジ床をここ 1 箇所で強制する。
  build-and-test:
    # 🔴 ここだけ `needs:` を残す —— **脚の成否を知る必要がある**ため。
    # したがって `always()` ＋ Gate が必須である（決定 12）。
    needs: [backend-test]
    # 🔴 always() が要る。脚が skip / 失敗しても集約は必ず report しなければ、
    # 必須チェックが恒久 pending になりマージできなくなる。
    if: ${{ always() && !startsWith(github.head_ref, 'automation/') }}
    runs-on: ubuntu-latest
    steps:
      # 🔴 判定を最初に置く。「backend 非変更だから緑」と「脚が落ちたのに緑」を
      # 取り違えないよう、理由を必ず出力する。
      # 🔴 脚の成否を最初に見る。**cancelled / failure を緑として通さない。**
      # 実測で、実行中の run を後続 push がキャンセルしたとき、この門が正しく赤くした。
      - name: Gate (脚の成否)
        run: |
          set -euo pipefail
          case "${{ needs.backend-test.result }}" in
            success) echo "全シャードが成功した（または backend 非変更で中身が skip された）。" ;;
            *) echo "::error::backend-test が ${{ needs.backend-test.result }} である。"; exit 1 ;;
          esac
      - uses: actions/checkout@v7
        with:
          fetch-depth: 2 # HEAD^1 の解決に必要な最小
      # 🔴 集約ジョブも**自前で**判定する（決定 12）。脚が「中身を skip して success」なのか
      # 「実際に走って success」なのかは needs の result では区別が付かないためである。
      # 同じコミット・同じ判定器なので脚と食い違わない。
      - name: Gate (backend 変更の有無)
        id: gate
        run: node scripts/detect-changed-areas.js --git
      # ⚠️ ここも setup-node を置かない（実測 5 秒。check-coverage.js は素の Node スクリプトで
      # 依存を持たず、ランナー既定の Node で動く）。集約ジョブは必須チェックの最後尾なので、
      # ここの数秒はそのまま待ち時間になる。
      # 全シャードのレポートを 1 箇所へ集める。merge-multiple で同名パスも共存させる。
      - uses: actions/download-artifact@v8
        if: ${{ steps.gate.outputs.backend == 'true' }}
        with:
          pattern: coverage-shard-*
          path: cov
          merge-multiple: true
      # カバレッジ floor（下限）の強制（issue #343 / IADR-0127）。閾値は coverage-floor.json。
      # 🔴 集計は **(ファイル, 行番号) の和集合**で行うため、シャード間でレポートが
      # 分かれていても分母・分子は変わらない（重複計上も起きない）。
      # 引き上げ（ratchet）は自動では行わない（--suggest が候補値を出すのみ。更新は人手の PR）。
      - name: Check coverage floor
        if: ${{ steps.gate.outputs.backend == 'true' }}
        run: |
          set -euo pipefail
          n=$(find cov -name coverage.cobertura.xml | wc -l)
          echo "集めたカバレッジレポート: $n 件"
          # 🔴 0 件で床検査を通さない（「何も測っていないのに緑」を作らない）。
          if [ "$n" -eq 0 ]; then
            echo "::error::カバレッジレポートが 1 件も無い。シャードの artifact 受け渡しを疑うこと。"
            exit 1
          fi
          node scripts/check-coverage.js --suggest --root cov

      # 参考: 他スタックの例
      # Node.js:
      #   - uses: actions/setup-node@v7
      #     with: { node-version: "20" }
      #   - run: npm ci
      #   - run: npm run lint --if-present
      #   - run: npm run build --if-present
      #   - run: npm test -- --coverage
      # Python:
      #   - uses: actions/setup-python@v7
      #     with: { python-version: "3.12" }
      #   - run: pip install -e .[test]
      #   - run: ruff check .
      #   - run: pytest --cov

  # フロントエンド（frontend/）の型検査・lint・単体/コンポーネントテスト（issue #106 / FR-13,FR-17,UC-06 / IADR-0080）。
  # 単独リポでは @foundation をテスト/型検査用スタブへ解決し、platform 非依存で自己完結する（合成時は platform の
  # src/vitest.config.ts が実 foundation へ解決して同じテストを走らせる）。実ブラウザ E2E（Playwright）・実 BFF/Keycloak
  # 疎通は実基盤依存として後続スライスへ分離するため、本ジョブは Node のみ（Docker 不要）で完結する。
  frontend:
    runs-on: ubuntu-latest
    # 🔴 NFR / IADR-0208 決定 8: changelog の自動 PR（automation/*）では回さない。
    # 実測で automation/changelog-update-develop の PR が CI を毎回起動し、
    # 6 件以上が action_required のまま積み上がっていた。生成物だけの PR に
    # restore / build / test は要らない。
    #
    # ⚠️ 必須チェックのジョブへ if: を付けてよい根拠: **`if` でスキップされたジョブは
    # 必須チェック上「合格」として扱われる**（docs/ai-workflow.md に明記）。
    # `paths:` でワークフローごと起動しない場合と違い、恒久 pending にはならない。
    # claude-code-review.yml が既に同型の除外条件を持っている（:69）。
    if: ${{ !startsWith(github.head_ref, 'automation/') }}
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install
        run: npm ci
      - name: Typecheck
        run: npm run typecheck
      - name: Lint
        run: npm run lint
      - name: Test
        run: npm run test

  # フロント3画面（SC-01/02/03）の Playwright E2E（issue #187 / IADR-0087）。実ブラウザ（chromium）で UI フロー
  # （表示・保存の 200/400/409 出し分け・権限外の存在秘匿・縮退）を検証する。BFF 応答はモック（page.route）で、
  # 実 BFF/Keycloak/クラスタ疎通には依存させない（実基盤 live 検証は #82 系／MSP#284 へ分離）。Docker 不要・frontend/ に閉じる。
  frontend-e2e:
    runs-on: ubuntu-latest
    # 🔴 NFR / IADR-0208 決定 8: changelog の自動 PR（automation/*）では回さない。
    # 実測で automation/changelog-update-develop の PR が CI を毎回起動し、
    # 6 件以上が action_required のまま積み上がっていた。生成物だけの PR に
    # restore / build / test は要らない。
    #
    # ⚠️ 必須チェックのジョブへ if: を付けてよい根拠: **`if` でスキップされたジョブは
    # 必須チェック上「合格」として扱われる**（docs/ai-workflow.md に明記）。
    # `paths:` でワークフローごと起動しない場合と違い、恒久 pending にはならない。
    # claude-code-review.yml が既に同型の除外条件を持っている（:69）。
    if: ${{ !startsWith(github.head_ref, 'automation/') }}
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install
        run: npm ci
      # NFR / IADR-0208: Playwright のブラウザをキャッシュする。実測で毎回 31 秒掛かっていた
      # （e2e:install = playwright install --with-deps chromium。apt-get も叩く）。
      # キーは package-lock.json のハッシュ（= @playwright/test の版が変われば取り直す）。
      - uses: actions/cache@v6
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('frontend/package-lock.json') }}
      # ヒット時もシステム依存（apt）の導入だけは要るため --with-deps は落とさない。
      # ブラウザ本体はキャッシュから復元され、ダウンロードが省かれる。
      - name: Install Playwright browser (chromium)
        run: npm run e2e:install
      - name: E2E typecheck
        run: npm run e2e:typecheck
      - name: E2E (Playwright)
        run: npm run e2e
      - name: Upload Playwright report on failure
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-report
          path: frontend/playwright-report/
          retention-days: 7

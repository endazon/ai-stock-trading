# gitleaks 誤検知の無効化（fingerprint 指定）。実鍵は混入していない。
#
# deploy/opend/k8s/rsa-secret.example.yaml は RSA 秘密鍵 Secret の「例」（プレースホルダ・実鍵なし・#124/#13）。
# commit 8b9210e で説明のため PEM マーカ行（BEGIN/END RSA PRIVATE KEY）を含めたため gitleaks の
# private-key ルールが検知した。マーカ行は後続コミットで除去済みだが、gitleaks は commit 範囲（履歴）を
# 走査するため当該コミットの finding を fingerprint で無効化する（履歴は書き換えない＝force-push しない方針）。
8b9210e355f2cf83c455e67ecfcfe6884198f0c0:deploy/opend/k8s/rsa-secret.example.yaml:private-key:22
#
# #245: Discord Bot の環境固有 ID（GuildId/ChannelId）を values 経路で渡す手順を書いた際、README・作業仕様書の
# **ダミー**の 18 桁 ID（123456789012345678 等）を gitleaks の discord-client-id ルールが検知した。Discord の
# サーバー/チャンネル ID は**認証情報ではない**うえ、当該値は実在しないダミーである（実 ID はコミットせず
# DISCORD_BOT_* env で与える設計＝IADR-0102）。現行ツリーの例はプレースホルダ（`<ギルドID>` 等）へ置換済みだが、
# gitleaks は commit 範囲（履歴）を走査するため当該コミットの finding を fingerprint で無効化する
# （履歴は書き換えない＝force-push しない方針）。
ff2291a26e121e7898febab61166b502d2da51a1:deploy/helm/ai-stock-trading/README.md:discord-client-id:83
ff2291a26e121e7898febab61166b502d2da51a1:deploy/helm/ai-stock-trading/README.md:discord-client-id:84
687f4bdfa391aa340ed3e2e8de49e1004583221e:docs/specs/20260725_245_discord-ids-via-values.md:discord-client-id:109
#
# #280: 報告書の方針テストで、報告書の**自然キー**（`weekly-2026-W31`）を名前付き引数
# `parentPeriodKey:` へ直に渡した行を gitleaks の generic-api-key ルールが検知した（キーワード "key" ＋
# エントロピー 3.5）。当該値は認証情報ではなく、ReportPeriod.ExpectedKey が導出する期間表記そのものである。
# 現行ツリーでは定数（ParentWeekly）へ括り出して同型の記述を解消済みだが、gitleaks は commit 範囲（履歴）を
# 走査するため当該コミットの finding を fingerprint で無効化する（履歴は書き換えない＝force-push しない方針）。
01c54d386770c93be9727fd9c8e18085bb63844a:backend/Services/ReportService/tests/ReportService.Domain.Tests/ReportPolicyDraftTests.cs:generic-api-key:30
#
# #308: 上と同型（報告書の自然キーの誤検知）。散文 LLM のタイムアウトを種別ごとに検証するテストで
# `PeriodKey = "weekly-…"` / `"monthly-…"` と書いた行を generic-api-key ルールが検知した。
# 現行ツリーでは定数（WeeklyPeriod / MonthlyPeriod）へ括り出して同型の記述を解消済みだが、
# gitleaks は commit 範囲（履歴）を走査するため当該コミットの finding を fingerprint で無効化する。
121e18b94f63ea84e0df10873214b7af6114d219:backend/Services/ReportService/tests/ReportService.Worker.Tests/HttpReportNarrativeDrafterTests.cs:generic-api-key:99
121e18b94f63ea84e0df10873214b7af6114d219:backend/Services/ReportService/tests/ReportService.Worker.Tests/HttpReportNarrativeDrafterTests.cs:generic-api-key:118

# #667 / IADR-0294: 設定キー名 `MarketData:Finnhub:EstimatedSymbolCount` が finnhub-access-token 規則（Finnhub: に続く英数 20 字）に
# 誤検知される。値ではなくキー名であり秘密ではない。履歴走査のため当該コミットの finding を fingerprint で無効化する。
53aaa5eab5416408d70f41716c88ac4d3cfab235:.ai-context/adr/README.md:finnhub-access-token:324
53aaa5eab5416408d70f41716c88ac4d3cfab235:.ai-context/specs/20260903_667_finnhub-daily-volume-control.md:finnhub-access-token:54
53aaa5eab5416408d70f41716c88ac4d3cfab235:.ai-context/specs/20260903_667_finnhub-daily-volume-control.md:finnhub-access-token:85
cf093d22f9f30fb286bbfacf3eab696a766397e7:deploy/helm/ai-stock-trading/README.md:finnhub-access-token:101

---
title: ADR-0023 が定めていない米国株日足 OHLC の代替源として moomoo が使えることの環流
type: plan-feedback
status: accepted
category: 要求の不足
related_ids: [FR-15, FR-20, ADR-0005, ADR-0019, ADR-0023]
source_repo: ai-stock-trading
source_ref: chore/ADR-0019-moomoo-poc-plan（作業仕様書 docs/specs/20260805_342_moomoo-poc-plan.md・IADR-0144 決定6）
author: Claude Code（実機確認セッション）／endazon（実機実行）
created: 2026-08-05
updated: 2026-08-06
dispatched: true
planning_issue: 205
---

# フィードバック: 米国株日足 OHLC の履歴源として moomoo を使える

> **✅ 裁定済み（2026-08-06）。本記録が求めた §提案 1 は採択された。**
> [ADR-0023](https://github.com/endazon/project-planning/blob/main/projects/ai-stock-trading/07_adr/ADR-0023_us-daily-ohlc-history-source.md)
> に **決定5「米国株の日足 OHLC 履歴源は moomoo OpenAPI の履歴 K 線とする」**が追加された
> （利用者裁定・環流 [project-planning#205](https://github.com/endazon/project-planning/issues/205)。
> planning `e36b592`）。**§提案 4（単一障害点の集中）も「許容する」と裁定された。**
> **§提案 2・3（取得枠の単位と回復周期／前復権と費用モデルの整合）は決定5 の「実装側で確認を要する 2 点」として
> 計画本文に取り込まれ、「本決定の前提であり、確認するまで本番のバックテストへ流さない」と明記された。**
> 実装側の追随は末尾の「追記（2026-08-06・後刻）」を参照。

## 種別

要求の不足。[ADR-0023](https://github.com/endazon/project-planning/blob/main/projects/ai-stock-trading/07_adr/ADR-0023_us-daily-ohlc-history-source.md) は Stooq が取得不能であることと回避実装の禁止を決めたが、**代替源を定めていない**。実測の結果、moomoo がその答えになる。

## 起点となる計画書

- 機能要求（FR）: FR-15（バックテスト）・FR-20（段階ゲート・Stage 0 の合格判定）
- 関連 ADR: **ADR-0023**（米国株日足 OHLC 履歴源）・ADR-0005（有料情報源の判断プロセス）・ADR-0019（moomoo PoC）
- 実装側の記録: `docs/specs/20260805_342_moomoo-poc-plan.md`／`docs/adr/IADR-0144_moomoo-short-selling-poc-outcomes.md` 決定 6

## 現状（As-Is）

- ADR-0023 決定 1 は Stooq が JavaScript proof-of-work のボット検知により実質的に取得不能であることを認め、**回避実装を明示的に禁止**した。
- **代替源は定めていない。** 実装側では `StooqHistoricalBarSource` が唯一の履歴プロバイダであり、既定は `none`（no-op）で安全側に倒れている。
- 結果として **FR-15 のバックテストに流せるデータが 1 件も無く、Stage 0 の合格判定が構造的に成立しない**。実装側の `IADR-0138`（Stage 0 の最大 DD 許容値を 0.15 → 0.10 へ厳格化）も「実効は #382 の解決に依存する」と記録しており、**一度も発火しない統制**になっている。

## 問題点 / あるべき姿（To-Be）

**moomoo OpenAPI の `QotRequestHistoryKL` で米国株の日足 OHLCV が取得できる。** 2026-08-05 に実測した。

```
KLQUOTA   retType=0  usedQuota: 0  remainQuota: 300
HISTKL    begin=2015-01-01  件数=1000  hasNextKey=True
   BAR 2015-01-02  o=24.648455279 h=24.659519313 l=23.75448132 c=24.192617072 v=212818504
HISTKL    begin=2005-01-01  →  2006-07-24 から返る
   BAR 2006-07-24  o=1.833753777 h=1.858898295 l=1.808908598 c=1.838543209 v=722988112
```

| 確認事項 | 結果 |
| --- | --- |
| 日足 OHLCV が取れるか | ✅ `KLType_Day` ／ `RehabType_Forward`（前復権） |
| 遡れる期間 | AAPL で **2006-07-24 まで**（約 20 年） |
| 1 リクエストの上限 | **1,000 件**。`NextReqKey` によるページングで継続できる |
| 取得枠 | `remainQuota: 300`（OpenD 起動ログの `Historical Candlestick Quota: 300` と一致） |
| 費用 | **追加費用なし**（既に接続している OpenD で取得できる。ADR-0005 の有料情報源プロセスに乗せる必要が無い） |

**あるべき姿**: ADR-0023 に「代替源は moomoo OpenAPI の履歴 K 線とする」旨の決定を追加する。**既にブローカとして採用済みであり、新たな契約も費用も生じない。**

### ただし未確定事項が 2 つある

1. **取得枠の単位と回復周期。** `remainQuota: 300` が「銘柄数」なのか「リクエスト数」なのかが分からない。本 probe は 2 リクエストを投げたが `usedQuota` は 0 のままだった（照会時点のスナップショットである可能性がある）。**バックテストで多数銘柄を遡ると枠を使い切る可能性があり、本実装前に確認が要る。**
2. **前復権（`RehabType_Forward`）の調整方式がバックテストの前提と一致するか。** 分割・配当の扱いが違えば成績が変わる。ADR-0016 決定 14 はバックテストに借株料・配当相当額を織り込むと定めており、**復権方式と配当の扱いが二重計上や欠落を起こさないかの確認が要る。**

### 単一障害点についての注記

06_technical/03_moomoo-integration の「リスク・未決事項」5 は「無料構成では市況データと発注が moomoo に集中する」ことを単一障害点として挙げている。**履歴データも moomoo に寄せると、この集中がさらに進む。** ただし履歴 OHLC は取引時のリアルタイム性を要求されず、取得済みデータをローカルに保持できるため、**発注・時価とは障害の性質が異なる**（moomoo が落ちてもバックテストは既存データで走る）。この差を踏まえた判断を求める。

## 実装で判明した経緯

実機確認セッション（2026-08-05）。#342（ADR-0019 の PoC）の項目 7 として、米国株日足 OHLC の取得可否を確認した。ADR-0019 決定 1 の項目 7（2026-08-04 追補）である。

quote 側の probe は `MMSPI_Qot`（133 メソッド）の実装を要するため、リフレクションから空実装を自動生成し、必要な 3 つ（`GetSecuritySnapshot` / `RequestHistoryKL` / 同 `Quota`）だけを実装した。

## 提案（計画への反映案）

- 反映先候補: **ADR-0023 の改定（新 ADR）** ／ 06_technical/02_datasource-candidates の更新
- 提案内容:
  1. **米国株日足 OHLC の履歴源を moomoo OpenAPI の履歴 K 線（`QotRequestHistoryKL`）とする決定を追加する。** 追加費用は生じない。
  2. **取得枠の単位・回復周期を確認する作業を実装側のタスクとして残す**（#382）。枠が銘柄数単位であれば、バックテスト対象銘柄数の上限が制約になる。
  3. **前復権の調整方式とバックテストの費用モデル（借株料・配当相当額）の整合を確認する。**
  4. 単一障害点の集中について、履歴データは発注・時価と障害の性質が異なることを踏まえて許容するかを判断する。

## 影響範囲

- **#382**（Stooq 取得不能への追随）: 本フィードバックが解決策になる。`StooqHistoricalBarSource` に代わる moomoo アダプタの実装が対象。
- **#16 / FR-15**（バックテスト基盤）: データ源が入ることで初めて実走できる。
- **IADR-0138**（Stage 0 の DD 厳格化）: 「実効は #382 の解決に依存する」という残余リスクが解消し得る。
- **ADR-0023**: 代替源の決定を追加。
- **02_datasource-candidates**: 情報源の区分に履歴 OHLC を追加。

---

## 追記（2026-08-06）: ADR-0023 の裁定結果と、実装側で追随した範囲

本記録は `status: open` のままである（**求めている「代替源を定める改定」は未裁定**）。ここに記すのは、
**既に裁定済みである ADR-0023 決定1〜4 の内容**と、それに対して実装側が
[#382](https://github.com/endazon/ai-stock-trading/issues/382) で追随した範囲である。

### 計画側で裁定済みの内容（ADR-0023・2026-08-04・環流 project-planning#57 への裁定）

| 決定 | 内容 | 実装への効き方 |
| --- | --- | --- |
| 決定1 | **Stooq は取得不能として扱い、ボット検知チャレンジの回避実装は書かない。** 区分は「検証用途」のまま残し、候補一覧からは削除しない | 実装・テストとも**維持**。回避実装は**書かない**（禁止事項としてコードにも明記） |
| 決定2 | 米国株日足 OHLC 履歴源が未確定であることを計画側で追跡可能にする。**解消するまで Stage 0 の合格判定を実施できない** | 既定 no-op の意味が「差し替え漏れ」から「**差し替え先の不在**」へ変わる |
| 決定3 | moomoo の履歴取得検証を **ADR-0019 の PoC 項目 7** として追加（品質の評価観点 6 点） | **本記録の実測がその項目 7 の結果である** |
| 決定4 | 検証の期限は **go-live 相当（基盤・可変機能ユニット双方の実装完了）を起算日とし 1 か月以内**。超過時は ADR-0005 の有料枠の検討へ自動的に移る | **期限の起算が PoC 項目 1〜6（2026-08-31）と異なる**。畳まないこと |

### 実装側が追随した範囲（#382・[IADR-0156](../docs/adr/IADR-0156_us-ohlc-history-source-absence.md)）

**振る舞いは変えていない（可視化のみ）。** コード・IADR・FR-15 の機能/テスト仕様書・`docs/blocked-tasks.md` に、
次の 4 点を**同時に**記した。1 点でも落とすと誤読になるためである。

1. 実装済みの履歴源は Stooq のみで、それは取得不能（回避しない）。
2. 既定は `none`（no-op）で、バーが取れなければ Stage 0 は不合格へ倒れる（fail-safe は壊れていない）。
3. **moomoo は代替源の候補として実測されたが、採用には ADR-0023 の改定裁定と実装の両方が要る**（いずれも未了）。
4. したがって **Stage 0 の合格判定は現時点で一度も発火し得ない**（恒久の状態）。

**「使える履歴源が無い」と単純化すると 3（本記録が報告した実測）を否定し、「moomoo で解決した」と書けば
裁定も実装も無いのに解決したように読める。** どちらの誤読も作らない書き方を実装側の全文書で統一した。

### ~~依然として計画側の裁定を要すること（本記録の要求は未充足）~~ **【解消済み 2026-08-06・後刻】**

> **本節は 2026-08-06 午前時点の記述であり、同日中に裁定が下りて解消した**（下の「追記（2026-08-06・後刻）」が正）。
> 削除せず残す——「裁定待ち」と書いた記録と、それが解けた記録の両方が経緯として意味を持つためである。

- **§提案 1（moomoo を履歴源とする決定を ADR-0023 の改定として追加する）は未裁定である。** そのため
  **moomoo アダプタは実装していない**（IADR-0156 決定6）。裁定前に結線すればテストが落ちる仕掛けを置いた
  （`未採用の代替源moomooを指定してもno_opへ倒れる_ADR0023の改定裁定待ち`）。
- §提案 2・3（取得枠の単位と回復周期／前復権と費用モデルの整合）は、採用が決まってから実機で確認する。
- §提案 4（単一障害点の集中を許容するか）も未裁定である。

---

## 追記（2026-08-06・後刻）: 裁定の内容と、実装側が追随した範囲

### 計画側の裁定（ADR-0023 決定5・利用者裁定・環流 project-planning#205）

| 本記録の提案 | 裁定 |
| --- | --- |
| 1. moomoo を履歴源とする決定を追加する | **✅ 採択。** 決定5 として追加（`QotRequestHistoryKL` / `KLType_Day` / `RehabType_Forward` / 1 リクエスト 1,000 件・`NextReqKey` でページング）。**追加費用なし**のため決定4 の有料枠（ADR-0005）は発動しない |
| 2. 取得枠の単位・回復周期を確認する作業を残す | **✅ 採択。** 決定5 の「実装側で確認を要する 2 点」の 1 として計画本文へ。**確認するまで本番のバックテストへ流さない** |
| 3. 前復権と費用モデル（ADR-0016 決定14）の整合を確認する | **✅ 採択。** 同 2 として計画本文へ |
| 4. 単一障害点の集中を許容するか | **✅ 許容。** 履歴 OHLC は停止しても取引が止まらず、発注・時価とは障害の性質が異なる。**二重化は別要求として起こす** |

**Stooq の扱い（決定1）は変更されなかった**（取得不能のまま候補として残す・回避実装は禁止）。

### 実装側が追随した範囲（[#382](https://github.com/endazon/ai-stock-trading/issues/382) の残り / [IADR-0157](../docs/adr/IADR-0157_moomoo-history-kline-adapter.md)）

- **`MoomooHistoricalBarSource` を実装した**（ページング・前復権・1,000 件・米国株のみ・欠測記録・レート自制）。
  SDK 依存は `MMApiMoomooHistoryKLineClient` の 1 クラスに閉じた。
- **既定 provider は `none` のままである。** moomoo は明示的に構成したときだけ使う。
  **提案 2・3 が未確認である以上、決定5 の前提が満たされていないためである。**
- 未確認 2 点は**取得のたびの警告ログ**・XML doc・IADR-0157・`docs/blocked-tasks.md` A-3 に残した。
- **PR [#415](https://github.com/endazon/ai-stock-trading/pull/415) が置いた「裁定が下りた日に赤くなる関門テスト」は
  設計どおり発火し、「採用後の正しい姿」へ書き換えた**（削除ではない・IADR-0157 決定5）。

### なお残ること（本記録の範囲外）

- 未確認 2 点の**実機確認**（実 OpenD が要る）。加えて**実 OpenD への疎通そのものを一度も検証していない**
  （protobuf の組み立て・`NextReqKey` の往復・`KLine.Time` の実書式）。いずれも `docs/blocked-tasks.md` A-3。

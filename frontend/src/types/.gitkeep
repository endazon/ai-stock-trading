# types 層（計画 13_frontend-stack §ディレクトリ構成・shared）: 共有の型。
#
# 本ユニットの共有型は BFF 契約の型であり、正規化関数と不可分であるため
# `src/lib/{risk,monitor}/contracts.ts` に値と同居させている（型だけをここへ切り出すと
# 型と正規化が別ファイルへ割れ、片方だけ直る事故を招く）。枠のみ（IADR-0290）。

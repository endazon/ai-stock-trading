# hooks/（計画 13_frontend-stack §ディレクトリ構成）: この feature 固有の React フック。
#
# 実測（#529 着手時）: 本 feature に画面内で閉じたフックは無い。状態は TanStack Query が持ち、
# フォームのローカル状態は各コンポーネントの `useState` に閉じている。
# **切り出すために抽象を作らない**（計画外の抽象化は避ける）。枠のみ。
